# UMD runtime dependency pin manifest (Plan E, P2-S2)
# ----------------------------------------------------
# Authoritative pins as resolved in the source of truth environment that runs
# the full UMD_TEST_POSTGRES=true suite (Python 3.13.5 / live Postgres 17).
# Container build pins these same versions in pyproject.toml; any bump requires
# the CVE + license review gates (deploy/security/CVE_WATCH.md,
# deploy/security/LICENSE_REVIEW.md) and a re-run of the full suite.
#
# Direct runtime dependencies (pinned):
fastapi==0.141.1
pydantic==2.13.4
pydantic-settings==2.15.0
pydantic-core==2.46.4
sqlalchemy==2.0.52
ocfl-py==2.1.0
alembic==1.19.1
psycopg[binary]==3.3.4
jsonschema==4.26.0
pypdf==6.16.2
pillow==11.3.0
pysubs2==1.9.0
python-multipart==0.0.32
uvicorn==0.30.6  # 0.30.7 was never published; 0.30.6 is the last real 0.30-line release

# Transitive runtime (locked at resolved versions for reproducible containers):
greenlet==3.5.5
Mako==1.4.1
typing-extensions==4.12.2
annotated-types==0.8.0
starlette==1.6.0
sniffio==1.3.1
certifi==2026.7.22
idna==3.19
six==1.17.0
jsonschema-specifications==2025.9.1
Pygments==2.21.0
PyYAML==6.0.2

# Dev / quality gates (not shipped in the runtime image):
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
httpx==0.28.1
ruff==0.16.4
mypy==2.3.1

# GATED / opt-in provider and security extensions — NOT part of the base image.
# Each is installed only through its documented opt-in extra and carries its own
# license/CVE review (see LICENSE_REVIEW.md). Pins here reflect the *required
# minimums* that the release gate enforces; the exact resolved version at build
# time must be recorded in the container SBOM:
#   faster-whisper==1.2.1   (MIT; exact resolved pin + model in deploy/pins/asr-runtime.md)
#   pyannote.audio>=3.0      (MIT, U1 legal gate)
#   PySceneDetect>=0.6       (BSD-3)
#   vllm>=0.6                (Apache-2.0; license interrogator for served weights)
#   hatchet-sdk==1.38.1     (SDK pin; CANDIDATE — PENDING live shape-test validation in Plan J)
#   hatchet-server==0.105.2  (server image pin; CANDIDATE — PENDING live shape-test validation
#                             in Plan J; no Docker locally so NOT release evidence).
#
#   P3-S2: the Dockerfile `worker` build target installs the pinned `.[worker]`
#   extra (hatchet-sdk==1.38.1) and smoke-tests the SDK import WITHOUT promoting
#   the pair. Installing the SDK in the worker image is NOT promotion; the pair
#   remains CANDIDATE/PENDING until real live validation on a hosted runner.
#   (upgrade = bump both surfaces in lockstep, new DAG universe, drain in-flight before activation)
#   hatchet-sdk (pinned exact — a build gate; see CVE_WATCH.md)
#   splink>=4, duckdb        (MIT)
#   vecalign>=1.0.1          (MIT)
#   pgvector          (Postgres 18.6 target; >=0.8.2 for CVE-2026-3172, target 0.8.6)