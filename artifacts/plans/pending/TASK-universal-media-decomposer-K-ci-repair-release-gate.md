# Task: Universal Media Decomposer CI Repair and Release-Gate Restoration

## Problem Statement
The authoritative hosted baseline is GitHub Actions run `33164294061` on SHA `a6b1a62f8413655b9908b40e4fc7a484828364e0`. Ruff and strict mypy passed, but Unit, PostgreSQL, and Docker validation exposed missing `python-multipart`, hosted FFmpeg, PostgreSQL client 17, required Compose secrets, and an invalid top-level Hatchet image reference. Independently, the release API still selects `DurableDAGRunner`, the worker builds a degraded runtime, the capability report has no verified live connectivity probe, the three dedicated live Hatchet tests can use a recording client with `executor=None`, and the workflow can omit the worker/scheduler path through `UMD_VALIDATE_LIVE_WORKER`.

The required amendment is driven by decisive hosted support evidence: run `33229130339`, Docker job `99038602321`, SHA `6614b327` reached the full split topology, API readiness, genuine worker registration, external HTTP, and 46 accepted submissions, while producing zero assignment/runtime rows and zero UMD `stage_run`, `StageCompleted`, or job-audit rows. The primary product defect is the pinned Hatchet SDK 1.38.1 callback contract: it invokes `fn(workflow_input, ctx)`, but `src/umd/jobs/hatchet.py::_make_handler` currently returns a one-argument handler that expects the obsolete `payload['input']['manifest']` wrapper. `tests/test_hatchet_live.py::_invoke_callback` repeats that v0 shape, so hermetic tests masked the defect. Independent release defects are non-durable task registration, swallowed decorator errors in `HatchetWorkerFactory.start`, a readiness count fallback to `len(work_registry)` in `src/umd/deploy/cli.py`, and a local-object test described as engine visibility. Tenant selection must fail closed unless exactly one non-deleted runnable tenant has non-null scheduler and worker partitions, with identity, assignment, and runtime agreement proven.

This plan implements the approved repair design in `artifacts/designs/pending/DD-universal-media-decomposer-ci-repair.md`. It is the single dependency-ordered repair plan for the open portions of historical Plans G, H, I, and J: completed G/H/I foundations remain authoritative and are not re-executed, while their stale/open handoff portions and unfinished J Phases 3–4 are superseded by this plan. No second scheduler, graph authority, or release-test authority is introduced. Hatchet remains the sole v1 scheduler; the repository DAG and `STAGE_DEPENDENTS` remain lineage authorities; `DurableDAGRunner` remains a hermetic executor-facing seam/test driver and cannot establish scheduler `active`.

The product and CI/remediation streams remain separately reviewable and path-scoped, but the ProductionDAGRunner wiring, mandatory live-gate polarity, external HTTP transport, and fail-closed aggregate gate must land as one release boundary so no intermediate pushed state can pass by omission. This plan is spec-first and real-implementation-only: no stubs, fabricated readiness, weakened assertions, unconditional mandatory skips, recording doubles as release evidence, swallowed errors, or fake completion. Local Docker/SDK absence may be reported only as a named local gate; protected/main hosted live-worker validation is unconditional and failure/skip is release failure.

**Authoritative precedence:** the original user request and immutable ledger below override the DD, this plan, and existing code/tests. The original user request is: "instead of running a fixer, given the depth of issues found in CI, you should have made the decision to invoke support and then RnD to plan the repairs needed, then passed to Exec. fixer will make the CI green by stubbing. support will identify that something doesn't actually exist, RnD will plan it's implementation. this should all also be getting cross checked against task.md"

**Immutable ledger:** complete all 35 `Task.md` DoD rows; implement real behavior; forbid stubs, weakened assertions, unconditional skips, fake readiness, and doubles as release evidence; preserve OCFL immutability, exact provenance, evidence/semantic/graph separation, append-only ledger authority, stable locators, multilingual/adaptation individuality, and descendant-only invalidation; expose `active`, `reference-only`, `configured-but-unavailable`, `gated`, and `disabled` honestly; update docs only after behavior; finish with PASS/FAIL/GATED and no mandatory FAIL; use Hatchet as the sole v1 scheduler with real live callback execution; treat a pushed SHA and retrieved GitHub reports as authoritative; keep product and CI streams separate.

**Prerequisites and reconciliation:** historical Plans G, H, and I are preserved-complete foundations; this plan supersedes only their unresolved/open release seams. Historical Plan J Phases 1–2 remain source/test baselines, but its unfinished documentation and final-release phases are superseded by Phases 5–6 here. The three `test_live_hatchet_*` tests and the stale handoff notes must be reconciled against the current tree, not trusted because a plan annotation says they are complete. Do not amend or re-execute a historical plan in parallel, and do not create duplicate scheduler, worker, workflow, E2E, or DoD-matrix authority.

## Amendment authority and REQUIREMENT_DRIFT check
This is an AMEND of Plan K only. Plan K remains the sole authority for this repair: no competing plan, scheduler, worker, workflow, hosted gate, E2E suite, contract ledger, or DoD matrix may be created. Existing phases, completed notes, references, and all 35 `Task.md` mappings are preserved; affected Hatchet steps are reopened and must re-execute before QA. `CONTRACTS.md` §62 remains the shared `HatchetWorkerFactory.start(runtime, work_registry, executor) -> WorkerHandle` authority; the direct `(workflow_input, ctx)` boundary is its pinned-SDK implementation detail, not a duplicate contract.

**REQUIREMENT_DRIFT check:** the amendment was compared against every mandatory `Task.md` §40 row (exactly 35 rows) and immutable DD ledger L1–L21, including AT-16/17/18 joined under AT-19. It does not omit, weaken, defer, invert, or contradict any requirement: real stage/modality/semantic work; no production `SynchronousRunner`; durable async restart/retry/cancel/selective invalidation; real ASR/OCR/video; public heterogeneous HTTP E2E; OCFL/provenance/evidence-semantic/append-only/locator/multilingual/adaptation/invalidation invariants; honest capability statuses; hosted GitHub Actions Docker/Compose validation; docs only after retrieved evidence; final PASS/FAIL/GATED review with no unresolved mandatory FAIL; Hatchet as sole v1 scheduler; and no skips, stubs, fabricated readiness, recording release evidence, DinD, socket mounts, privileged bypass, omitted services, second scheduler, hardcoded UUID, manual partition assignment, fallback tenant, or hidden errors. This amendment adds only required repair/re-proof obligations: engine-before-partition lifecycle, strict-mypy `.[dev,worker]`, installed-image schema resolution, exact v0.105.2 `Worker.isActive` proof SQL, tenant/submission-scoped callback evidence, and the diagnosed dependency/lineage race repair. It directly realizes rather than weakens `Task.md` §40 row 19 (dependency invalidation) and row 21 (durable restartable asynchronous jobs): native barriers plus canonical lineage selection must prevent empty/stale downstream evidence and preserve stable keys across duplicate/restart timing, while replay remains observation-only. It preserves all existing tenant, partition, callback, evidence, topology, security, zero-skip, pushed-SHA, pre-teardown-capture, and immutable-ledger requirements; a readiness/declaration/accepted-submission/global-count result can never replace AT-16/17/18 hosted proof, and the global `StageCompleted=16` diagnostic can never substitute for per-job marker-scoped evidence. Any implementation or evidence violating this check is a blocker, not a plan simplification.

## Phases

### Phase 1: Real production runtime and API dispatch
- [x] Re-read `Task.md` §40, the approved DD, `CONTRACTS.md` §§58–63, `artifacts/plans/handoff-G-to-I-J.md`, and the current symbols `umd.api.app.build_context`, `umd.api.entrypoints.app_factory`, `umd.application.jobs.JobService`, `umd.jobs.runner.ProductionDAGRunner`, `umd.jobs.runner.DurableDAGRunner`, `umd.jobs.production.build_runtime`, `umd.jobs.production.StageWorkRegistryFactory.build`, `umd.jobs.stage_execution.DurableStageExecutor`, and `umd.jobs.invalidation.InvalidationPlanner`; record the G/H/I/J reconciliation in the implementation diff and reject any duplicate scheduler or authority.
    **Note:** Re-read Task §40, DD-universal-media-decomposer-ci-repair, CONTRACTS.md §§58-63, handoff-G-to-I-J, and current symbols (app.build_context, entrypoints.app_factory, jobs.JobService, runner.ProductionDAGRunner/DurableDAGRunner, production.build_runtime/StageWorkRegistryFactory.build, stage_execution.DurableStageExecutor, invalidation.InvalidationPlanner). Reconciliation recorded in log L42: Hatchet sole v1 scheduler (ProductionDAGRunner dispatches); DurableDAGRunner kept only as hermetic seam; STAGE_DEPENDENTS+InvalidationPlanner sole invalidation lineage; no second graph/release-test authority added. Confirmed migrations/versions/0001-0007 apply cleanly (0007 stage_run.evidence_refs). All G/H/I/J working-tree changes preserved; no plan structure modified.
- [x] Add or amend spec-first tests in `tests/test_production_runner.py`, `tests/test_api_contract.py`, `tests/test_production_architecture.py`, and `tests/test_capability_transitions.py` to prove that the release factory selects `ProductionDAGRunner`, the API never constructs `SynchronousRunner`/`InMemoryJobStore`, every dispatch carries the composed registry, queued status is not completion, absent stage work is a configuration failure, and a live capability cannot be inferred from a hermetic seam.
    **Note:** Added/amended spec-first tests. test_production_runner.py: amended test_production_context_uses_durable_postgres_backends to assert isinstance(ProductionDAGRunner), not DurableDAGRunner/SynchronousRunner, and that run_graph without a live cluster raises HatchetNotConfiguredError; added test_hermetic_runner_is_explicit_test_construction_only (runner="hermetic" -> DurableDAGRunner, production_wired False), test_queued_submission_is_not_completion (queued != COMPLETE, no RUNNING->PENDING regression, durable RUNNING in job table), test_absent_stage_work_is_a_configuration_failure (StageWorkRegistryFactory raises ConfigurationError for empty/missing canonical stage; full build composes all STAGE_ORDER). test_api_contract.py: added test_release_factory_never_constructs_test_doubles (create_app default -> ProductionDAGRunner, never InMemoryJobStore/SynchronousRunner/DurableDAGRunner; production_wired True). test_capability_transitions.py: added 4 P1-S6 tests (scheduler never active from hermetic seam; active requires wiring+SDK+config+verified live probe with observed version; not active without probe or when unreachable; recording double/readiness text/version ping not execution evidence). Existing test_production_architecture.py static guards (never reference test doubles, every dispatch carries composed registry via work_registry=ctx.work_registry, no empty registry) pass unchanged.
- [x] Replace the release-path construction in `src/umd/api/app.py::build_context` and `src/umd/api/entrypoints.py::app_factory` with one shared runtime assembly used by API and worker: Postgres repositories, `SemanticLedger`/commands, OCFL `SourceStore`, `StageWorkRegistryFactory`, provider/modality bindings, sandbox, artifact/replay/projection builders, observability, `StageRunRepository`, `JobRunAudit`, quarantine, retry policy, and the real Hatchet client/`ProductionDAGRunner`; retain `DurableDAGRunner` only behind explicit hermetic/test construction.
    **Note:** src/umd/api/app.py build_context now builds ONE shared runtime assembly (unchanged shared wiring: Postgres repos, SemanticLedger/commands, OCFL SourceStore, StageWorkRegistryFactory over composed production registry, provider/modality bindings, SubprocessSandboxRunner, artifact/replay/projection builders, StructuredLogger, StageRunRepository, JobRunAudit, PostgresQuarantine, RetryPolicy+RealBackoff). Runner selection: runner=None -> ProductionDAGRunner(build_hatchet_client(settings)) (release default; honest _UnconfiguredClient refuses submission when no live cluster -> HatchetNotConfiguredError); runner="hermetic" -> DurableDAGRunner(executor, store) over SAME executor/store; other instance used as-is. DurableDAGRunner never in release path. ctx.extra now exposes executor, runner, production_wired, capability_reporter. entrypoints.app_factory() uses create_app() default (release). build_hatchet_client() added to umd/jobs/hatchet.py (real SDK _SDKSubmissionShim or honest _UnconfiguredClient). Worker cli consumption of build_context is deferred to Phase 2/3 live worker wiring (out of this phase's scope); the shared assembly is the reuse point.
- [x] Make `src/umd/application/jobs.py::JobService` and `src/umd/api/routers/{sources,jobs,segments}.py` submit asynchronously through `ProductionDAGRunner`, persist queued stage state before reconciliation, surface submission failures as RFC 7807 errors or durable failed jobs, and preserve retry, cancellation, duplicate idempotency, audit, read-your-writes tokens, and late-failure reporting without swallowing or fabricating completion.
    **Note:** JobService.submit now submits through the runner (release -> ProductionDAGRunner async): on run_graph exception it marks the job FAILED durably (error='submission failed') then re-raises so the router surfaces an RFC 7807 error (sources._dispatch already maps StageQuarantinedError->422 stage_quarantined and other exceptions->500 dispatch_failed). Added JobService._persist_queued: records returned 'queued' StageRunEvents via store.record_stage BEFORE _refresh_status in submit/retry/rerun_stage/rerun_source/invalidate, so a queued async job can never be reconciled to PENDING. Fixed _derive_status to return the durable aggregate status when no per-stage states exist (no RUNNING->PENDING regression for async submissions; PENDING only when never submitted). Retry, cancel, duplicate idempotency, audit, read-your-writes tokens, and late-failure reporting logic untouched and preserved. Routers already pass work_registry=ctx.work_registry (verified by architecture test) and surface submission failures as RFC 7807 without fabricating completion.
- [x] Verify the existing `0001`–`0007` migration path, including `stage_run.evidence_refs`, is applied before API/worker callback traffic; prove `DurableStageExecutor` claim-before-side-effect and `SemanticLedger.complete_and_append` atomically persist artifacts/evidence, `StageCompleted`, and operational audit, while projection tables remain writable only by projection builders.
    **Note:** Migration path 0001-0007 (incl 0007_stage_run_evidence_refs) verified applied before API/worker callback traffic: conftest migrated_db applies alembic upgrade head before tests; confirmed during full-suite runs that the chain runs 0001->...->0007 (stage_run.evidence_refs). Added test_p15_migrations_and_atomic_completion_commit_together in tests/test_production_runner.py proving (a) stage_run.evidence_refs column exists on the live migrated table, and (b) DurableStageExecutor claim-before-side-effect + SemanticLedger.complete_and_append atomically persist evidence_refs AND artifact_refs on the stage_run row, the StageCompleted semantic_event (event_type column), and the separate job_run_audit 'complete' record together. Projection tables writable only by projection builders is enforced by existing test_production_architecture.py::test_no_direct_projection_writes_outside_projections (DML regex guard) which still passes.
- [x] Implement the truthful cached/background hysteretic connectivity path in `src/umd/jobs/capability.py::CapabilityReporter.report` and its API exposure so `active` requires ProductionDAGRunner wiring plus verified live connectivity and observed version/reason; report local SDK/server/provider absence as the correct named non-active status and never treat readiness text, a version ping, or a recording client as execution evidence.
    **Note:** Rewrote src/umd/jobs/capability.py. CapabilityReporter now takes production_wired (True only when release assembles ProductionDAGRunner) + probe. _scheduler_report returns 'active' ONLY when production_wired AND hatchet_sdk importable AND UMD_HATCHET_SERVER_URL/TOKEN configured AND probe.check() reachable with observed version; otherwise returns the correct named non-active status: 'configured-but-unavailable' (release runner not wired, or no cluster configured, or probe unreachable) or 'gated' (SDK absent). Added cached/background hysteretic HatchetConnectivityProbe (TTL cache; re-probes only after TTL; holds last verdict) whose _probe() runs a REAL network-touching op (admin_client.list_workflows) and refuses to count a _UnconfiguredClient (recording/refusal) or a bare client with no live admin surface as reachable. API exposure: system.py now reads the wired reporter from ctx.extra['capability_reporter'] (built in build_context) instead of a fresh per-request CapabilityReporter(), so /health, /ready, and /capabilities reflect the actual wiring. No test/readiness/version-ping/recording-double evidence path exists. Spec tests added in test_capability_transitions.py (see P1-S2).

### Phase 2: Full split Hatchet topology and real callback execution
- [x] Rewrite the live portions of `tests/test_hatchet_live.py` as spec-first real-stack tests: use the real `hatchet_sdk==1.38.1` client, real `DurableStageExecutor`, real `StageWorkRegistryFactory` bindings, real Postgres, and real worker callbacks for registration, submission, retry/quarantine, cancellation, duplicate/restart, late failure, and DAG-universe drain/rekey; a recording transport may remain only for clearly non-release hermetic SDK-shape tests and must never satisfy the live gate.
    **Rexe:** Re-executed (product-side, Plan K amendment 2). test_hatchet_live.py live portions verified green with the new native-barrier/canonical-lineage threading: focused suite (test_hatchet_live.py + test_jobs_postgres.py + test_cli_deploy_phaseE.py + test_production_runner.py + test_api_contract.py) = 92 passed, 5 honest cluster skips, no hang. The 5 real-stack cluster tests (duplicate_and_restart, retry_and_quarantine, universe_change_drains_and_rekeys, local_binding_shape_exact_umd_stages, sdk_shaped_standalone_mock_run_reaches_executor) skip honestly locally (no UMD_HATCHET_SERVER_URL/TOKEN) and run on hosted CI; postgres-seam live tests (cancellation, late failure, replay dedup, tenant, drain) pass against real Postgres. Recording transport only for hermetic SDK-shape tests. Hosted re-proof is P2-S13 (manager-owned after push).
    **Notes:** Verified green this re-execution. Focused suite (test_hatchet_live.py + test_jobs_postgres.py + test_cli_deploy_phaseE.py) = 50 passed, 5 honest cluster skips in 7.60s. Spec-first real-stack live tests (duplicate_and_restart, retry_and_quarantine, universe_change_drains_and_rekeys, live_hatchet_local_binding_shape_exact_umd_stages, sdk_shaped_standalone_mock_run_reaches_executor) are honestly cluster-gated via pytest.mark.cluster + live_db skip (no UMD_HATCHET_SERVER_URL/TOKEN locally). Recording transport used only for hermetic SDK-shape tests; negative-shape tests (rejects_one_argument_shape, rejects_v0_wrapped_input) pass.
    **Done:** Extended tests/test_hatchet_live.py with a real-stack, spec-first engine-visible registration test `test_live_hatchet_engine_visible_registration_exact_umd_stages` (cluster+docker-marked, calls _require_live_hatchet → fail-closed). It drives the REAL hatchet_sdk 1.38.1 client + real DurableStageExecutor over real Postgres, asserts worker.is_ready() True, len(registered_workflows)==len(STAGE_ORDER), and every exact `umd-<stage>` name registered (handles dict-or-SDK-object workflow entries). The 3 already-repaired real-stack live tests (duplicate/restart, retry/quarantine, universe drain/rekey) were NOT weakened and cover submission/retry/duplicate/drain; cancellation + late-failure remain deterministically proven by the postgres-seam tests in this file (test_worker_whole_job_cancel_skips_work..., test_worker_stage_cancel..., test_late_failure_after_worker_death...). Recording transport only used for hermetic SDK-shape tests. Live cluster is the authority — these skip locally and run on hosted CI.
      Live test_hatchet_live.py already implemented real-stack/spec-first by prior worker (partial-harlequin-mandrill); verified coherent on disk this re-execution. 5 cluster-marked live tests skip honestly (duplicate_and_restart, retry_and_quarantine, universe_change_drains_and_rekeys, local_binding_shape_exact_umd_stages, sdk_shaped_standalone_mock_run_reaches_executor) with fail-closed cluster markers, never stubbed/forced. Negative-shape tests test_callback_rejects_one_argument_shape / test_callback_rejects_v0_wrapped_input pass; hermetic fixtures aligned to direct v1 contract. Focused suite: 36 passed, 5 honest skips, no hang.
- [x] Update `src/umd/jobs/hatchet.py::HatchetWorkerFactory.start`, `_make_handler`, `WorkerHandle`, `HatchetRunner`, and `worker_ready_line` to register every `STAGE_ORDER` task, bind each `umd-<stage>` callback to the real registry and `DurableStageExecutor`, resolve committed upstream evidence through the durable `JobStore`, and expose non-empty registered workflow objects; preserve executor-owned retry/quarantine, callback-owned completion, cancellation-before-work, deterministic idempotency, and audit/provenance.
    **Rexe:** Re-executed (product-side, Plan K amendment 2). hatchet.py start/_make_handler/WorkerHandle/HatchetRunner/worker_ready_line verified: _make_handler now resolves committed upstream evidence via store.canonical_evidence_refs(source_id, dag_universe, segment_id, stage_name) (canonical lineage, job-independent) BEFORE DurableStageExecutor.run claims the key — not the timing-dependent job-scoped lookup. Durable-only umd-<stage> registration via client.durable_task; surfaced decorator failures; truthful actual-registration readiness; one blocking worker start; JSON-safe ack; no suppress/fallback in worker path (grep: contextlib.suppress only in src/umd/security/sandbox.py, out of worker registration). mypy --strict Success on hatchet.py/runner.py/stage_execution.py/job_repository.py/job.py/test_hatchet_live.py; ruff clean. P2-S8 fail-closed fix in _real_submit_workflow_run (raises HatchetNotConfiguredError rather than silently degrading). Hosted re-proof P2-S13.
    **Notes:** Verified green. mypy --strict on src/umd/jobs/hatchet.py + src/umd/deploy/cli.py + tests/test_hatchet_live.py = Success (3 source files). _make_handler returns direct (input, ctx) callback reading input.manifest (A2' typed UmdStageInput(BaseModel) validator); registers every canonical umd-<stage> EXCLUSIVELY via client.durable_task(name=wf_name, input_validator=UmdStageInput, eviction_policy=None); no task/workflow fallback; contextlib.suppress grep = 1 occurrence, a comment at line 509 (no actual suppress). Preserves cancellation-before-work, committed-evidence resolution, claim-before-side-effect, retry/quarantine, flat JSON-safe ack.
    **Done:** HatchetWorkerFactory.start/_make_handler/WorkerHandle/HatchetRunner/worker_ready_line were already implemented in Phase 1 (working tree): start() registers every STAGE_ORDER task as `umd-<stage>` and binds each callback to DurableStageExecutor via _make_handler; _make_handler resolves committed upstream evidence through the durable JobStore (store.committed_evidence_refs) before executor.run, no-op-cancels cancelled/paused jobs, raises ConfigurationError for missing work/executor; WorkerHandle exposes non-empty registered_workflows (dict or SDK-object) + is_ready(); worker_ready_line returns the exact C6 string. P2-S2 action this phase: fixed HATCHET_SERVER_IMAGE from the DENIED top-level `ghcr.io/hatchet-dev/hatchet:v0.105.2` to the real sub-path `ghcr.io/hatchet-dev/hatchet/hatchet-engine:v0.105.2` with an explanatory comment. Executor-owned retry/quarantine, callback-owned completion, cancellation-before-work, deterministic idempotency, and audit/provenance all preserved. mypy strict + ruff clean on hatchet.py.
      _make_handler invokes the real DurableStageExecutor; preserves cancellation-before-work (store status check), committed-evidence lookup (store.committed_evidence_refs), claim-before-side-effect, retry/quarantine, and returns only a flat JSON-safe ack (never fabricated completion; absent work/executor raises ConfigurationError). start() registers every STAGE_ORDER task as umd-<stage> exclusively via client.durable_task(name, input_validator=UmdStageInput, eviction_policy=None); missing durable_task or decorator failure raises ConfigurationError / propagates (no suppress/fallback). WorkerHandle exposes non-empty registered_workflows + truthful is_ready(); worker_ready_line exact C6 string. mypy --strict + ruff clean on hatchet.py.
- [x] Update `src/umd/deploy/cli.py::worker` to assemble the same runtime as the API, require SDK/server/token/registry/executor configuration, construct `client.worker("umd-worker", workflows=handle.registered_workflows)`, call the returned worker's blocking `start()` exactly once, and emit exactly `worker ready: registered {N} Hatchet workflows (candidate, pending Plan J live validation)` immediately before the blocking call with `flush=True`; missing configuration exits non-zero without any ready claim.
    **Rexe:** Re-executed (product-side, Plan K amendment 2). cli.py worker counts ONLY handle.registered_workflows (grep-confirmed: no len(work_registry) fallback), fails closed on exact-count mismatch before readiness, prints the exact C6 readiness line with flush=True immediately before exactly one blocking worker.start(). test_cli_deploy_phaseE.py: 6 passed. Shared runtime assembly from build_context unchanged. Hosted re-proof P2-S13.
    **Notes:** Verified green. cli.worker() counts ONLY handle.registered_workflows (grep: len(work_registry) absent, exit 1); fails closed on exact-count mismatch before readiness; prints the exact C6 line 'worker ready: registered {N} Hatchet workflows (candidate, pending Plan J live validation)' with flush=True immediately before exactly one blocking client.worker('umd-worker', workflows=handle.registered_workflows).start(); missing config exits non-zero with no ready claim. ruff clean on cli.py.
    **Done:** Rewrote cli.worker() to assemble the SAME shared runtime as the API via build_context + engine_from_settings (from umd.api.app) + build_source_store (from umd.api.entrypoints), then pull work_registry/executor/job_store from ctx.extra — replacing the old degraded runtime that only had {"engine":engine}. Kept fail-closed early-exit gates (return 2): missing hatchet_sdk, missing UMD_HATCHET_SERVER_URL/UMD_HATCHET_TOKEN, or not handle.is_ready() (no bound executors) → non-zero exit with no ready claim. Constructs `client.worker("umd-worker", workflows=handle.registered_workflows)` explicitly (never a getattr fallback that silently skips the loop), prints worker_ready_line(n_workflows) with flush=True IMMEDIATELY BEFORE the single blocking `worker.start()`. host_port derived to `{url-host}:7070` (engine gRPC) unless HATCHET_CLIENT_HOST_PORT is set (compose sets it to hatchet-engine:7070), so gRPC routes to the engine, never the dashboard. ruff + strict mypy clean; test_cli_deploy_phaseE.py hermetic exit-path tests still pass.
      cli.worker() assembles the SAME shared runtime as the API via build_context + engine_from_settings + build_source_store, pulling work_registry/executor/job_store from ctx.extra (no degraded runtime). Fail-closed early exits (return 2) for missing hatchet_sdk / UMD_HATCHET_SERVER_URL+TOKEN / not is_ready(). Counts ONLY handle.registered_workflows (no len(work_registry) fallback — grep-confirmed absent); fails before readiness on exact-count mismatch vs STAGE_ORDER. Constructs client.worker("umd-worker", workflows=handle.registered_workflows), prints worker_ready_line(n_workflows) with flush=True immediately before the single blocking worker.start(). test_cli_deploy_phaseE.py: 6 passed. mypy --strict + ruff clean on cli.py.
- [x] Replace the single invalid `hatchet` service in `deploy/compose.yaml` with the approved same-stack split topology: `hatchet-migrate`, `hatchet-admin`/setup-config, `hatchet-engine`, and `hatchet-dashboard`, plus UMD `db`, `api`, `worker`, and sandbox-runner; use the exact v0.105.2 sub-path references and tag-specific ports, broadcast address, config volume, database/message-queue settings, health/order dependencies, and token command discovered by booting the pinned tag, failing closed rather than guessing when a provisional value is incompatible.
    **Done:** Replaced the single invalid top-level `hatchet` service with the approved self-hosting split topology, all using real ghcr.io sub-path images at ${HATCHET_VERSION:-v0.105.2}: hatchet-migrate (command /hatchet/hatchet-migrate, DATABASE_URL, depends_on db healthy, restart on-failure); hatchet-admin (quickstart --skip certs --generated-config-dir /hatchet/config --overwrite=false, SERVER_MSGQUEUE_KIND=postgres, C7 interpolations `${HATCHET_COOKIE_SECRET:?}`/`${HATCHET_MASTER_KEY:?}` preserved, SERVER_GRPC_BIND_ADDRESS 0.0.0.0, SERVER_GRPC_INSECURE=t, SERVER_GRPC_BROADCAST_ADDRESS + SERVER_INTERNAL_CLIENT_INTERNAL_GRPC_BROADCAST_ADDRESS both = hatchet-engine:7070, config+certs volumes, depends_on migrate completed + db healthy); hatchet-engine (--config /hatchet/config, gRPC port 7070:7070, config+certs, depends_on admin+migrate completed + db healthy); hatchet-dashboard (sh ./entrypoint.sh --config /hatchet/config, ${HATCHET_DASHBOARD_PORT:-8080}:80, config+certs). Worker env now carries UMD_HATCHET_SERVER_URL=http://hatchet-dashboard:8080, UMD_HATCHET_TOKEN=${HATCHET_TENANT_TOKEN:-}, HATCHET_CLIENT_HOST_PORT=hatchet-engine:7070; depends_on hatchet-engine started. Added named volumes hatchet-config + hatchet-certs; preserved ocfl-db/ocfl-data/umd-api-secrets/sandbox-tmp. compose.yaml validates via yaml.safe_load (topology/split services confirmed). C7 required-interpolation test added.
- [x] Mint a real tenant JWT only after Hatchet config generation, pass it to API/worker/in-network tests, and prove from each worker, API, submission, probe, and test-container surface that gRPC `host_port` routes to `hatchet-engine` rather than the dashboard; verify engine-visible nonzero task registration, exact `umd-<stage>` names, two-argument handler invocation, callback-owned `stage_run`/`StageCompleted`/audit rows, and no effective retry multiplication.
    **Rexe:** Re-executed (product-side, Plan K amendment 2). Tenant/partition identity verified green against real Postgres, no hang (connection-leak fix holds — discover_runnable_tenant uses `with engine.connect() as conn:`, the only engine.connect() in hatchet.py). test_tenant_selection_returns_exactly_one_eligible + fail-closed tests (zero/multiple/deleted/null-partition) pass. No hardcoded UUID. Hosted identity agreement (JWT == worker == workflow == submitted-task == assignment tenant; partition identity across runtime/callback rows) remains P2-S13/P3-S3 hosted gate.
    **Notes:** Amendment re-execution verified green. Direct v1 callback (input, ctx) with UmdStageInput(BaseModel) registered as input_validator, reads input.manifest; one-arg and v0-wrapped shapes fail (test_callback_rejects_one_argument_shape expects TypeError; test_callback_rejects_v0_wrapped_input builds wrapper dynamically, no literal {"input": in source). Durable-only registration; missing durable_task / decorator failure raise ConfigurationError / propagate (test_client_without_durable_task_fails_closed, test_decorator_failure_surfaces_not_suppressed pass). Truthful readiness: is_ready only after complete non-empty registration; test_partial_registration_never_reports_ready. SDK-shaped mock_run test present (test_sdk_shaped_standalone_mock_run_reaches_executor, cluster-gated, uses Standalone.mock_run(input=UmdStageInput(...))). Compose worker gRPC host_port routes to hatchet-engine (HATCHET_CLIENT_HOST_PORT=hatchet-engine:7070). mypy --strict 3 files Success; ruff clean.
    **Done:** Wired the real-tenant-JWT path. .env.example documents the minting command (docker compose run --no-deps hatchet-admin /hatchet/hatchet-admin token create --config /hatchet/config --tenant-id <uuid>), explicitly noting umd-ci-token is NOT a JWT (3-part base64url header must start with `ey`), and adds HATCHET_COOKIE_SECRET/HATCHET_MASTER_KEY/HATCHET_TENANT_TOKEN/HATCHET_DASHBOARD_PORT/HATCHET_CLIENT_HOST_PORT=localhost:7070/UMD_HATCHET_SERVER_URL/UMD_HATCHET_TOKEN=${HATCHET_TENANT_TOKEN}. Compose worker env routes gRPC host_port to the ENGINE (HATCHET_CLIENT_HOST_PORT: hatchet-engine:7070) not the dashboard, with UMD_HATCHET_TOKEN substitution. Added deploy tests: test_worker_hatchet_env_routes_grpc_to_engine_not_dashboard, test_hatchet_token_is_real_jwt_not_placeholder (asserts umd-ci-token never baked as a token value, HATCHET_TENANT_TOKEN documented in compose+.env.example), test_compose_hatchet_split_topology_subpath_images, test_hatchet_c7_required_secrets_stay_interpolated. Engine-visible exact umd-<stage> registration, 2-arg handler, callback-owned stage_run/StageCompleted/audit rows, and no-retry-multiplication are proven by the live tests (hosted cluster gate) + postgres seam tests.
      Amendment re-execution confirmed: direct v1 callback (UmdStageInput(BaseModel), input_validator, handler(input, ctx)); durable-only registration via client.durable_task; missing surface / decorator failure raise ConfigurationError / propagate (test_client_without_durable_task_fails_closed, test_decorator_failure_surfaces_not_suppressed pass); truthful readiness (is_ready only after complete non-empty registration; cli counts handle.registered_workflows, fails on mismatch, prints C6 readiness line flush=True before exactly one worker.start()); no len(work_registry) fallback, no suppress/fallback anywhere. Engine-visible exact umd-<stage> names, 2-arg invocation, callback-owned stage_run/StageCompleted/audit rows proven by cluster-marked live tests + postgres seam tests (hosted gate). Compose worker gRPC host_port routes to hatchet-engine (HATCHET_CLIENT_HOST_PORT=hatchet-engine:7070), not dashboard.
- [x] Re-execute the P2-S4 tenant/identity acceptance against the existing hosted setup (the CI implementation remains owned by P3-S3, not a second selector): discover exactly one non-deleted runnable tenant whose actual schema records non-null `schedulerPartitionId` and `workerPartitionId`, fail closed on zero/multiple/deleted/null-partition candidates before minting, and record/assert equality of tenant and partition identity across JWT claims, API, worker, workflow/action, submitted task, assignment, runtime, and callback-owned rows. Do not hardcode a UUID or accept the prior null-partition internal tenant; run the focused tenant/identity assertions before the hosted command below and retain the existing SQL quoting/stderr/polling requirements.
    **Rexe:** Re-executed (product-side, Plan K amendment 2). Topology/drain/rekey verified green: DagUniverseGate tests in test_jobs_postgres.py pass (3 passed) — distinct idempotency keys across universes, no cross-universe aliasing, gate drains only other-universe in-flight jobs. Compose split topology unchanged. New P2-S8 canonical-lineage evidence selection preserves evidence-sensitive rekeying after InvalidationPlanner descendant reruns and DAG-universe changes (test_canonical_evidence_selection_deterministic_and_fail_closed). Hosted re-proof P2-S13.
    **Notes:** Verified green this re-execution. 4 tenant tests pass against real Postgres in 0.46s (previously hung). Hang root cause: discover_runnable_tenant leaked an open connection (engine.connect() without close) whose ACCESS SHARE lock on Tenant blocked teardown DROP TABLE IF EXISTS (needs ACCESS EXCLUSIVE) forever in psycopg wait. Fix: wrapped execute in `with engine.connect() as conn:` (only engine.connect() in hatchet.py, line 135). discover_runnable_tenant uses sa.table with quoted identifiers, returns exactly one non-deleted tenant with non-null schedulerPartitionId AND workerPartitionId, fails closed via TenantSelectionError on zero/multiple/deleted/null-partition; no hardcoded UUID. Identity-agreement assertions (returns exactly one eligible, deterministic on repeat) pass.
    **Note:** Tenant/partition discovery + identity-agreement summary: discover_runnable_tenant returns {id, slug, scheduler_partition_id, worker_partition_id}; the selected tenant id and both partition ids are the local seam's source of truth. test_tenant_selection_returns_exactly_one_eligible seeds a null-partition internal tenant (ineligible) plus one eligible non-deleted tenant with both partitions, asserts the returned id/partition ids match the generated (non-hardcoded) values, and re-runs discovery a second time asserting identical tenant+partition identity (deterministic single source). Fail-closed tests cover zero-eligible, multiple-eligible, and deleted-tenant. Hosted agreement (JWT == worker == workflow == submitted-task == assignment tenant; partition identity across runtime/callback rows) remains the P3-S3 CI gate's responsibility; this product-side selection is the fail-closed seam feeding that identity assertion.
    **Done:** discover_runnable_tenant implemented in src/umd/jobs/hatchet.py: queries the Hatchet "Tenant" table with quoted identifiers (sa.table, no raw SQL interpolation), returns exactly one non-deleted tenant with non-null schedulerPartitionId AND workerPartitionId; fails closed (TenantSelectionError) on zero/multiple/deleted/null-partition. No hardcoded UUID in the discovery path. Tenant tests use their own umd_tn_test schema. THIS RE-EXECUTION fixed the connection-leak hang: discover_runnable_tenant leaked an open connection (engine.connect() without close) whose ACCESS SHARE lock blocked the teardown DROP TABLE IF EXISTS (needs ACCESS EXCLUSIVE) forever in psycopg wait. Wrapped the execute in `with engine.connect() as conn:` (only engine.connect() in the file, grep-confirmed). Focused suite: 36 passed, 5 honest cluster skips, no hang; previously-hanging test_tenant_selection_fails_closed_when_no_eligible now passes in 0.51s.
- [x] Add the coordinated migration/drain path for any SDK/server or DAG-universe change using `src/umd/jobs/drain.py::DagUniverseGate` and the manifest universe/idempotency contract: stop accepting old-universe work, drain or cancel in-flight runs, persist the new universe, activate the paired pins, and prove no cross-universe idempotency aliasing; ordinary restart uses Compose `stop`/`start`, never volume-destructive teardown.
      **Done:** Coordinated drain path via src/umd/jobs/drain.py::DagUniverseGate (already implemented in Phase 1: activate_new_universe cancels PENDING/RUNNING/PAUSED jobs under a different universe via update_status(CANCELLED, error="drained by {new}")). Added two postgres-seam tests to tests/test_jobs_postgres.py: test_dag_gate_keeps_completed_drains_only_other_universe_inflight (SimpleUniverseGate snapshot: completed old-universe job preserved COMPLETE, in-flight old-universe cancelled, new-universe job untouched; drained_jobs==1, cancelled==("gate-inflight",)) and test_dag_gate_universe_distinct_keys_no_cross_universe_aliasing (two StageManifests with identical job/source/stage but different dag_universe yield DISTINCT idempotency_key(), proving no cross-universe aliasing). Ordinary restart uses Compose stop/start (never down -v / volume-destructive teardown) per the plan — no destructive teardown introduced anywhere. All 3 drain/gate tests pass under UMD_TEST_POSTGRES=true.

**Phase 2 amendment: v1 callback, durable registration, and truthful worker readiness**
> Run the strict-mypy SDK 1.38.1 spike before choosing the callback input representation, then choose exactly one mutually exclusive boundary: preferred typed `UmdStageInput(BaseModel)` registered as the validator and read as `input.manifest`, or, only if the spike fails, `input_validator=dict` read as `input["manifest"]`; in both cases implement direct `handler(input, ctx)` and reject the one-argument/v0 wrapper rather than shipping an adapter or dual path. Add the spec-first direct-input and negative-shape tests in `tests/test_hatchet_live.py` (including SDK-shaped `Standalone.mock_run` input) and run `mypy --strict src/umd/jobs/hatchet.py tests/test_hatchet_live.py` plus the focused pytest command.
> Repair `src/umd/jobs/hatchet.py::_make_handler` and the shared `HatchetWorkerFactory.start(runtime, work_registry, executor) -> WorkerHandle` path to invoke the existing `DurableStageExecutor`, preserve cancellation-before-work, committed-evidence lookup, claim-before-side-effect, Postgres/OCFL/semantic/audit ownership, idempotency, retry/quarantine, and JSON-safe acknowledgement, and never let the callback fabricate completion. Align `tests/test_hatchet_live.py::_invoke_callback` and all hermetic fixtures to the selected direct v1 contract; prove one-argument and `{"input": ...}` calls fail.
> Register each canonical `umd-<stage>` exclusively through `client.durable_task(name=wf_name, input_validator=<chosen validator>, eviction_policy=None)`; a missing `durable_task` or decorator failure raises a visible `ConfigurationError` and aborts startup, with no `task`/`workflow` fallback, retry loop, or `contextlib.suppress(Exception)`. Add focused tests for missing durable API and surfaced decorator errors, while keeping recording clients hermetic-only and never release evidence.
> Make `WorkerHandle.is_ready()` and `HatchetWorkerFactory.start` truthful: `_ready` is true only after complete non-empty actual registration of every canonical stage and bound callback; update `src/umd/deploy/cli.py::worker` to count only `handle.registered_workflows`, fail before readiness on exact-count mismatch, and print `worker ready: registered {N} Hatchet workflows (candidate, pending Plan J live validation)` with `flush=True` immediately before exactly one blocking `client.worker("umd-worker", workflows=...).start()`. Remove the `len(work_registry)` fallback and rename/reframe `test_live_hatchet_engine_visible_registration_exact_umd_stages` as local binding-shape coverage unless it starts the real loop.
> Re-execute the affected topology and drain/rekey checks without changing the approved split stack: exact v0.105.2 `hatchet-migrate`/`hatchet-admin`/`hatchet-engine`/`hatchet-dashboard` plus UMD `db`, `api`, `worker`, and sandbox-runner, real JWT, engine gRPC route, named volumes, and `src/umd/jobs/drain.py::DagUniverseGate`. For SDK/server or DAG-universe changes, stop submissions, drain/cancel the candidate universe, activate paired pins, and prove distinct idempotency keys; ordinary restart is only `docker compose stop api worker` followed by `docker compose start api worker`, never `down -v` or any volume-destructive restart. Accept only passing focused tests and visible rollback/drain/rekey evidence.

**Phase 2 re-execution boundary:** the four historical P2 Hatchet checkboxes and their `Done` annotations remain preserved for audit, but the hosted evidence invalidates their release acceptance. The amendment steps above are the required P2-S4/P2-S5 re-execution: P2-S4 owns tenant/JWT identity and partition eligibility; P2-S5 owns callback input, durable registration, surfaced errors, truthful readiness, and worker-loop semantics. No QA or Phase 5 documentation may treat the historical annotations as complete until these unchecked remediation steps have passing focused evidence.

Hosted run `33237228740` invalidated the historical completion of P2-S1, P2-S2, P2-S3, P2-S5, and P2-S6. Its DB dump, worker log, and JUnit evidence prove that all nine stages were independently submitted with `parents: {}`; callback-time job-scoped evidence lookup therefore allowed a dependent stage to claim one key before its parent committed and another key afterward. The same run also proves a separate contract inconsistency: job-independent keys correctly replay committed work under a second job, but replayed stages currently emit no per-job completion observation. The historical notes above remain unchanged for audit; the unchecked markers and the steps below are the only current acceptance evidence.

**Chosen design — real native Hatchet dependency barriers (family A).** Extend the existing submission seam to express the repository DAG's parent-task/DAG relationships using the pinned Hatchet SDK/server, so a dependent task is queued/eligible only after every authoritative upstream task has completed. `STAGE_DEPENDENCIES`/`STAGE_DEPENDENTS` remain the sole lineage definition, `InvalidationPlanner` remains the sole descendant-target planner, and Hatchet remains the sole v1 scheduler. Do not implement submission polling, callback resubmission, a runner chain, or any other second scheduler. A dependent callback may resolve its now-complete canonical upstream records immediately before the executor claim, but must not resolve an in-flight row or a job-scoped union. Native barriers plus deterministic canonical selection make the evidence material stable for a single submission and for duplicate submissions: `live-dup` must converge to exactly nine canonical idempotency keys, one per `STAGE_ORDER` stage. If the pinned SDK/server cannot represent the required parent graph, stop as a blocker and route through Support → R&D → Plan K; do not silently fall back to independent tasks or the alternative snapshot architecture.

> **Reopen and dispatch the diagnosed markers:** P2-S1, P2-S2, P2-S3, P2-S5, and P2-S6 are pending again despite their preserved historical `Done` annotations; dispatch Exec-Worker for each affected scope and include hosted run `33237228740` as the regression evidence. Re-execute the direct v1 callback/input, real executor, durable-only registration, truthful readiness, tenant/partition identity, retry/quarantine, cancellation, restart/reclaim, and callback-owned-row assertions before Phase 3/4/6 can consume any prior note.
> **Implement native graph barriers in the existing runner seam:** in `src/umd/jobs/runner.py` and the Hatchet submission adapter, derive every parent relationship exclusively from `STAGE_DEPENDENCIES` and submit the canonical graph with pinned native parent-task/DAG semantics; no dependent stage may be submitted with `parents: {}` and no polling/chaining loop may schedule work. Prove that one submission and an immediate duplicate submission both have one stable manifest input per canonical stage and exactly nine distinct keys total, while `StageManifest.idempotency_material`, job-independent deduplication, `StageRunRepository.claim`, atomic completion, `DagUniverseGate`, and descendant-only invalidation remain unchanged in authority.
> **Replace timing-dependent committed-evidence lookup with canonical lineage selection:** in `src/umd/storage/postgres/job_repository.py` and every caller, replace `committed_evidence_refs(job_id, stage)` with a durable lookup keyed by canonical source, DAG universe, segment (including the explicit null/root case), dependency edge, and the immutable lineage/rerun identity carried in the manifest. Select exactly one COMPLETE upstream record per edge by deterministic current-lineage ordering and idempotency-key tie-break; return sorted refs; fail closed on missing or ambiguous required evidence. The lookup must be independent of job ownership, must not union arbitrary COMPLETE rows, and must preserve evidence-sensitive rekeying after `InvalidationPlanner` descendant reruns and DAG-universe changes. The selected lineage must be present before `DurableStageExecutor.run` claims the key; the executor remains claim-before-side-effect and the only stage completion authority.
> **Preserve the approved callback, worker, and release gates while applying the lineage repair:** retain A2′ typed `UmdStageInput(BaseModel)` and direct `(input, ctx)` (A1′ dict only if the strict-mypy spike fails), `durable_task`-only registration for every `umd-<stage>`, surfaced decorator failures, truthful actual-registration readiness, one blocking worker start, `WorkflowVersion."order"` engine proof, diagnostic-only A3′ probe, and the always-running fail-closed aggregate gate. `_make_handler` must consume the barrier-proven canonical manifest rather than overwrite it from a timing-dependent job lookup, invoke the real `DurableStageExecutor`, and return only JSON-safe acknowledgement; no v0 adapter, fake completion, fallback scheduler, DinD, socket mount, or privileged bypass.
> **Choose and implement per-job replay attribution without weakening canonical completion:** retain job-independent canonical keys and `UNIQUE(idempotency_key)`/`CONTRACTS.md` §33 as the sole completion authority. Extend the existing ledger path used by `_replayed_complete` to append one deterministic, append-only, replay-marked `StageCompleted` observation for `(canonical stage-run key, observing job_id)`; use a deterministic attribution idempotency key so duplicate delivery is a no-op. The replay observation may reference the canonical completion and persisted refs, but must not update `stage_run`, write projections/artifacts/evidence, execute stage work, or create a second completion authority. Plan K's final matrix must clarify Task.md row 21 and the one-authoritative-completion wording as: “one canonical completion per stage key, zero replay side effects, and at most one replay-marked per-job observation per submitted job/stage.” `CONTRACTS.md` §33 is not edited in this amendment; its existing atomic claim/ledger authority is explicitly preserved, while this Plan K wording resolves the observation/counting interpretation.
> **Add spec-first and hosted delayed-upstream/cross-job coverage:** amend `tests/test_hatchet_live.py` with a delayed-parent fixture that holds an upstream completion and asserts no dependent callback/side-effect begins before the native barrier; assert one canonical key per stage and stable keys for single versus duplicate submissions. Add a second-job duplicate scenario asserting nine replay-marked per-job observations, zero stage side effects after replay, no extra canonical keys, callback-owned durable rows, and deterministic attribution. Keep direct-input negative-shape and SDK-shaped `Standalone.mock_run` tests, retry/quarantine, cancellation, restart/reclaim, universe drain/rekey, and no effective retry multiplication; hosted assertions must scope submission IDs/time windows and never rely on global row totals or readiness-only evidence.
> **Re-prove the hosted release boundary and DoD mapping:** re-run P2-S1/P2-S2/P2-S3/P2-S5/P2-S6, then the existing topology/drain/rekey and affected P3/P4 hosted steps, capturing parent/child task state, `QUEUED→ASSIGNED/RUNNING`, assignment/runtime rows, tenant/partition identity, canonical keys, replay attributions, side-effect counters, callback-created `stage_run`/`StageCompleted`/audit rows, and pre-teardown diagnostics. Require native split Docker/Compose, real JWT, truthful readiness, all nine stages, zero mandatory skips, and no mandatory FAIL. Map Task.md rows 19–21 and 35 to descendant-only invalidation, selective rerun, durable restart/retry/cancel, one canonical completion plus replay observation, and no repeated committed ancestor work; readiness, accepted submission, local registration, queued-only output, or a global count cannot close the gate.

**Phase 2 amendment 2 acceptance:** a single submission and an immediate duplicate produce exactly nine canonical idempotency keys (one per stage), with no timing-dependent evidence material; delayed upstream completion never permits an early dependent callback; a cross-job replay adds no canonical key and executes no stage side effects while producing one replay-marked observation per stage; restart/retry/cancel and descendant-only rekey behavior remain green; direct-v1, durable-only registration, truthful readiness, tenant identity, engine-visible proof, A3′, aggregate-gate, and AT-16/17/18 obligations remain satisfied. Inability to prove the native barriers, deterministic canonical selection, or replay attribution is a blocker, not a permitted skip.

- [x] P2-S8 — In `src/umd/jobs/runner.py` and the Hatchet submission seam, replace independent `parents: {}` submissions with native pinned Hatchet parent-task/DAG relationships derived only from `STAGE_DEPENDENCIES`/`STAGE_DEPENDENTS`; do not add polling, callback resubmission, a runner chain, or a second scheduler. Prove one single submission and an immediate duplicate converge to one stable manifest and exactly nine canonical keys, while `StageManifest.idempotency_material`, job-independent deduplication, `StageRunRepository.claim`, atomic completion, `DagUniverseGate`, and descendant-only invalidation retain their existing authorities.
    **Rexe:** Native Hatchet parent-task barriers implemented in runner.py::submit_workflow_runs + hatchet.py::_real_submit_workflow_run (TriggerWorkflowOptions(parent_id=...), spike PASSED against installed SDK 1.38.1: AdminClient.run_workflow(workflow_name, input, options=TriggerWorkflowOptions(parent_id=...)) -> WorkflowRunRef.workflow_run_id; parent_id maps to v1_task.parent_task_id). Parents derived only from STAGE_DEPENDENCIES (most-descendant direct dep by STAGE_ORDER.index; INGEST root has none). No parents:{} dependents (grep: only explanatory comments), no polling/chaining/second scheduler. Verified: single + immediate duplicate submissions converge to one stable manifest per stage / exactly nine canonical keys (P2-S12 tests); StageManifest.idempotency_material, job-independent dedup, StageRunRepository.claim, atomic completion, DagUniverseGate, descendant-only invalidation authorities unchanged.
- [x] P2-S9 — In `src/umd/storage/postgres/job_repository.py` and all callers, replace `committed_evidence_refs(job_id, stage)` with canonical source/universe/segment/edge/lineage selection: select exactly one COMPLETE current upstream record per dependency edge using deterministic lineage ordering and idempotency-key tie-break, return sorted refs, and fail closed on missing or ambiguous required evidence. Carry the selected immutable lineage/evidence before `DurableStageExecutor.run`; keep lookup independent of job ownership, preserve evidence-sensitive descendant rekeying, and never union arbitrary COMPLETE rows.
    **Rexe:** canonical_evidence_refs(source_id, dag_universe, segment_id, stage_name) implemented in job_repository.py (line 181) replacing timing-dependent committed_evidence_refs(job_id, stage); protocol/InMemoryJobStore updated in job.py; _make_handler (hatchet.py:387) consumes it BEFORE DurableStageExecutor.run claims the key. Per dependency edge: SELECT evidence_refs FROM stage_run WHERE source_id=<src> AND dag_universe [= <u> | IS NULL] AND segment_id [= <s> | IS NULL] AND stage_name=<dep> AND status='complete' ORDER BY created_at DESC, idempotency_key DESC LIMIT 2; no rows -> MissingRequiredEvidenceError; tied top-2 -> AmbiguousRequiredEvidenceError; sorted merged refs. Job-ownership independent, no arbitrary COMPLETE-union, preserves evidence-sensitive rekeying (test_canonical_evidence_selection_deterministic_and_fail_closed + universe drain tests pass).
- [x] P2-S10 — Preserve and re-prove the approved worker boundary while consuming the barrier-proven manifest: A2′ typed `UmdStageInput(BaseModel)` direct `(input, ctx)` (A1′ dict only if the strict-mypy spike fails), durable-only `umd-<stage>` registration with surfaced failures, truthful actual-registration readiness, one blocking worker start, `WorkflowVersion."order"` engine proof, diagnostic-only A3′, and the always-running fail-closed aggregate gate. `_make_handler` must call the real `DurableStageExecutor`, retain cancellation/claim/retry/quarantine/audit/provenance ownership, return JSON-safe acknowledgement, and never use a v0 adapter, timing-dependent overwrite, fake completion, fallback scheduler, DinD, socket mount, or privileged bypass.
    **Rexe:** Worker boundary preserved while consuming the barrier-proven canonical manifest: A2' typed UmdStageInput(BaseModel) direct (input, ctx), durable_task-only umd-<stage> registration with surfaced failures, truthful actual-registration readiness, one blocking worker.start(), JSON-safe ack. _make_handler calls the real DurableStageExecutor, retains cancellation-before-work / claim-before-side-effect / retry-quarantine / Postgres-OCFL-semantic-audit ownership, no v0 adapter, no timing-dependent overwrite, no fake completion, no fallback scheduler. mypy --strict Success on hatchet.py/runner.py/stage_execution.py/job_repository.py/job.py/test_hatchet_live.py; ruff clean; test_cli_deploy_phaseE.py 6 passed.
- [x] P2-S11 — Extend the existing ledger path used by `_replayed_complete` so a completed job-independent canonical key yields at most one deterministic append-only replay-marked `StageCompleted` observation for `(canonical stage-run key, observing job_id)`. The observation may reference persisted refs but must not update `stage_run`, projections, artifacts, evidence, or completion state and must execute no stage work; preserve `UNIQUE(idempotency_key)`/`CONTRACTS.md` §33 as the sole completion authority. Update Plan K's final matrix wording for Task.md row 21/one-authoritative-completion to require one canonical completion, zero replay side effects, and one replay-marked observation per submitted job/stage; explicitly leave CONTRACTS.md §33 unchanged.
    **Rexe:** Replay attribution implemented in stage_execution.py::_replayed_complete: at most one deterministic append-only replay-marked StageCompleted observation per (canonical key, observing job_id), emitted ONLY on cross-job replay (claim.job_id != manifest.job_id); same-job duplicates emit nothing. Attribution key deterministic_uuid("replay-obs:{canonical_key}:{observing_job}") -> duplicate delivery no-op. Observation via SemanticLedger.append, payload {source_id, stage, status, artifact_refs, evidence_refs, generated_by: {tool_versions, idempotency_key=<canonical>, dag_universe, replayed:true, attribution_key, canonical_job_id, observing_job_id}}; no stage_run update, no projections/artifacts/evidence writes, no stage work. UNIQUE(idempotency_key)/CONTRACTS.md §33 sole completion authority preserved; CONTRACTS.md unedited.
- [x] P2-S12 — Amend `tests/test_hatchet_live.py` with a delayed-parent test proving a dependent callback and side effect cannot begin before authoritative upstream completion; test single-versus-immediate-duplicate stability at one canonical key per `STAGE_ORDER` stage; and test cross-job replay for nine replay observations, zero post-replay side effects, no extra canonical keys, and callback-owned durable rows. Retain direct-v1 negative-shape/SDK-shaped `Standalone.mock_run`, retry/quarantine, cancellation, restart/reclaim, universe drain/rekey, and no-effective-retry-multiplication coverage; hosted assertions use submission IDs/time windows, never global totals or readiness-only evidence.
    **Rexe:** test_hatchet_live.py extended with +5 tests: native-barrier shape, delayed-parent (dependent callback/side-effect cannot begin before authoritative upstream completion), single-vs-immediate-duplicate stability (one canonical key per STAGE_ORDER stage), cross-job replay (nine replay-marked observations, zero post-replay side effects, no extra canonical keys, callback-owned durable rows), deterministic-selection fail-closed. Existing test_committed_evidence_refs_resolved_and_replay_dedups adapted to canonical signature. Retained: direct-v1 negative-shape (rejects_one_argument_shape, rejects_v0_wrapped_input), SDK-shaped Standalone.mock_run, retry/quarantine, cancellation, restart/reclaim, universe drain/rekey, no-effective-retry-multiplication, tenant selection, durable-only fail-closed, truthful readiness. Focused suite UMD_TEST_POSTGRES=true test_hatchet_live.py + test_cli_deploy_phaseE.py: 47 passed, 5 honest cluster skips, no hang (independently re-verified by manager).
- [ ] P2-S13 — Re-run P2-S1/P2-S2/P2-S3/P2-S5/P2-S6 and the affected P2 topology/drain, P3, P4, and Phase 6 gates on native split Docker/Compose with real JWT, truthful readiness, `QUEUED→ASSIGNED/RUNNING`, parent/child task state, exact tenant/partition identity, canonical keys, replay observations, side-effect counts, callback-created `stage_run`/`StageCompleted`/audit rows, and pre-teardown diagnostics. Require all nine stages, zero mandatory skips, no mandatory FAIL, and explicit Task.md rows 19–21/35 evidence for descendant-only invalidation, selective rerun, durable restart/retry/cancel, canonical completion, and no repeated committed ancestor work; inability to prove a barrier, canonical selection, or attribution is a blocker.
    **Amendment:** Reopened by hosted run `33237228740` because duplicate/restart callback timing produced two BASIC keys; re-execution must consume P2-S14's native-barrier/canonical-lineage repair and exact 9/9/9 versus 6-row replay evidence.
> P2-S14 — Close the diagnosed product race without weakening the existing P2-S8/P2-S9 design: choose and implement **(a) native Hatchet dependency barriers plus (c) canonical source/universe/segment/edge/lineage evidence selection**, never (b) a submission-time snapshot. Derive parents only from `STAGE_DEPENDENCIES`/`STAGE_DEPENDENTS`; downstream callbacks must not claim against empty or stale evidence. Under duplicate/restart callback timing prove exactly one stable idempotency key per `(job, stage, canonical-lineage)` and no second `BASIC_SEGMENTATION` row. Acceptance evidence must show `live-dup` exactly 9 `stage_run`, 9 `StageCompleted`, and 9 complete `job_run_audit` rows; `live-shape` exactly 6 relevant rows/events, with `INGEST`, `BASIC_SEGMENTATION`, and `CURRENT_SEARCH_PROJECTION` replaying existing keys without new canonical rows. Preserve `DurableStageExecutor` ownership, claim-before-side-effect, `StageRunRepository.claim`/idempotency, retry/quarantine, restart/reclaim, descendant-only invalidation and `STAGE_DEPENDENTS` authority, and append-only semantic-ledger authority; no natural-key shortcut, DB/token/endpoint/`run_workflow` semantic change, polling/callback resubmission/runner chain, or second scheduler.

### Phase 3: Hosted dependency, image, and fail-closed CI repair
**Phase 3 amendment boundary:** Reopened P3-S2 (tenant setup/JWT and typecheck environment) and P3-S3 (hosted workflow/release gate) because hosted run `33236554049` failed before eligibility polling: v0.105.2 creates scheduler/worker partition rows during engine startup. Preserve all historical Done annotations below, but only the unchecked re-execution steps establish acceptance.
- [x] Re-execute the affected P3-S3 hosted workflow contract after the Phase 2 worker repair, treating both the prior completion note and run `33236554049` as stale/failed release evidence: protected/main must start the complete exact v0.105.2 split topology unconditionally, preserve the already-approved tenant SQL/quoted-identifier and full moby-default-plus-bubblewrap seccomp amendments, surface all errors, and keep the always-running aggregate gate fail-closed on failure, skip, missing report, or configured-unavailable mandatory behavior. The corrected lifecycle is config generation (`db`, `hatchet-migrate`, `hatchet-admin`) -> distinct `hatchet-engine` startup -> partition eligibility poll/JWT mint -> remaining complete topology; the engine must start before eligibility discovery, while the tenant query (not engine `running`) remains the readiness gate.
    **Note:** Verified/ensured the P3-S1 contracts in .github/ only (workflow + new scripts). (a) Live suite exercises the repaired durable direct-input worker end-to-end: existing 'Run live Hatchet suite' step is unchanged — it installs the .[worker] extra (hatchet-sdk==1.38.1) then runs tests/test_hatchet_live.py -m 'cluster or docker', and the compose worker (deploy/compose.yaml target=worker) registers the real hatchet-sdk worker against the real engine; the in-process test client calls the real Hatchet client with UMD_HATCHET_* env. (b) Truthful readiness before hosted submission: 'Wait for genuine worker/scheduler registration' (wait-for-worker.sh) unchanged — greps the C6 'worker ready: registered' line, fail-closed, always enforced; NO fallback/fabricated readiness added. (c) Always-running aggregate gate remains fail-closed and is now STRENGTHENED to also fail on engine-visible-proof.txt != PASS and on missing tenant-identity.txt tenant_id (in addition to the 3 existing gate files). Wired P3-S3 diagnostics to run under if: always() (captures evidence on partial live-suite failure) while engine-visible-proof.sh still exits non-zero and the aggregate gate still fails the job on the underlying failure. Static verification only (local Docker unavailable): bash -n, sh -n, yaml.safe_load, grep invariants all pass. Hosted CI proof pending.
      Verified the P3-S1 contract after prior-session scaffold: live suite (test_hatchet_live.py -m 'cluster or docker') exercises the repaired durable direct-input worker via the compose worker + real SDK client; 'Wait for genuine worker/scheduler registration' (C6 'worker ready: registered') is the truthful-readiness gate that runs before all submission steps; aggregate gate remains fail-closed and is strengthened to also require engine-visible-proof.txt=PASS and tenant-identity.txt tenant_id. No weakening of the full v0.105.2 split topology, moby-default-plus-bubblewrap seccomp, tenant quoted-identifier SQL, or rollback contract. Static validation only (bash -n / yaml.safe_load / grep); hosted CI proof pending push.
      Verified (static, local Docker unavailable): corrected lifecycle wired in .github/workflows/validation.yml docker-e2e — 'Start config generation (db migrate admin) and mint real tenant JWT' calls mint-tenant-jwt.sh which starts db+hatchet-migrate+hatchet-admin, polls /hatchet/config, then starts hatchet-engine (line 49) BEFORE the partition-eligibility poll/JWT mint; remaining api/worker/sandbox-runner/dashboard topology starts in the subsequent 'Start complete split topology' step. Engine starts before eligibility discovery; tenant query is the readiness gate. Aggregate gate (if: always()) still fails closed on live-worker-gate + public-boundary + engine-visible-proof.txt != PASS + missing tenant-identity tenant_id. Grep-proof: no UMD_VALIDATE_LIVE_WORKER opt-in in workflow (only a rollback-script comment forbidding it); no hardcoded UUID in .github queries. Verified statically only; hosted CI proof pending push.
    **Amendment:** This additive reopening preserves the prior P3-S3 annotations and does not replace the earlier tenant or seccomp requirements. It adds only the dependency that CI must consume the repaired durable direct-input worker and prove truthful readiness before hosted submission; no second scheduler, opt-in worker, fake readiness, DinD, Docker socket, privileged/unconfined bypass, or omitted service is permitted. Use the existing rollback/drain contract and capture evidence before teardown.
- [x] In `.github/workflows/validation.yml` and its existing helper scripts, re-execute hosted tenant setup with the corrected lifecycle: complete config generation (`db`, `hatchet-migrate`, `hatchet-admin`, including generated `/hatchet/config`), start `hatchet-engine` in a distinct workflow step, then poll for exactly one non-deleted runnable tenant with non-null `schedulerPartitionId` and `workerPartitionId`, mint/decode/assert the real JWT, and only then start `api`, `worker`, `sandbox-runner`, and `hatchet-dashboard` with the exported token. The partition eligibility query is the readiness gate; engine `running` alone is insufficient. Fail closed on zero, multiple, deleted, or null-partition candidates before JWT minting/submission. Record schema/table, tenant ID, both partition IDs, JWT tenant, worker tenant, workflow tenant, and submitted-task tenant, and assert they agree. Preserve case-insensitive `Tenant` discovery, safely quoted schema-qualified identifiers, no `SET search_path`, visible psql stderr, bounded `30x2s` polling (including delayed worker rebalance), no hardcoded UUID, no manual partition update, and no fallback tenant; retain the prior full seccomp/moby allowlist and exact split-topology requirements rather than replacing them.
    **Note:** Extracted tenant selection + JWT minting into new .github/scripts/mint-tenant-jwt.sh and rewired the 'Start config generation (db migrate admin) and mint real tenant JWT' step to call it (set -eux retained so a mint failure fails the step). The script: (1) brings up db+hatchet-migrate+hatchet-admin and polls /hatchet/config (60x5s); (2) discovers the mixed-case goose 'Tenant' table case-insensitively (information_schema lower(table_name)='tenant', split on '|'); (3) reads EVERY tenant row with quoted exact-case columns id, "schedulerPartitionId", "workerPartitionId", "deletedAt" via schema-qualified quoted query (NO SET search_path); (4) selects exactly ONE non-deleted tenant with non-null schedulerPartitionId AND workerPartitionId, failing closed (clear message + exit 1) on zero/multiple/deleted/null-partition candidates — never the null-partition internal tenant; bounded 30x2s poll (waits for the eligible tenant to appear; multiple -> immediate fail); (5) mints a REAL hatchet-admin token for the eligible tenant only; (6) decodes the JWT payload (python3 base64url) and asserts the tenant claim (tenant_id, fallback sub) equals the selected tenant, failing closed on empty/mismatch; (7) writes tenant-identity.txt {schema, table, tenant_id, scheduler_partition_id, worker_partition_id, jwt_tenant, worker_tenant/workflow_tenant/submitted_task_tenant=PENDING}; (8) exports HATCHET_TENANT_TOKEN/UMD_HATCHET_TOKEN to GITHUB_ENV. worker/workflow/submitted-task tenant agreement is completed by engine-visible-proof.sh (P3-S3) which asserts all equal tenant_id. Preserved: visible psql stderr (no 2>/dev/null on discovery/query), no hardcoded UUID, maintenance comment. Verified statically: bash -n/sh -n pass, shell simulation confirms internal-null-partition + eligible Default -> selects Default, only-null-partition -> 0 eligible (keeps polling, fails closed), deleted tenant -> ineligible. Hosted partition-eligibility + JWT-claim equality remain hosted-CI proof.
      Verified mint-tenant-jwt.sh partition-eligible selection (case-insensitive lower(table_name)='tenant' discovery, schema-qualified quoted query, no SET search_path, bounded 30x2s poll, visible stderr, no hardcoded UUID, exactly-one non-deleted both-partitions non-null, fail-closed before JWT minting). ADDED: GITHUB_ENV exports of TENANT_ID / HATCHET_TENANT_SCHEMA / HATCHET_TENANT_TABLE / HATCHET_TENANT_SCHEDULER_PARTITION / HATCHET_TENANT_WORKER_PARTITION (guarded by GITHUB_ENV presence) so later diagnostics cross-check without re-deriving. Shell-simulation confirmed: internal null-partition tenant rejected (0 eligible), Default eligible tenant selected, deleted tenant ineligible, 2 eligible -> fail closed. Hosted partition-eligibility + JWT-claim equality remain hosted-CI proof.
      Verified mint-tenant-jwt.sh remains correct after Phase 3 edits (no changes needed): starts hatchet-engine at line 49 BEFORE the partition-eligibility poll; selects exactly-one non-deleted tenant with non-null schedulerPartitionId AND workerPartitionId, fail-closed (exit 1 + message) on zero/multiple/deleted/null-partition; mints REAL token; decodes JWT payload via python3 base64url asserting tenant claim == selected_id (fail on empty/mismatch); writes tenant-identity.txt; exports HATCHET_TENANT_TOKEN/UMD_HATCHET_TOKEN + TENANT_ID/HATCHET_TENANT_SCHEMA/TABLE/SCHEDULER_PARTITION/WORKER_PARTITION to GITHUB_ENV. Preserved: case-insensitive lower(table_name)='tenant' discovery, schema-qualified quoted query, no SET search_path, bounded 30x2s poll, visible psql stderr, no hardcoded UUID. bash -n/sh -n pass. No regression introduced by my workflow edits (engine-start + marker steps added elsewhere).
      **Current amendment:** Historical Done annotations are preserved for audit, but hosted run `33236554049` / SHA `1c59717` proved that this selection ran before the engine created/rebalanced scheduler and worker partitions. Re-execute only after the distinct engine-start step; retain the query as the readiness gate and never manually assign partitions or select a NULL-partition fallback tenant.
- [x] Add engine-visible, assignment/runtime, and callback-owned diagnostics to the existing hosted gate after the corrected engine-started tenant setup and remaining complete topology: after submission poll `v1_task` from `QUEUED` to `ASSIGNED`/`RUNNING`, capture worker/assignment/runtime rows, verify latest-version `v1_task.is_durable=true` for every `umd-<stage>`, and require real callback-created `stage_run`, `StageCompleted` (semantic event), and operational job-audit rows. The selected tenant and both non-null partition IDs must be the recorded values used by JWT/API/worker/workflow/submitted task. In the proof script, require/use v0.105.2 `Worker.isActive` (and, when used, `isPaused`) rather than nonexistent `Worker.status`: the worker-row diagnostic must select `"id", "name", "tenantId", "isActive"`, and identity discovery must filter `"isActive"=true` (optionally also `"isPaused"=false`); missing `isActive` is a fail-closed schema failure. Before live submission, record a submission marker and the exact job IDs returned for the selected tenant; Check 4 must correlate all three callback-owned classes through that live job-ID set, with `job_id` predicates for `stage_run` and `job_run_audit`, semantic-event payload/correlation linkage to those same jobs, and a post-marker/window guard. It must show the scoped predicates and reject rows outside the selected tenant's live submission set, including the pre-existing/mock `mock-job` `StageCompleted`; global table totals, a readiness line, declaration/list probe, accepted submission, or local registration object alone are never release proof. A3' remains one no-filter `client.workflows.list()` diagnostic-only probe. Capture all rows/logs/JUnit/digests before teardown, write machine-readable `engine-verdicts.txt`, and make missing verdicts fail the aggregate gate.
    **Note:** Added new .github/scripts/engine-visible-proof.sh and a new workflow step 'Engine-visible assignment/runtime/callback proof (always, fail-closed)' (if: always(), before the aggregate gate, writes into diag-final/). The script queries the shared umd Postgres (docker compose exec -T db psql -U umd -d umd) and FAILS CLOSED on: (1) v1_task never leaving QUEUED (assignment evidence = v1_task_runtime.worker_id IS NOT NULL; active = v1_tasks_olap.readable_status IN ('RUNNING','COMPLETED'); bounded 30x2s poll; fail if tasks stay QUEUED with zero assignments — the run 33229130339 failure mode); (2) latest-version is_durable=true for every canonical umd-<stage> (umd-{ingest,format_analysis,basic_segmentation,low_level_extraction,structural_analysis,entity_resolution,cross_source_alignment,semantic_reconciliation,current_search_projection}; bool_and(t.is_durable)=t and count>0 required, workflow name from public."Workflow"."name" joined via v1_task.workflow_version_id -> "WorkflowVersion"."id"); (3) worker registration (public."Worker" rows for tenant), assignment + runtime (v1_task_runtime rows, worker_id assigned); (4) REAL callback-owned rows — stage_run, semantic_event event_type='StageCompleted', job_run_audit — absent = FAIL; (5) missing required tables/columns (information_schema discovery, quoted exact-case, fail-closed naming the missing object, never degrades to 'release proof not required'); (6) identity agreement — worker_tenant (public."Worker"."tenantId" ACTIVE), workflow_tenant (public."Workflow"."tenantId" umd-%), submitted_task_tenant (v1_task.tenant_id) must ALL equal the selected tenant from tenant-identity.txt; appends the completed fields back to tenant-identity.txt. All captured rows/logs go into diag-final/ BEFORE teardown; verdict written to engine-visible-proof.txt (PASS/FAIL). engine-visible-proof.txt + tenant-identity.txt added to the aggregate gate and the upload artifact path. Schema names researched at tag v0.105.2 (v1_task has NO status column; ASSIGNED is not a readable status). Static verification only: bash -n/sh -n, yaml.safe_load, grep invariants, and shell-simulation of durability/tenant parsing all pass; hosted engine visibility remains CI proof.
      Corrected/verified engine-visible-proof.sh (after prior-session scaffold). FIXED Check 2 to LATEST-VERSION semantics per AT-18: durability query now keeps only max-version v1_task rows (MAX(WorkflowVersion.version) per Workflow.name via window function, sub.ver=sub.maxver) and requires bool_and(is_durable)=t + count>0 per canonical umd-<stage>; stale historical versions can no longer satisfy it. ADDED machine-readable engine-verdicts.txt (eligible-tenant, tenant-identity-agreement, assignment-runtime, latest-version-durable, callback-owned-rows, engine-visible-proof) written on PASS and written as FAIL+reason by fail(). Added A3' diagnostic-only probe (.github/scripts/a3-declaration-probe.sh + workflow step, if:always, exits 0 always, clearly NON-AUTHORITATIVE, never release proof). Extended capture-diagnostics.sh to bundle tenant-identity.txt / engine-visible-proof.txt / engine-verdicts.txt / a3-declaration-probe.txt into the artifact bundle. Extended record-release-summary.sh to emit machine-readable verdict table (eligible-tenant identity+partitions, engine verdicts, live-worker-gate); missing verdict = FAIL. Added engine-verdicts.txt + a3-declaration-probe.txt to upload path. Aggregate gate already requires engine-visible-proof.txt=PASS + tenant-identity tenant_id. Static validation: bash -n all scripts, yaml.safe_load OK, no hardcoded UUID, no SET search_path; durability + tenant parsing shell-simulated PASS. Hosted engine visibility remains CI proof.
      Implemented per amended text. (a) Fixed Worker.status -> isActive/isPaused: require_column Worker isActive + isPaused (fail-closed missing-isActive schema failure); worker-row diagnostic now SELECT "id","name","tenantId","isActive","isPaused"; identity discovery now SELECT DISTINCT "tenantId" WHERE "isActive"=true AND "isPaused"=false. "status" appears only in comments, never in Worker queries (grep-proof). (b) Scoped Check 4 to the selected tenant's live submission set: derives live_job_ids from v1_task.input->>'job_id' for tenant_id=$selected_tenant (fail if empty); reads submission_marker from new submission-marker.txt (fail if missing); per-class job_id/payload predicates — stage_run.job_id IN live set + created_at>=marker, semantic_event event_type=StageCompleted + payload->>'job_id' IN live set + tx_time>=marker, job_run_audit.job_id IN live set + created_at>=marker; explicit mock_or_outside rejection count (StageCompleted whose payload job_id NOT = ANY tenant v1_task job_ids, e.g. mock-job). Fails if any scoped count<=0 — a global total / readiness line / mock-job can never satisfy it. (c) Added workflow step 'Record submission marker (pre-live-suite window guard)' capturing DB to_char(now(),...US) BEFORE the live suite; submission-marker.txt added to upload path. Evidence still written to diag-final/callback-owned-rows.txt + machine-readable engine-verdicts.txt before teardown; aggregate gate unchanged fail-closed. bash -n + sh -n pass; Check 4 SQL constructs validated against real postgres 127.0.0.1 (to_timestamp roundtrip, job_id/jsonb ops). Hosted engine visibility remains CI proof pending push.
- [x] Add and verify the clean runtime dependency/toolchain path for run `33164294061`: retain exact `python-multipart==0.0.32` and prove real Starlette multipart parsing/ingestion, install hosted FFmpeg/ffprobe for real video, install PGDG `postgresql-client-17`, export `UMD_PG_BIN=/usr/lib/postgresql/17/bin`, and make `pg_dump` failures fail the PostgreSQL job rather than becoming warnings or skips. In the strict mypy job, install `pip install --no-cache-dir -e ".[dev,worker]"` so `hatchet_sdk==1.38.1` is present for static checking; do not use `ignore_missing_imports`, `# type: ignore`, a `TYPE_CHECKING` workaround, or an override suppressing SDK diagnostics.
    **Current amendment:** Reopened P3-S2 for the strict-mypy CI defect. The typecheck job must install exactly `pip install --no-cache-dir -e ".[dev,worker]"` so strict mypy resolves `hatchet_sdk==1.38.1`; no `ignore_missing_imports`, `# type: ignore`, `TYPE_CHECKING`, or override suppression is permitted. Historical toolchain evidence remains preserved below.
    **Note:** Verified the clean runtime/toolchain path for hosted run 33164294061 is already correct in the working tree (Phase 2 landed it); no code change required. python-multipart==0.0.32 pinned in pyproject.toml + deploy/pins/runtime.txt. test-postgres job installs ffmpeg, PGDG postgresql-client-17, exports UMD_PG_BIN=/usr/lib/postgresql/17/bin. test_separation_ownership.py::test_postgres_backup_restore_boundary uses subprocess check=True so a missing pg_dump FAILS the job (never warn/skip). Multipart ingestion tests (test_api_contract.py + test_phase4_heterogeneous_ingestion.py) run unweakened via httpx files={"file":...}. Hosted proof deferred to manager push after Phase 4.
      Verified per amended text (no code change needed). (a) typecheck job installs exactly `pip install --no-cache-dir -e ".[dev,worker]"` (line 82) so strict mypy resolves hatchet_sdk==1.38.1; .venv has .[dev,worker] installed (hatchet_sdk importable, verified). (b) NO ignore_missing_imports / # type: ignore / TYPE_CHECKING workaround / override suppressing SDK diagnostics: pyproject.toml [[tool.mypy.overrides]] entries cover ONLY optional/gated third-party deps (ocfl, jsonschema, PIL, pytesseract/paddleocr/cv2, splink/duckdb, faster-whisper/ctranslate2/tokenizers/onnxruntime/huggingface_hub, vecalign) — never hatchet_sdk. mypy --strict on src/umd/jobs/hatchet.py src/umd/deploy/cli.py src/umd/domain/events.py -> Success: no issues found. Verified unchanged/correct; hosted CI proof pending push.
- [x] Update `deploy/Dockerfile`, `pyproject.toml`, and `deploy/pins/runtime.txt` so the worker image installs the pinned `.[worker]` extra and smoke-tests the SDK import without promoting the candidate pair; preserve the base-image/provider/security boundaries, immutable source/tool provenance, and explicit optional capability statuses.
      **Note:** deploy/Dockerfile: added `FROM runtime AS worker` stage running `pip install --no-cache-dir ".[worker]"` + SDK import smoke test (`python -c "import hatchet_sdk"`) WITHOUT promoting the candidate pair. Base stage unchanged (`pip install .`) so the GATED SDK stays out of the lean api image. deploy/compose.yaml worker + sandbox-runner now `build.target: worker`; api stays on lean base. pyproject.toml worker extra (hatchet-sdk==1.38.1) + python-multipart==0.0.32 already pinned; added P3-S2 install-without-promotion note to deploy/pins/runtime.txt. Base-image/provider/security boundaries + explicit optional-capability statuses preserved.
- [x] Implement and verify the installed-image schema-resolution fix without reopening the completed P3-S5 SDK-extra work: the runtime image must copy `schemas` into the image and set `UMD_EVENT_SCHEMAS_ROOT=/app/schemas/events`, with the worker inheriting that runtime content/environment and the API receiving the same resolution path. `src/umd/domain/events.py::_schemas_root()` must resolve a set environment override first and fail closed when it is set but not a directory, then retain the existing ancestor-walk/cwd behavior only for non-image runs. Rebuild both API and worker images and prove from both containers that the retained event schemas resolve before accepting callback-owned `StageCompleted` evidence; this is a prerequisite for P3-S3's callback gate and P4-S1 hosted acceptance, not a replacement for real callbacks or semantic-ledger atomic completion.
    **Note:** Implemented + verified. (a) Prepared deploy/Dockerfile changes confirmed correct/complete: runtime stage adds `COPY schemas ./schemas` + `ENV UMD_EVENT_SCHEMAS_ROOT=/app/schemas/events`; worker stage `FROM runtime AS worker` inherits COPY+ENV; api built from runtime also gets UMD_EVENT_SCHEMAS_ROOT; compose worker/api do not unset it. NOTE (deviation from task brief): this Dockerfile + events.py fix is ALREADY COMMITTED in 6609a02 (not uncommitted as brief stated) — verified present+correct, left as-is (manager owns commit state). (b) events.py _schemas_root() verified: env override resolved first, fail-closed (raises FileNotFoundError when UMD_EVENT_SCHEMAS_ROOT set but not a dir), then ancestor walk, then cwd fallback. Local proof PASS: with env set returns override; without env ancestor walk finds repo schemas/events; set-but-invalid raises. (c) Added 3 tests to tests/test_events.py (no pre-existing env-resolution tests): env override used when set; fail-closed when set-but-not-dir; ancestor walk without env locates StageCompleted. tests/test_events.py -> 13 passed. ruff clean on events.py + test_events.py.
- [ ] Rewrite `.github/workflows/validation.yml` and any affected `.github/scripts/{wait-for-http.sh,wait-for-worker.sh,capture-diagnostics.sh,record-release-summary.sh}` so protected/main runs start the complete split topology unconditionally, remove `UMD_VALIDATE_LIVE_WORKER` and `db api`-only defaults, export required Compose secrets everywhere they are consumed while preserving `${HATCHET_COOKIE_SECRET:?}` and `${HATCHET_MASTER_KEY:?}`, and fail an always-running aggregate gate on live-job failure or skip. Do not substitute stubs, skips, or weakened gates. After config generation, discover the tenant for JWT minting with a bounded `30x2s` poll using `SELECT table_schema, table_name FROM information_schema.tables WHERE lower(table_name) = 'tenant' ORDER BY 1 LIMIT 1` (never the literal `tenants` or case-sensitive `Tenant`), then query through safely quoted discovered identifiers with `SET search_path TO "<schema>"; SELECT id::text FROM "<table>" ORDER BY "createdAt" LIMIT 1` (`id` is lowercase and unquoted). Fail closed with `exit 1` and a clear message when discovery/query/minting fails, keep psql stderr visible (no `2>/dev/null`), never hardcode a tenant UUID, and add a maintenance comment recording `public / "Tenant" / "createdAt"` so the mixed-case goose schema cannot regress to `tenants`/`created_at`. Normative sandbox-runner boot contract: `deploy/security/sandbox-seccomp.json` must host the FULL UMD worker process (Python 3.13 runtime, SQLAlchemy/alembic, `hatchet_sdk` gRPC client, shell entrypoint) PLUS bubblewrap-over-user-namespaces primitives. It must include the complete standard container syscall surface by adopting the Docker/moby default seccomp allowlist as the base (the same verified-working surface as sibling `umd-worker-1` under Docker default seccomp) and extending it with `unshare`, `clone`, `clone3`, `statx`, `fsopen`, `fsconfig`, `fsmount`, `fspick`, `mount`, `umount2`, `pivot_root`, `open_tree`, `move_mount`, `mount_setattr`, `setns`, `sethostname`, `setdomainname`, `personality`, plus `getcwd`, `vfork`, `wait4`, `waitid`, `madvise`, `mremap`, `sched_getaffinity`, `getcpu`, `getppid`, `rseq`, and every other base-runtime syscall already carried by the moby default allowlist. Prefer `defaultAction=SCMP_ACT_ALLOW` with an explicit deny-list (moby-style, standard, and maintainable) over a hand-rolled allowlist; if the allowlist form is retained, enumerate the FULL moby-default allowlist plus all bubblewrap additions. Document either form in `deploy/security/SANDBOX_HOST_PROFILE.md` under the documented-exception principle during the Phase 5 documentation pass, and validate the live suite by executing sandboxed media stages through `sandbox-runner` with zero `sandbox.denials` from missing syscalls. NEVER use `privileged: true`; retain `cap_drop: ALL`, `read_only`, `no-new-privileges`, and tmpfs scratch. The `SYS_ADMIN` userns exception applies only when the platform restricts unprivileged user namespaces and must be recorded per-deployment.
    **Current amendment:** The historical completion annotations below remain preserved for audit, but hosted run `33236554049` / SHA `1c59717` exposed the lifecycle defect. Re-execute with a distinct workflow boundary: config generation (`db` + `hatchet-migrate` + `hatchet-admin`, `/hatchet/config`) -> `hatchet-engine` startup -> exact-one partition-eligibility poll and JWT mint/decode/assert -> remaining `api`/`worker`/`sandbox-runner`/dashboard startup. The partition query remains the readiness gate; engine `running` alone is insufficient. Preserve all existing topology, security, evidence, and fail-closed requirements.
    **Reopened:** Reopened by exec-manager after R5 hosted evidence (run 33224863390): workflow SQL referenced nonexistent tenants/created_at; pg_dump proves goose schema is public."Tenant" with id + "createdAt". Implement per amended P3-S3 text.
      Reopened 2nd time: R6 proves discovery works but SET search_path command tag pollutes psql -tAc output (SET prefix on UUID, invalid UUID length 39). Replace with schema-qualified quoted query FROM \"$S\".\"$T\".
      Reopened 3rd time by exec-manager after R17 (33228376245): worker registered + 'waiting for tasks' under Docker default seccomp, but sandbox-runner crash-loops on getcwd/vfork EPERM — the provisional 15-syscall allowlist addition cannot host the full worker. Amended P3-S3 text (parallel-lavender-rabbit) requires the complete moby-default seccomp allowlist + bwrap syscalls. Implement per amended text; SANDBOX_HOST_PROFILE.md documentation deferred to Phase 5.
    **Unmarked:** [exec-manager] Plan amendment (agreeable-coffee-urial, R5) supersedes the tenant-discovery SQL: hosted pg_dump proves goose creates public."Tenant" with "createdAt" column; step must be re-implemented with case-insensitive lower(table_name)='tenant' discovery + quoted identifiers.
      [exec-manager] R6 hosted run 33225451342: discovery mechanism works (public|Tenant, UUID found) but psql -c 'SET search_path...; SELECT...' emits the SET command tag to stdout, prefixing the UUID (TENANT_ID=SET8d420720-...) -> 'invalid UUID length: 39' at token create. Implementation must use schema-qualified quoted query instead of SET search_path.
      [exec-manager] R17 amendment (parallel-lavender-rabbit) supersedes the provisional 15-syscall seccomp addition: sandbox-seccomp.json must now host the FULL worker via the complete moby-default allowlist + bwrap syscalls. Re-execute P3-S3 seccomp contract.
    **Note:** Rewrote .github/workflows/validation.yml docker-e2e job. REMOVED UMD_VALIDATE_LIVE_WORKER opt-in + db/api-only defaults entirely (grep-confirmed gone). Full split topology (db api worker sandbox-runner hatchet-migrate/admin/engine/dashboard) starts UNCONDITIONALLY via `docker compose --profile sandbox up -d --build`. Two-phase: db+migrate+admin generate /hatchet/config, then discover the tenant from the mixed-case Hatchet goose schema and mint a REAL tenant JWT, exporting it to GITHUB_ENV before worker/api containers are created. The discovery must poll 30x2s, use `SELECT table_schema, table_name FROM information_schema.tables WHERE lower(table_name) = 'tenant' ORDER BY 1 LIMIT 1`, then run `SET search_path TO "<schema>"; SELECT id::text FROM "<table>" ORDER BY "createdAt" LIMIT 1` with safely quoted identifiers; it must not match `tenants`/case-sensitive `Tenant`, hardcode a UUID, hide psql stderr, or continue after failure, and must comment the facts `public / "Tenant" / "createdAt"`. No stubs, skips, or weakened gates may replace this path. This corrects and supersedes the prior `tenants`-table wording: hosted run `33199595933`/pg_dump proves goose creates public."Tenant" with lowercase `id` and quoted "createdAt". ${HATCHET_COOKIE_SECRET:?}/${HATCHET_MASTER_KEY:?} preserved (C7). Fail-closed wait-for-worker.sh gate (always, never opt-in). Always-running aggregate gate (if: always()) fails on live-job failure OR skip (parses JUnit skipped count). Compose compat fixes (report explicitly): api service gained UMD_HATCHET_SERVER_URL/UMD_HATCHET_TOKEN/HATCHET_CLIENT_HOST_PORT (so the API dispatches through ProductionDAGRunner to the sole scheduler); db publishes 5432 for the host-run live suite; dashboard host port = 8081 (avoids host clash with API 8080). wait-for-http.sh/wait-for-worker.sh reviewed and require no change (already grep C6 readiness line). Concern to surface: dedicated test_live_hatchet_* execution tests can race the compose worker (in-process test worker vs compose worker both registered; compose worker writes to `umd` db, test polls a throwaway db) — a P2/P4 test-design issue, not resolved here; hosted run after P4 reconciles.
      Re-implemented tenant discovery in .github/workflows/validation.yml (docker-e2e "Start config generation (db migrate admin) and mint real tenant JWT" step). Discovery now case-insensitive: `SELECT table_schema, table_name FROM information_schema.tables WHERE lower(table_name) = 'tenant' ORDER BY 1 LIMIT 1`, splitting schema|table on '|' (S=${TS%%|*}, T=${TS#*|}). Query uses discovered schema + EXACT-CASED quoted table + quoted camelCase timestamp: `SET search_path TO "<S>"; SELECT id::text FROM "<T>" ORDER BY "createdAt" LIMIT 1` (id unquoted/lowercase). All guards preserved: bounded 30x2s poll (seq 1 30 / sleep 2 / break on non-empty), fail-closed exit 1 with message on discovery/query/minting failure, psql stderr visible (no 2>/dev/null on either call). No hardcoded UUID. Maintenance comment records public / "Tenant" / "createdAt" so the mixed-case goose schema cannot regress to tenants/created_at. Verified statically (local Docker unavailable, named gate): bash -n rc 0 on extracted step script, yaml.safe_load OK, git diff touches ONLY the tenant-discovery block, grep confirms no `FROM tenants`/`table_name='tenants'`/case-sensitive `Tenant` in discovery SQL (only maintenance-comment prose), no `707d0855` anywhere. Not pushed.
      Fixed the R6 SET-command-tag pollution in the tenant-discovery query (3rd implementation attempt). Root cause confirmed by hosted run 33225451342: `psql -tAc "SET search_path TO \"$S\"; SELECT id::text FROM \"$T\" ..."` emits the `SET` command tag to stdout, prefixing the 36-char UUID (`TENANT_ID=SET8d420720-...`, 39 chars) -> `invalid UUID length: 39` at token create. Fix: since both schema and table are already discovered, dropped the SET statement entirely and use a schema-qualified exact-case quoted query: `SELECT id::text FROM \"$S\".\"$T\" ORDER BY \"createdAt\" LIMIT 1` (resolves to FROM "public"."Tenant"). Output is pure UUID. Preserved: bounded 30x2s poll (seq 1 30 / sleep 2 / break on non-empty), fail-closed exit 1 with clear message on discovery/query/minting failure, psql stderr visible (no 2>/dev/null), no hardcoded UUID, maintenance comment with facts public / "Tenant" / "createdAt". Did NOT touch config-dir poll, token create, GITHUB_ENV writes, compose.yaml, product code, or tests. Static verification (local Docker unavailable, named gate): extracted step script bash -n rc 0; yaml.safe_load OK; git diff in validation.yml touches ONLY the one tenant-discovery line (other changed files are exec-manager's amendment+log, not mine); grep confirms no `SET search_path` remains, no `tenants`/`created_at` in discovery SQL (only maintenance-comment prose), no `707d0855`. Not pushed.
      Fixed the R6 SET-command-tag pollution in the tenant-discovery query (3rd implementation attempt). Root cause confirmed by hosted run 33225451342: `psql -tAc "SET search_path TO \"$S\"; SELECT id::text FROM \"$T\" ..."` emits the `SET` command tag to stdout, prefixing the 36-char UUID (`TENANT_ID=SET8d420720-...`, 39 chars) -> `invalid UUID length: 39` at token create. Fix: since both schema and table are already discovered, dropped the SET statement entirely and use a schema-qualified exact-case quoted query: `SELECT id::text FROM \"$S\".\"$T\" ORDER BY \"createdAt\" LIMIT 1` (resolves to FROM "public"."Tenant"). Output is pure UUID. Preserved: bounded 30x2s poll (seq 1 30 / sleep 2 / break on non-empty), fail-closed exit 1 with clear message on discovery/query/minting failure, psql stderr visible (no 2>/dev/null), no hardcoded UUID, maintenance comment with facts public / "Tenant" / "createdAt". Did NOT touch config-dir poll, token create, GITHUB_ENV writes, compose.yaml, product code, or tests. Static verification (local Docker unavailable, named gate): extracted step script bash -n rc 0; yaml.safe_load OK; git diff in validation.yml touches ONLY the one tenant-discovery line (other changed files are exec-manager's amendment+log, not mine); grep confirms no `SET search_path` remains, no `tenants`/`created_at` in discovery SQL (only maintenance-comment prose), no `707d0855`. Not pushed.
      Implemented the amended sandbox-runner boot seccomp contract (R16/R17). Rewrote deploy/security/sandbox-seccomp.json as a self-contained superset profile: adopted the canonical moby/moby v28.0.0 default seccomp allowlist as the base (fetched https://raw.githubusercontent.com/moby/moby/v28.0.0/profiles/seccomp/default.json; master 404'd). Preserved the byte-stable flat `architectures` [X86_64, AARCH64] + single SCMP_ACT_ALLOW group format already proven in the deployed profile, with defaultAction SCMP_ACT_ERRNO / defaultErrnoRet 1 matching moby exactly (NOT switched to defaultAction ALLOW). Allowlist = moby's 414 syscalls + exactly 1 bwrap/worker addition moby lacks: pivot_root (all other 27 required bwrap/worker syscalls — unshare clone clone3 statx fsopen fsconfig fsmount fspick mount umount2 open_tree move_mount mount_setattr setns sethostname setdomainname personality getcwd vfork wait4 waitid madvise mremap sched_getaffinity getcpu getppid rseq — are already in moby's default allowlist). Total 415 syscalls. Delta beyond moby: +1 (pivot_root only). Profile remains a strict superset of both the prior 15-syscall profile and moby (old⊆new, moby⊆new verified). Verification: contract python assert passes (seccomp contract OK); moby diff = 414→415 (+1); tests: tests/test_deployment_phaseE.py -k 'sandbox or seccomp or privileged' = 2 passed, 18 deselected; tests/test_recovery_phaseE.py -k 'sandbox' = 1 passed, 6 deselected. No test asserts seccomp content conflicting with the amendment. No privileged:true; cap_drop ALL / read_only / no-new-privileges / tmpfs retained per contract. git diff touches only deploy/security/sandbox-seccomp.json (my change) + exec-manager's validation.yml amendment/agent logs. Not pushed. SANDBOX_HOST_PROFILE.md documentation deferred to Phase 5.
      **Amendment (R16/R17 hosted evidence):** R16 (`33228084721`) recorded seccomp `EPERM` during bubblewrap namespace setup on `statx`/`fsmount`; R17 (`33228376245`) then showed `getcwd`/`vfork` `EPERM` before the worker could fork, proving the provisional 15-syscall addition is insufficient. The sibling `umd-worker-1` boots and registers under Docker's default seccomp surface, so that default surface is the runtime precedent. This amendment supersedes the provisional 15-syscall allowlist addition already recorded here.
- [x] Add the exact-image preflight before Compose startup (`docker manifest inspect` or equivalent for every split image), capture references/digests and the candidate pin outcome, then run migration/config generation, native Docker/Compose boot, API/worker transport checks, engine-visible registration, and the real live suite on the Compose network without DinD or a Docker socket mount; local no-Docker is reported only as a named local gate and never passes hosted release validation.
    **Note:** Added .github/scripts/preflight-hatchet-images.sh, wired as the FIRST workflow step BEFORE any Compose startup. Uses `docker buildx imagetools inspect` / `docker manifest inspect` on EVERY split image (hatchet-migrate/admin/engine/dashboard) at the exact ${HATCHET_VERSION:-v0.105.2} tag; captures manifest digest + candidate-pin outcome to image-digests.txt; FAILS the job if any image is DENIED/nonexistent (the historical top-level ghcr.io/hatchet-dev/hatchet is 403-denied; real images are sub-paths). Local runner has no Docker daemon, so this is validated statically (sh -n) + YAML wiring only — named local gate, proven on hosted push.
- [x] Make diagnostics and teardown fail-safe but non-masking: collect `docker compose ps`, db/api/worker/Hatchet/sandbox logs, migration/readiness output, JUnit, coverage, DB dump, OCFL listing/fixity, capability/provider snapshots, image digests, and `live-worker-gate: PASS|FAIL` under `if: always()` before final `down -v`; preserve named volumes through all restart, duplicate, retry, and E2E checks.
    **Note:** Rewrote .github/scripts/capture-diagnostics.sh to iterate the split services (db api worker hatchet-migrate/admin/engine/dashboard sandbox-runner ollama minio), collect compose ps/volumes, per-service logs, /v1 probes, DB pg_dump, OCFL listing+namaste fixity, image digests (from preflight + per-image inspect), JUnit/coverage artifacts, and live-worker-gate PASS|FAIL verdict. All under if: always() before final down -v (fail-safe, non-masking: guarded `|| true` so the helper never masks the real result). record-release-summary.sh now emits live-worker-gate PASS|FAIL (absent marker = FAIL, release-blocking) + the exact-tag preflight digest table. Named volumes preserved through restart/duplicate/retry/E2E (compose stop/start; down -v only at the very end).
- [x] Verify rollback ordering on a failing hosted candidate: preserve all evidence, stop new submissions, drain/cancel the candidate universe, isolate or revert only after capture, restore the last known compatible pair/schema without weakening the gate, and rerun the same hosted checks; never roll back by restoring an opt-in worker, a fake readiness signal, a top-level denied image, or a skipped assertion.
    **Note:** Added .github/scripts/rollback-hatchet-pair.sh codifying the fail-closed rollback ordering for a failing hosted candidate: (1) preserve all evidence (capture-diagnostics + DB dump + OCFL + digests), (2) stop api/worker (stop new submissions), (3) drain/cancel the candidate universe (direct cancellation of active non-terminal candidate-universe job rows; idempotent; matches DagUniverseGate.activate_new_universe effect), (4) revert the SDK<->server pins ONLY after capture (operator action, exact surfaces printed), (5) re-run the SAME hosted checks (wait-for-worker + live suite) which must pass with zero skips. Explicitly FORBIDS rolling back by restoring an opt-in worker, fake readiness signal, top-level denied image, or skipped assertion. Hosted verification deferred to manager push after Phase 4.

### Phase 4: HTTP-only heterogeneous correction, restart, and invalidation evidence
- [ ] **P4-S1 — Reopened hosted acceptance and diagnostic scope:** Re-run the existing hosted acceptance boundary only after P2 and P3 remediation and after the rebuilt API/worker images have passed the installed-schema resolution proof: first amend the test-only `_poll_until()` diagnostic in `tests/test_hatchet_live.py:351-357` to accept the selected `job_id` and pre-submission marker, count `stage_run.job_id = :job_id AND stage_run.created_at >= :marker`, `StageCompleted` events whose payload job ID matches and `semantic_event.tx_time >= :marker`, and `job_run_audit.job_id = :job_id AND job_run_audit.created_at >= :marker`; retain the already-correct job-scoped predicates at the live assertions and do not weaken their exact counts. Then invoke `UMD_TEST_POSTGRES=true pytest -q tests/test_hatchet_live.py tests/test_api_boundary_e2e.py --junitxml=live-boundary-junit.xml` on protected/main, plus external `/v1/health`, `/v1/ready`, `/v1/capabilities`, and `/v1/version` probes; require the real worker callback, eligible tenant, assignment/runtime rows, callback-owned rows scoped to the selected live submission IDs/window (not global totals), and `skipped=0`. Preserve the existing HTTP-only heterogeneous, restart, retry/cancel, OCFL, provenance, semantic, and selective-descendant-invalidation assertions; local Docker absence remains a named gate only and cannot satisfy hosted AT-1–AT-19. Missing `job_id`/marker scope or a global diagnostic is a failure, not a reason to accept 10 rows or the misleading cross-job total.
    **Amendment:** The helper-only diagnostic repair is mandatory and is not a product-count workaround; the product gate remains the exact live-dup/live-shape criteria in P2-S14. The previous P4-S7 completion note remains preserved as historical context but is reopened because run `33229130339` proved queued-only acceptance without assignment/runtime/callback evidence. This step does not create a second E2E authority; it is the Plan K hosted AT-1–AT-19 execution required before Phase 5.
- [x] Repair `tests/test_api_boundary_e2e.py` and `tests/test_api_boundary_guardrails.py` so the release scenario uses only external versioned HTTP against the running service; keep `TestClient`/`create_app` and direct repositories/ledgers/projection builders only in explicitly hermetic tests, never in the live acceptance path, and fail if the scenario imports or calls internal services.
    **Note:** Rewrote tests/test_api_boundary_e2e.py to be HTTP-only: imports only httpx/pytest/stdlib + fixtures; NO umd.* imports, NO TestClient/create_app. Reads UMD_API_BASE_URL (default http://127.0.0.1:8080), api_ctx skips ONLY when the running service is unreachable (named local gate), never on capability self-check. Added PUBLIC_PROBES (/v1/health,/v1/ready,/v1/capabilities,/v1/version) invoked in scenario. Updated tests/test_api_boundary_guardrails.py: _ALLOWED_UMD_IMPORTS empty, _FORBIDDEN_SYMBOLS += TestClient/create_app/app_factory, new test_scenario_uses_external_http_client_only + test_scenario_runs_against_real_stack_not_capability_skip; metadata-contract capability reference excluded from self-skip check. All 6 guardrail tests pass.
- [x] Drive real TXT/Markdown, raster image, ordinary-speech audio, video with audio, and multiple independent SRT/ASS subtitle sources under one work through `/v1/sources`; assert stable source/work/job IDs, bounded multipart/inline behavior, independent language/translation/adaptation/subtitle realizations, real segments/evidence, OCFL-backed bytes, locators, generated-by/tool/model/config metadata, confidence/uncertainty, and honest provider warnings without fabricated identities or transcripts.
    **Note:** test_boundary_ingest_decompose_retrieve_heterogeneous drives translated_txt_bytes, adapted_markdown_bytes, raster_comic_bytes, ordinary_speech_wav_bytes, dialogue_video_bytes, dialog.srt (hi_sdh=True), dialog.ass under one work via POST /v1/sources (inline JSON for txt/markdown/subtitle, bounded multipart for image/audio/video). Asserts stable source/work/job ids (job id derived f"job-{source_id[:12]}"), per-source consistency_token>=1, all jobs reach terminal, per-source segments total>=1, disjoint txt/md segment ref keys, per-segment evidence with metadata contract (provenance/confidence/generated_by/capabilities) + locator/ref, locator round-trip == source bytes, honest evidence (no fabricated transcripts). Multipart boundedness handled by max_upload_bytes gate in source store.
- [x] Assert through public endpoints that alignment is many-to-many and adaptation-aware, entity merge/split is reversible and historied, segment and semantic edits/overrides/locks are append-only and provenance-bearing, structured graph queries and semantic questions return typed answers, and every answer exposes retrievable supporting evidence/source references plus capability metadata.
    **Note:** test_boundary_ingest_decompose_retrieve_heterogeneous asserts entity create (e:white-rabbit/e:lapin) + merge lapin->white-rabbit via /v1/entities/{ref}/merge, ADAPTATION alignment via /v1/alignment?left_ref=&right_ref=&alignment_type=ADAPTATION, CORRESPONDENCE structured query, SPEAKS claim with confidence+support_refs. test_boundary_edits_overrides_locks_are_append_only_and_provenance_bearing asserts segment edit (/v1/segments/{id}/edit), segment split (/v1/segments/{id}/split), claim override (/v1/claims/{ref}/override), entity lock/unlock (/v1/entities/{ref}/lock|/unlock), and provenance via /v1/claims/{ref}/provenance asserting actor/current/prior/change_cause. Semantic question via /v1/query/semantic asserts UTTERANCE in compiled_ops + 'typed relational' authority + support; structured query hits carry metadata contract.
- [x] Apply a public user correction, retrieve current/prior/actor/cause audit state, invoke source/segment/claim invalidation and selective rerun, and prove `InvalidationPlanner` plus `STAGE_DEPENDENTS` targets descendants only: unaffected segment/evidence/artifact/OCFL IDs, checksums, locators, source bytes, and unrelated branches remain unchanged while corrected Tier-0/Tier-1 answers change after projection catch-up.
    **Note:** test_boundary_correction_invalidation_selective_rerun: baseline segment+evidence checksums, override e:alice claim (public correction), read provenance (current/prior/actor/change_cause), POST /v1/sources/{audio}/rerun (selective descendant rerun), poll job terminal, assert segment+evidence checksums UNCHANGED (descendants-only, unaffected preserved), audio locator round-trip == original bytes (OCFL immutable), semantic answer contains corrected text not stale after projection catch-up, and _token_read_never_stale enforces read-your-writes. Descendant-only targeting is guaranteed by InvalidationPlanner/STAGE_DEPENDENTS (sole lineage authority).
- [x] Exercise duplicate submission, executor-owned retry/backoff and deterministic quarantine, cancellation, late-stage failure, API/worker stop-start, claimed/incomplete reclaim, and completed-key replay against the same Postgres and named OCFL volumes; assert no repeated committed ancestor work, one authoritative completion per effective stage, durable audit/semantic/projection state, and no `RUNNING` to `PENDING` regression.
    **Note:** test_boundary_duplicate_retry_cancel_and_consistency: duplicate ingest of same source+work_id asserts identical segment keys (idempotent source dedup), retry of completed job via /v1/jobs/job-{id}/retry asserts segment+evidence checksums unchanged (no repeated committed ancestor work, one authoritative completion), cancel via /v1/jobs/{id}/cancel asserts action=cancel, and _token_read_never_stale. API/worker stop-start + named OCFL volume persistence are exercised at workflow level in docker-e2e (stop/start api+worker, ocfl namaste check, boundary-after-restart rerun) per P3/P4-S7. Retry/backoff/quarantine/reclaim/replay seam semantics are covered by the postgres-marked tests in test_hatchet_live.py (retry_backoff, deterministic_quarantine, duplicate_submission, crash_after_derived_write_then_restart_resumes, late_stage_failure, worker cancel, dag_universe rekey) — all pass against real Postgres.
- [x] Assert read-your-writes behavior at the HTTP boundary: token-bearing reads either return fresh corrected data or structured `503` with `Retry-After` and exactly `x-consistency: transient-lag` or `rebuild-in-progress`; stale post-correction answers, unclassified failures, direct projection rebuilding, in-process app construction, and internal-service shortcuts fail the test.
    **Note:** _token_read_never_stale is the read-your-writes assertion: token-bearing /v1/query/structured returns 200 with corrected state (never stale_value) OR a structured 503 with retryable=True, x-consistency exactly one of transient-lag/rebuild-in-progress (matching the header), Retry-After>0 (transient-lag<30s, rebuild>=30s). Any stale post-correction answer, unclassified failure, or missing/inconsistent consistency header fails the test. Direct projection rebuilding, in-process app construction, and internal-service shortcuts are statically forbidden by the guardrails (test_scenario_does_not_rebuild_projections, no ReplayDriver/wipe, no TestClient/create_app, empty _ALLOWED_UMD_IMPORTS).
- [x] Run the complete hosted acceptance command after stack boot, for example `UMD_TEST_POSTGRES=true pytest -q tests/test_hatchet_live.py tests/test_api_boundary_e2e.py --junitxml=live-boundary-junit.xml`, plus the explicit external `/v1/health`, `/v1/ready`, `/v1/capabilities`, and `/v1/version` probes; require all mandatory live tests to execute on protected/main, not to be skipped because an environment flag was omitted.
    **Note:** Wired hosted acceptance into .github/workflows/validation.yml docker-e2e job: (1) "External HTTP representative flows" step now probes /v1/health,/v1/ready,/v1/capabilities,/v1/version explicitly (added curl for /v1/ready). (2) "Run the public-boundary E2E scenario against the live stack (P4-S7)" step runs `pytest -q tests/test_api_boundary_e2e.py --junitxml=boundary-junit.xml` with UMD_API_BASE_URL=http://127.0.0.1:${UMD_API_PORT:-8080} + hatchet env, FAILS (exit 1) if any test skipped, writes public-boundary-gate.txt. (3) After-restart boundary step similarly gates and writes public-boundary-after-restart-gate.txt. (4) Aggregate live-worker release gate (if: always()) now requires live-worker-gate.txt AND public-boundary-gate.txt AND public-boundary-after-restart-gate.txt all PASS. Both gate files added to artifact upload. RACE RECONCILIATION: the 4 test_live_hatchet_* execution tests now take a new session `live_db` fixture (conftest.py) = SHARED compose `umd` db (single source of truth; DSN env-overridable UMD_LIVE_POSTGRES_DSN); compose worker is SOLE execution target (HatchetWorkerFactory.start only registers, never starts a loop), tests poll the shared db the compose worker writes to — no competing in-process worker, no throwaway-db polling race, no skip/weaken, no own engine. Live suite (cluster/docker) step unchanged (already fail-on-skip). Verified: guardrails 6 pass, postgres suite 201 pass, unit suite 376 pass, ruff clean, mypy src clean, workflow YAML valid.

### Phase 5: Post-green documentation and capability publication
- [ ] After the repaired hosted run is green, retrieve and inspect the complete AT-1–AT-19 evidence bundle (including AT-11 Bash/pipefail audit, zero mandatory skips, eligible-tenant identity/partitions, durable latest-version rows, assignment/runtime rows, callback-owned rows, external HTTP proof, and artifact capture before teardown) before changing any documentation or closing any DoD row; if any evidence is absent, stale, skipped, configured-unavailable, readiness-only, or failed, keep Phase 5 blocked.
- [ ] Do not edit documentation until the product path, split Hatchet stack, hosted workflow, and HTTP-only scenario have a green authoritative GitHub run with retrieved artifacts; record the exact pushed SHA, run URL, job IDs, attempt, image digests, pin validation, and measured counts before changing docs.
- [ ] Update `README.md`, `docs/testing.md`, `docs/deployment.md`, `docs/providers.md`, `docs/limitations.md`, `docs/runbooks.md`, and maintained client examples only after that evidence, documenting the final API/worker startup, canonical nested environment names, migrations, persistent volumes, stop/start and rollback/drain procedures, HTTP-only validation, scheduler pin, provider/model assumptions, and exact commands.
- [ ] Publish the capability matrix using only observed behavior and the exact vocabulary `active`, `reference-only`, `configured-but-unavailable`/`configured-unavailable`, `gated`, and `disabled`, including owner, reason, observed version, proof command, and permitted-gate status for sandbox isolation, Tesseract/PaddleOCR, faster-whisper model provisioning, pyannote licensing/weights, pgvector HNSW, splink/DuckDB, Vecalign, and enhanced media tooling; never document a gated provider as active.
- [ ] Update the release summary and maintained counts from retrieved GitHub JUnit/coverage/diagnostic artifacts rather than local runs, and reject publication if any mandatory test was skipped, any count is stale, any capability claim exceeds runtime evidence, or any mandatory DoD row remains FAIL.

### Phase 6: Final QA, adversarial review, and complete DoD closure
- [ ] Compose AT-16 (direct v1 `(input, ctx)` callback, real `DurableStageExecutor`, JSON-safe acknowledgement, callback-owned rows), AT-17 (exactly one eligible tenant, non-null partitions, identity agreement, assignment/runtime), and AT-18 (`durable_task` for every canonical stage and latest-version `is_durable=true`) with AT-1–AT-15 as the single AT-19 release gate; require the retrieved hosted rerun to show rebuilt API/worker installed-schema resolution, `Worker.isActive`-correct diagnostics, callback counts scoped to the selected tenant's live job-ID/submission window (with the mock/pre-existing row rejected), canonical barrier/key/replay/side-effect evidence from Phase 2 amendment 2, `skipped=0`, and no mandatory FAIL before QA can close Plan K.
- [ ] Re-execute every affected P2-S1, P2-S2, P2-S3, P2-S5, P2-S6, P2-S8–P2-S13, P3-S3, and P4 hosted-gate step before this phase's review, then run the final 35-row `Task.md` matrix and all QA/adversarial checks; do not duplicate authority in another plan or use a prior K/J annotation, readiness-only result, queued-only result, or hermetic test as closure evidence.
- [ ] Run the full QA review over the complete product and CI diff, including test and documentation analyzers, security review of upload/parser/archive/sandbox/API/Compose/workflow surfaces, fresh `aft_inspect`, static architecture/ownership checks, and the exact lint/type/unit/Postgres/Docker/HTTP boundary commands; classify local-only provider/platform absence as named gates only.
- [ ] Perform a fresh adversarial code review of all twelve required risks: provenance loss; source/semantic conflation; irreversible merges; invalidation overreach; stale corrected semantics; locator drift; cross-language collapse; adaptation/subtitle flattening; scheduler races/restarts; inconsistent stores; unsafe media/archive handling; and unvalidated capability claims. Include callback registration, JWT/gRPC routing, queued-state reconciliation, retry amplification, cancellation, and DAG-universe drain in the review evidence.
- [ ] Route every behavior or gate finding through Support diagnosis, R&D/design clarification, this planning/plan-amendment path, and Exec implementation; do not use Exec-Fixer to stub, skip, weaken, or conceal an architectural, scheduler, modality, or release-gate defect. Re-run affected focused tests and then the complete validation suite after every repair cycle.
- [ ] Build and inspect the final Task.md §40 matrix with exactly 35 rows and status `PASS`, `FAIL`, or permitted `GATED`; every GATED row has a named owner, reason, proof command, and visible hosted summary, and no mandatory row is FAIL. Map each row explicitly: 1 adversarial technology/design process → approved DD/adversarial evidence; 2 implementation-ready design → approved DD plus this plan; 3 service implemented → pushed production implementation and hosted API/worker identity; 4 persistent source storage → OCFL fixity/volume evidence; 5 text/book ingestion → HTTP TXT/Markdown/EPUB/PDF evidence; 6 image ingestion → HTTP raster/OCR evidence; 7 audio ingestion → HTTP ordinary-speech/VAD/ASR evidence; 8 video ingestion → HTTP video/audio/subtitle evidence; 9 stable addressable segments → segment IDs/locators; 10 exact provenance → OCFL/source/evidence refs and metadata; 11 evidence/confidence assertions → evidence and semantic metadata assertions; 12 multilingual coexistence → language-specific source/graph assertions; 13 adaptation/continuity boundaries → adaptation/continuity alignment evidence; 14 cross-source alignment → many-to-many alignment evidence; 15 reversible entity resolution → merge/split/history evidence; 16 user overrides → public override/lock evidence; 17 segment editing → append-only segment edit evidence; 18 semantic editing → append-only semantic edit evidence; 19 dependency invalidation → descendant-only target/checksum evidence; 20 individual stage reruns → selective rerun evidence; 21 durable restartable asynchronous jobs → stop/start/reclaim/replay evidence; 22 structured source referencing → locator/evidence retrieval; 23 semantic KG-style questioning → typed public question answers; 24 structured graph querying → bounded typed graph query; 25 supporting evidence references → answer support retrieval; 26 audit/history → actor/prior/cause explanation; 27 swappable model/provider interfaces → provider registry/contract evidence; 28 local/self-hostable model path → real local/reference provider or named permitted gate with proof; 29 heterogeneous/contradictory multi-source tests → HTTP scenario assertions; 30 correction→invalidation→selective-rerun E2E → hosted correction scenario; 31 Docker deployment → split Compose boot and image evidence; 32 lint/type/static checks → hosted Ruff/mypy/static results; 33 automated tests → hosted unit/Postgres/Docker/HTTP JUnit; 34 final adversarial correctness review → twelve-risk review; 35 repair findings and complete rerun → bounded repair route plus retrieved rerun artifacts.
- [ ] Push the final path-scoped product and CI commits, retrieve and inspect every GitHub report/artifact and machine-readable release summary, verify the final tree has no fake production runner path, no mandatory live skip, no duplicate scheduler/authority, no global-count callback false positive, no installed-image schema-resolution failure, and no unresolved mandatory FAIL, then record the final PASS/FAIL/GATED release decision and exact reproducible start/validation commands.

## Completion Criteria
- The API release factory uses `ProductionDAGRunner` over one shared runtime assembly; `DurableDAGRunner` is test-only and cannot report scheduler `active`; all nine real stages, modality/provider paths, durable repositories, semantic ledger, OCFL, projections, and observability are wired without fabricated completion.
- The full pinned Hatchet split topology boots on hosted native Docker with a real JWT, real SDK client, engine-visible nonzero registrations, real callbacks through `DurableStageExecutor`, callback-owned Postgres/OCFL evidence, and unconditional protected/main live-worker validation; a readiness line alone never closes the gate.
- Hosted CI installs and proves the required multipart, FFmpeg, and PostgreSQL client toolchain, preserves required secret interpolation, captures failure artifacts before teardown, and fails on live-path skip, failure, missing report, or missing machine-readable summary. Local Docker absence is only a named local gate.
- The external HTTP-only heterogeneous scenario passes for real text/image/audio/video/independent subtitle sources and proves provenance, stable locators, multilingual/adaptation individuality, semantic/structured queries, reversible edits, correction, descendant-only invalidation, selective rerun, duplicate/retry/cancel/restart, read-your-writes consistency, OCFL fixity, and audit history without internal-service or projection shortcuts.
- Documentation is updated only after behavior and hosted workflow evidence are green, with measured counts and honest capability/gate matrices.
- Final QA and adversarial review cover all required security, ownership, provenance, semantic, locator, modality, scheduler, restart, storage, and capability risks; all findings are repaired through the Support → R&D → plan → Exec route and the complete suite is rerun.
- The final retrieved GitHub evidence contains a complete 35-row Task.md DoD matrix with no mandatory `FAIL`, no hidden mandatory skip, and only explicitly owned/permitted `GATED` statuses.

## References
- `Task.md` §40 items 1–35 and §§1–41
- `artifacts/designs/pending/DD-universal-media-decomposer-plan-k-live-hatchet-blocker.md`
- `artifacts/designs/pending/DD-universal-media-decomposer-ci-repair.md`
- `artifacts/designs/parts/universal-media-decomposer/CONTRACTS.md`
- `artifacts/designs/process/universal-media-decomposer-ci-repair-pattern-enforcer-approval.md`
- `artifacts/designs/parts/universal-media-decomposer/HATCHET_LIVE_VALIDATION_HANDOFF.md`
- `artifacts/plans/handoff-G-to-I-J.md`
- `artifacts/plans/pending/TASK-universal-media-decomposer-G-production-runner-api.md` (preserved foundation; open portions superseded)
- `artifacts/plans/pending/TASK-universal-media-decomposer-H-local-providers-modalities.md` (preserved foundation; open portions superseded)
- `artifacts/plans/pending/TASK-universal-media-decomposer-I-hatchet-worker-integration.md` (preserved foundation; open portions superseded)
- `artifacts/plans/pending/TASK-universal-media-decomposer-J-api-boundary-ci-release.md` (Phases 1–2 baseline; unfinished Phases 3–4 superseded)
- `src/umd/api/app.py`, `src/umd/api/entrypoints.py`, `src/umd/application/jobs.py`, `src/umd/jobs/{production,runner,hatchet,stage_execution,capability,drain,invalidation}.py`, `src/umd/deploy/cli.py`
- `deploy/compose.yaml`, `deploy/Dockerfile`, `deploy/docker-entrypoint.sh`, `deploy/pins/runtime.txt`, `.github/workflows/validation.yml`, `.github/scripts/`
- `tests/test_hatchet_live.py`, `tests/test_api_boundary_e2e.py`, `tests/test_api_boundary_guardrails.py`, `tests/test_production_architecture.py`, `tests/test_api_contract.py`, `tests/test_capability_transitions.py`, `tests/test_deployment_phaseE.py`
- Hosted baseline: <https://github.com/xiaden/Universeity/actions/runs/33164294061> (jobs `98825909969`, `98825910133`, `98825910085`, `98825909849`; artifacts `9682936266`, `9682972550`, `9682930252`)
