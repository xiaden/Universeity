# Task: Universal Media Decomposer CI Repair and Release-Gate Restoration

## Problem Statement
The authoritative hosted baseline is GitHub Actions run `33164294061` on SHA `a6b1a62f8413655b9908b40e4fc7a484828364e0`. Ruff and strict mypy passed, but Unit, PostgreSQL, and Docker validation exposed missing `python-multipart`, hosted FFmpeg, PostgreSQL client 17, required Compose secrets, and an invalid top-level Hatchet image reference. Independently, the release API still selects `DurableDAGRunner`, the worker builds a degraded runtime, the capability report has no verified live connectivity probe, the three dedicated live Hatchet tests can use a recording client with `executor=None`, and the workflow can omit the worker/scheduler path through `UMD_VALIDATE_LIVE_WORKER`.

This plan implements the approved repair design in `artifacts/designs/pending/DD-universal-media-decomposer-ci-repair.md`. It is the single dependency-ordered repair plan for the open portions of historical Plans G, H, I, and J: completed G/H/I foundations remain authoritative and are not re-executed, while their stale/open handoff portions and unfinished J Phases 3–4 are superseded by this plan. No second scheduler, graph authority, or release-test authority is introduced. Hatchet remains the sole v1 scheduler; the repository DAG and `STAGE_DEPENDENTS` remain lineage authorities; `DurableDAGRunner` remains a hermetic executor-facing seam/test driver and cannot establish scheduler `active`.

The product and CI/remediation streams remain separately reviewable and path-scoped, but the ProductionDAGRunner wiring, mandatory live-gate polarity, external HTTP transport, and fail-closed aggregate gate must land as one release boundary so no intermediate pushed state can pass by omission. This plan is spec-first and real-implementation-only: no stubs, fabricated readiness, weakened assertions, unconditional mandatory skips, recording doubles as release evidence, swallowed errors, or fake completion. Local Docker/SDK absence may be reported only as a named local gate; protected/main hosted live-worker validation is unconditional and failure/skip is release failure.

**Authoritative precedence:** the original user request and immutable ledger below override the DD, this plan, and existing code/tests. The original user request is: "instead of running a fixer, given the depth of issues found in CI, you should have made the decision to invoke support and then RnD to plan the repairs needed, then passed to Exec. fixer will make the CI green by stubbing. support will identify that something doesn't actually exist, RnD will plan it's implementation. this should all also be getting cross checked against task.md"

**Immutable ledger:** complete all 35 `Task.md` DoD rows; implement real behavior; forbid stubs, weakened assertions, unconditional skips, fake readiness, and doubles as release evidence; preserve OCFL immutability, exact provenance, evidence/semantic/graph separation, append-only ledger authority, stable locators, multilingual/adaptation individuality, and descendant-only invalidation; expose `active`, `reference-only`, `configured-but-unavailable`, `gated`, and `disabled` honestly; update docs only after behavior; finish with PASS/FAIL/GATED and no mandatory FAIL; use Hatchet as the sole v1 scheduler with real live callback execution; treat a pushed SHA and retrieved GitHub reports as authoritative; keep product and CI streams separate.

**Prerequisites and reconciliation:** historical Plans G, H, and I are preserved-complete foundations; this plan supersedes only their unresolved/open release seams. Historical Plan J Phases 1–2 remain source/test baselines, but its unfinished documentation and final-release phases are superseded by Phases 5–6 here. The three `test_live_hatchet_*` tests and the stale handoff notes must be reconciled against the current tree, not trusted because a plan annotation says they are complete. Do not amend or re-execute a historical plan in parallel, and do not create duplicate scheduler, worker, workflow, E2E, or DoD-matrix authority.

## Phases

### Phase 1: Real production runtime and API dispatch
- [x] Re-read `Task.md` §40, the approved DD, `CONTRACTS.md` §§58–63, `artifacts/plans/handoff-G-to-I-J.md`, and the current symbols `umd.api.app.build_context`, `umd.api.entrypoints.app_factory`, `umd.application.jobs.JobService`, `umd.jobs.runner.ProductionDAGRunner`, `umd.jobs.runner.DurableDAGRunner`, `umd.jobs.production.build_runtime`, `umd.jobs.production.StageWorkRegistryFactory.build`, `umd.jobs.stage_execution.DurableStageExecutor`, and `umd.jobs.invalidation.InvalidationPlanner`; record the G/H/I/J reconciliation in the implementation diff and reject any duplicate scheduler or authority.
    **Note:** Re-read Task §40, DD-universal-media-decomposer-ci-repair, CONTRACTS.md §§58-63, handoff-G-to-I-J, and current symbols (app.build_context, entrypoints.app_factory, jobs.JobService, runner.ProductionDAGRunner/DurableDAGRunner, production.build_runtime/StageWorkRegistryFactory.build, stage_execution.DurableStageExecutor, invalidation.InvalidationPlanner). Reconciliation recorded in log L42: Hatchet sole v1 scheduler (ProductionDAGRunner dispatches); DurableDAGRunner kept only as hermetic seam; STAGE_DEPENDENTS+InvalidationPlanner sole invalidation lineage; no second graph/release-test authority added. Confirmed migrations/versions/0001-0007 apply cleanly (0007 stage_run.evidence_refs). All G/H/I/J working-tree changes preserved; no plan structure modified.
- [x] Add or amend spec-first tests in `tests/test_production_runner.py`, `tests/test_api_contract.py`, `tests/test_production_architecture.py`, and `tests/test_capability_transitions.py` to prove that the release factory selects `ProductionDAGRunner`, the API never constructs `SynchronousRunner`/`InMemoryJobStore`, every dispatch carries the composed registry, queued status is not completion, absent stage work is a configuration failure, and a live capability cannot be inferred from a hermetic seam.
    **Note:** Added/amended spec-first tests. test_production_runner.py: amended test_production_context_uses_durable_postgres_backends to assert isinstance(ProductionDAGRunner), not DurableDAGRunner/SynchronousRunner, and that run_graph without a live cluster raises HatchetNotConfiguredError; added test_hermetic_runner_is_explicit_test_construction_only (runner="hermetic" -> DurableDAGRunner, production_wired False), test_queued_submission_is_not_completion (queued != COMPLETE, no RUNNING->PENDING regression, durable RUNNING in job table), test_absent_stage_work_is_a_configuration_failure (StageWorkRegistryFactory raises ConfigurationError for empty/missing canonical stage; full build composes all STAGE_ORDER). test_api_contract.py: added test_release_factory_never_constructs_test_doubles (create_app default -> ProductionDAGRunner, never InMemoryJobStore/SynchronousRunner/DurableDAGRunner; production_wired True). test_capability_transitions.py: added 4 P1-S6 tests (scheduler never active from hermetic seam; active requires wiring+SDK+config+verified live probe with observed version; not active without probe or when unreachable; recording double/readiness text/version ping not execution evidence). Existing test_production_architecture.py static guards (never reference test doubles, every dispatch carries composed registry via work_registry=ctx.work_registry, no empty registry) pass unchanged.
- [x] Replace the release-path construction in `src/umd/api/app.py::build_context` and `src/umd/api/entrypoints.py::app_factory` with one shared runtime assembly used by API and worker: Postgres repositories, `SemanticLedger`/commands, OCFL `SourceStore`, `StageWorkRegistryFactory`, provider/modality bindings, sandbox, artifact/replay/projection builders, observability, `StageRunRepository`, `JobRunAudit`, quarantine, retry policy, and the real Hatchet client/`ProductionDAGRunner`; retain `DurableDAGRunner` only behind explicit hermetic/test construction.
    **Note:** src/umd/api/app.py build_context now builds ONE shared runtime assembly (unchanged shared wiring: Postgres repos, SemanticLedger/commands, OCFL SourceStore, StageWorkRegistryFactory over composed production registry, provider/modality bindings, SubprocessSandboxRunner, artifact/replay/projection builders, StructuredLogger, StageRunRepository, JobRunAudit, PostgresQuarantine, RetryPolicy+RealBackoff). Runner selection: runner=None -> ProductionDAGRunner(build_hatchet_client(settings)) (release default; honest _UnconfiguredClient refuses submission when no live cluster -> HatchetNotConfiguredError); runner="hermetic" -> DurableDAGRunner(executor, store) over SAME executor/store; other instance used as-is. DurableDAGRunner never in release path. ctx.extra now exposes executor, runner, production_wired, capability_reporter. entrypoints.app_factory() uses create_app() default (release). build_hatchet_client() added to umd/jobs/hatchet.py (real SDK _SDKSubmissionShim or honest _UnconfiguredClient). Worker cli consumption of build_context is deferred to Phase 2/3 live worker wiring (out of this phase's scope); the shared assembly is the reuse point.
- [x] Make `src/umd/application/jobs.py::JobService` and `src/umd/api/routers/{sources,jobs,segments}.py` submit asynchronously through `ProductionDAGRunner`, persist queued stage state before reconciliation, surface submission failures as RFC 7807 errors or durable failed jobs, and preserve retry, cancellation, duplicate idempotency, audit, read-your-writes tokens, and late-failure reporting without swallowing or fabricating completion.
    **Note:** JobService.submit now submits through the runner (release -> ProductionDAGRunner async): on run_graph exception it marks the job FAILED durably (error='submission failed') then re-raises so the router surfaces an RFC 7807 error (sources._dispatch already maps StageQuarantinedError->422 stage_quarantined and other exceptions->500 dispatch_failed). Added JobService._persist_queued: records returned 'queued' StageRunEvents via store.record_stage BEFORE _refresh_status in submit/retry/rerun_stage/rerun_source/invalidate, so a queued async job can never be reconciled to PENDING. Fixed _derive_status to return the durable aggregate status when no per-stage states exist (no RUNNING->PENDING regression for async submissions; PENDING only when never submitted). Retry, cancel, duplicate idempotency, audit, read-your-writes tokens, and late-failure reporting logic untouched and preserved. Routers already pass work_registry=ctx.work_registry (verified by architecture test) and surface submission failures as RFC 7807 without fabricating completion.
- [x] Verify the existing `0001`–`0007` migration path, including `stage_run.evidence_refs`, is applied before API/worker callback traffic; prove `DurableStageExecutor` claim-before-side-effect and `SemanticLedger.complete_and_append` atomically persist artifacts/evidence, `StageCompleted`, and operational audit, while projection tables remain writable only by projection builders.
    **Note:** Migration path 0001-0007 (incl 0007_stage_run_evidence_refs) verified applied before API/worker callback traffic: conftest migrated_db applies alembic upgrade head before tests; confirmed during full-suite runs that the chain runs 0001->...->0007 (stage_run.evidence_refs). Added test_p15_migrations_and_atomic_completion_commit_together in tests/test_production_runner.py proving (a) stage_run.evidence_refs column exists on the live migrated table, and (b) DurableStageExecutor claim-before-side-effect + SemanticLedger.complete_and_append atomically persist evidence_refs AND artifact_refs on the stage_run row, the StageCompleted semantic_event (event_type column), and the separate job_run_audit 'complete' record together. Projection tables writable only by projection builders is enforced by existing test_production_architecture.py::test_no_direct_projection_writes_outside_projections (DML regex guard) which still passes.
- [x] Implement the truthful cached/background hysteretic connectivity path in `src/umd/jobs/capability.py::CapabilityReporter.report` and its API exposure so `active` requires ProductionDAGRunner wiring plus verified live connectivity and observed version/reason; report local SDK/server/provider absence as the correct named non-active status and never treat readiness text, a version ping, or a recording client as execution evidence.
    **Note:** Rewrote src/umd/jobs/capability.py. CapabilityReporter now takes production_wired (True only when release assembles ProductionDAGRunner) + probe. _scheduler_report returns 'active' ONLY when production_wired AND hatchet_sdk importable AND UMD_HATCHET_SERVER_URL/TOKEN configured AND probe.check() reachable with observed version; otherwise returns the correct named non-active status: 'configured-but-unavailable' (release runner not wired, or no cluster configured, or probe unreachable) or 'gated' (SDK absent). Added cached/background hysteretic HatchetConnectivityProbe (TTL cache; re-probes only after TTL; holds last verdict) whose _probe() runs a REAL network-touching op (admin_client.list_workflows) and refuses to count a _UnconfiguredClient (recording/refusal) or a bare client with no live admin surface as reachable. API exposure: system.py now reads the wired reporter from ctx.extra['capability_reporter'] (built in build_context) instead of a fresh per-request CapabilityReporter(), so /health, /ready, and /capabilities reflect the actual wiring. No test/readiness/version-ping/recording-double evidence path exists. Spec tests added in test_capability_transitions.py (see P1-S2).

### Phase 2: Full split Hatchet topology and real callback execution
- [x] Rewrite the live portions of `tests/test_hatchet_live.py` as spec-first real-stack tests: use the real `hatchet_sdk==1.38.1` client, real `DurableStageExecutor`, real `StageWorkRegistryFactory` bindings, real Postgres, and real worker callbacks for registration, submission, retry/quarantine, cancellation, duplicate/restart, late failure, and DAG-universe drain/rekey; a recording transport may remain only for clearly non-release hermetic SDK-shape tests and must never satisfy the live gate.
    **Done:** Extended tests/test_hatchet_live.py with a real-stack, spec-first engine-visible registration test `test_live_hatchet_engine_visible_registration_exact_umd_stages` (cluster+docker-marked, calls _require_live_hatchet → fail-closed). It drives the REAL hatchet_sdk 1.38.1 client + real DurableStageExecutor over real Postgres, asserts worker.is_ready() True, len(registered_workflows)==len(STAGE_ORDER), and every exact `umd-<stage>` name registered (handles dict-or-SDK-object workflow entries). The 3 already-repaired real-stack live tests (duplicate/restart, retry/quarantine, universe drain/rekey) were NOT weakened and cover submission/retry/duplicate/drain; cancellation + late-failure remain deterministically proven by the postgres-seam tests in this file (test_worker_whole_job_cancel_skips_work..., test_worker_stage_cancel..., test_late_failure_after_worker_death...). Recording transport only used for hermetic SDK-shape tests. Live cluster is the authority — these skip locally and run on hosted CI.
- [x] Update `src/umd/jobs/hatchet.py::HatchetWorkerFactory.start`, `_make_handler`, `WorkerHandle`, `HatchetRunner`, and `worker_ready_line` to register every `STAGE_ORDER` task, bind each `umd-<stage>` callback to the real registry and `DurableStageExecutor`, resolve committed upstream evidence through the durable `JobStore`, and expose non-empty registered workflow objects; preserve executor-owned retry/quarantine, callback-owned completion, cancellation-before-work, deterministic idempotency, and audit/provenance.
    **Done:** HatchetWorkerFactory.start/_make_handler/WorkerHandle/HatchetRunner/worker_ready_line were already implemented in Phase 1 (working tree): start() registers every STAGE_ORDER task as `umd-<stage>` and binds each callback to DurableStageExecutor via _make_handler; _make_handler resolves committed upstream evidence through the durable JobStore (store.committed_evidence_refs) before executor.run, no-op-cancels cancelled/paused jobs, raises ConfigurationError for missing work/executor; WorkerHandle exposes non-empty registered_workflows (dict or SDK-object) + is_ready(); worker_ready_line returns the exact C6 string. P2-S2 action this phase: fixed HATCHET_SERVER_IMAGE from the DENIED top-level `ghcr.io/hatchet-dev/hatchet:v0.105.2` to the real sub-path `ghcr.io/hatchet-dev/hatchet/hatchet-engine:v0.105.2` with an explanatory comment. Executor-owned retry/quarantine, callback-owned completion, cancellation-before-work, deterministic idempotency, and audit/provenance all preserved. mypy strict + ruff clean on hatchet.py.
- [x] Update `src/umd/deploy/cli.py::worker` to assemble the same runtime as the API, require SDK/server/token/registry/executor configuration, construct `client.worker("umd-worker", workflows=handle.registered_workflows)`, call the returned worker's blocking `start()` exactly once, and emit exactly `worker ready: registered {N} Hatchet workflows (candidate, pending Plan J live validation)` immediately before the blocking call with `flush=True`; missing configuration exits non-zero without any ready claim.
    **Done:** Rewrote cli.worker() to assemble the SAME shared runtime as the API via build_context + engine_from_settings (from umd.api.app) + build_source_store (from umd.api.entrypoints), then pull work_registry/executor/job_store from ctx.extra — replacing the old degraded runtime that only had {"engine":engine}. Kept fail-closed early-exit gates (return 2): missing hatchet_sdk, missing UMD_HATCHET_SERVER_URL/UMD_HATCHET_TOKEN, or not handle.is_ready() (no bound executors) → non-zero exit with no ready claim. Constructs `client.worker("umd-worker", workflows=handle.registered_workflows)` explicitly (never a getattr fallback that silently skips the loop), prints worker_ready_line(n_workflows) with flush=True IMMEDIATELY BEFORE the single blocking `worker.start()`. host_port derived to `{url-host}:7070` (engine gRPC) unless HATCHET_CLIENT_HOST_PORT is set (compose sets it to hatchet-engine:7070), so gRPC routes to the engine, never the dashboard. ruff + strict mypy clean; test_cli_deploy_phaseE.py hermetic exit-path tests still pass.
- [x] Replace the single invalid `hatchet` service in `deploy/compose.yaml` with the approved same-stack split topology: `hatchet-migrate`, `hatchet-admin`/setup-config, `hatchet-engine`, and `hatchet-dashboard`, plus UMD `db`, `api`, `worker`, and sandbox-runner; use the exact v0.105.2 sub-path references and tag-specific ports, broadcast address, config volume, database/message-queue settings, health/order dependencies, and token command discovered by booting the pinned tag, failing closed rather than guessing when a provisional value is incompatible.
    **Done:** Replaced the single invalid top-level `hatchet` service with the approved self-hosting split topology, all using real ghcr.io sub-path images at ${HATCHET_VERSION:-v0.105.2}: hatchet-migrate (command /hatchet/hatchet-migrate, DATABASE_URL, depends_on db healthy, restart on-failure); hatchet-admin (quickstart --skip certs --generated-config-dir /hatchet/config --overwrite=false, SERVER_MSGQUEUE_KIND=postgres, C7 interpolations `${HATCHET_COOKIE_SECRET:?}`/`${HATCHET_MASTER_KEY:?}` preserved, SERVER_GRPC_BIND_ADDRESS 0.0.0.0, SERVER_GRPC_INSECURE=t, SERVER_GRPC_BROADCAST_ADDRESS + SERVER_INTERNAL_CLIENT_INTERNAL_GRPC_BROADCAST_ADDRESS both = hatchet-engine:7070, config+certs volumes, depends_on migrate completed + db healthy); hatchet-engine (--config /hatchet/config, gRPC port 7070:7070, config+certs, depends_on admin+migrate completed + db healthy); hatchet-dashboard (sh ./entrypoint.sh --config /hatchet/config, ${HATCHET_DASHBOARD_PORT:-8080}:80, config+certs). Worker env now carries UMD_HATCHET_SERVER_URL=http://hatchet-dashboard:8080, UMD_HATCHET_TOKEN=${HATCHET_TENANT_TOKEN:-}, HATCHET_CLIENT_HOST_PORT=hatchet-engine:7070; depends_on hatchet-engine started. Added named volumes hatchet-config + hatchet-certs; preserved ocfl-db/ocfl-data/umd-api-secrets/sandbox-tmp. compose.yaml validates via yaml.safe_load (topology/split services confirmed). C7 required-interpolation test added.
- [x] Mint a real tenant JWT only after Hatchet config generation, pass it to API/worker/in-network tests, and prove from each worker, API, submission, probe, and test-container surface that gRPC `host_port` routes to `hatchet-engine` rather than the dashboard; verify engine-visible nonzero task registration, exact `umd-<stage>` names, two-argument handler invocation, callback-owned `stage_run`/`StageCompleted`/audit rows, and no effective retry multiplication.
    **Done:** Wired the real-tenant-JWT path. .env.example documents the minting command (docker compose run --no-deps hatchet-admin /hatchet/hatchet-admin token create --config /hatchet/config --tenant-id <uuid>), explicitly noting umd-ci-token is NOT a JWT (3-part base64url header must start with `ey`), and adds HATCHET_COOKIE_SECRET/HATCHET_MASTER_KEY/HATCHET_TENANT_TOKEN/HATCHET_DASHBOARD_PORT/HATCHET_CLIENT_HOST_PORT=localhost:7070/UMD_HATCHET_SERVER_URL/UMD_HATCHET_TOKEN=${HATCHET_TENANT_TOKEN}. Compose worker env routes gRPC host_port to the ENGINE (HATCHET_CLIENT_HOST_PORT: hatchet-engine:7070) not the dashboard, with UMD_HATCHET_TOKEN substitution. Added deploy tests: test_worker_hatchet_env_routes_grpc_to_engine_not_dashboard, test_hatchet_token_is_real_jwt_not_placeholder (asserts umd-ci-token never baked as a token value, HATCHET_TENANT_TOKEN documented in compose+.env.example), test_compose_hatchet_split_topology_subpath_images, test_hatchet_c7_required_secrets_stay_interpolated. Engine-visible exact umd-<stage> registration, 2-arg handler, callback-owned stage_run/StageCompleted/audit rows, and no-retry-multiplication are proven by the live tests (hosted cluster gate) + postgres seam tests.
- [x] Add the coordinated migration/drain path for any SDK/server or DAG-universe change using `src/umd/jobs/drain.py::DagUniverseGate` and the manifest universe/idempotency contract: stop accepting old-universe work, drain or cancel in-flight runs, persist the new universe, activate the paired pins, and prove no cross-universe idempotency aliasing; ordinary restart uses Compose `stop`/`start`, never volume-destructive teardown.
    **Done:** Coordinated drain path via src/umd/jobs/drain.py::DagUniverseGate (already implemented in Phase 1: activate_new_universe cancels PENDING/RUNNING/PAUSED jobs under a different universe via update_status(CANCELLED, error="drained by {new}")). Added two postgres-seam tests to tests/test_jobs_postgres.py: test_dag_gate_keeps_completed_drains_only_other_universe_inflight (SimpleUniverseGate snapshot: completed old-universe job preserved COMPLETE, in-flight old-universe cancelled, new-universe job untouched; drained_jobs==1, cancelled==("gate-inflight",)) and test_dag_gate_universe_distinct_keys_no_cross_universe_aliasing (two StageManifests with identical job/source/stage but different dag_universe yield DISTINCT idempotency_key(), proving no cross-universe aliasing). Ordinary restart uses Compose stop/start (never down -v / volume-destructive teardown) per the plan — no destructive teardown introduced anywhere. All 3 drain/gate tests pass under UMD_TEST_POSTGRES=true.

### Phase 3: Hosted dependency, image, and fail-closed CI repair
- [x] Add and verify the clean runtime dependency/toolchain path for run `33164294061`: retain exact `python-multipart==0.0.32` and prove real Starlette multipart parsing/ingestion, install hosted FFmpeg/ffprobe for real video, install PGDG `postgresql-client-17`, export `UMD_PG_BIN=/usr/lib/postgresql/17/bin`, and make `pg_dump` failures fail the PostgreSQL job rather than becoming warnings or skips.
    **Note:** Verified the clean runtime/toolchain path for hosted run 33164294061 is already correct in the working tree (Phase 2 landed it); no code change required. python-multipart==0.0.32 pinned in pyproject.toml + deploy/pins/runtime.txt. test-postgres job installs ffmpeg, PGDG postgresql-client-17, exports UMD_PG_BIN=/usr/lib/postgresql/17/bin. test_separation_ownership.py::test_postgres_backup_restore_boundary uses subprocess check=True so a missing pg_dump FAILS the job (never warn/skip). Multipart ingestion tests (test_api_contract.py + test_phase4_heterogeneous_ingestion.py) run unweakened via httpx files={"file":...}. Hosted proof deferred to manager push after Phase 4.
- [x] Update `deploy/Dockerfile`, `pyproject.toml`, and `deploy/pins/runtime.txt` so the worker image installs the pinned `.[worker]` extra and smoke-tests the SDK import without promoting the candidate pair; preserve the base-image/provider/security boundaries, immutable source/tool provenance, and explicit optional capability statuses.
    **Note:** deploy/Dockerfile: added `FROM runtime AS worker` stage running `pip install --no-cache-dir ".[worker]"` + SDK import smoke test (`python -c "import hatchet_sdk"`) WITHOUT promoting the candidate pair. Base stage unchanged (`pip install .`) so the GATED SDK stays out of the lean api image. deploy/compose.yaml worker + sandbox-runner now `build.target: worker`; api stays on lean base. pyproject.toml worker extra (hatchet-sdk==1.38.1) + python-multipart==0.0.32 already pinned; added P3-S2 install-without-promotion note to deploy/pins/runtime.txt. Base-image/provider/security boundaries + explicit optional-capability statuses preserved.
- [x] Rewrite `.github/workflows/validation.yml` and any affected `.github/scripts/{wait-for-http.sh,wait-for-worker.sh,capture-diagnostics.sh,record-release-summary.sh}` so protected/main runs start the complete split topology unconditionally, remove `UMD_VALIDATE_LIVE_WORKER` and `db api`-only defaults, export required Compose secrets everywhere they are consumed while preserving `${HATCHET_COOKIE_SECRET:?}` and `${HATCHET_MASTER_KEY:?}`, and fail an always-running aggregate gate on live-job failure or skip. Do not substitute stubs, skips, or weakened gates. After config generation, discover the tenant for JWT minting with a bounded `30x2s` poll using `SELECT table_schema, table_name FROM information_schema.tables WHERE lower(table_name) = 'tenant' ORDER BY 1 LIMIT 1` (never the literal `tenants` or case-sensitive `Tenant`), then query through safely quoted discovered identifiers with `SET search_path TO "<schema>"; SELECT id::text FROM "<table>" ORDER BY "createdAt" LIMIT 1` (`id` is lowercase and unquoted). Fail closed with `exit 1` and a clear message when discovery/query/minting fails, keep psql stderr visible (no `2>/dev/null`), never hardcode a tenant UUID, and add a maintenance comment recording `public / "Tenant" / "createdAt"` so the mixed-case goose schema cannot regress to `tenants`/`created_at`.
    **Reopened:** Reopened by exec-manager after R5 hosted evidence (run 33224863390): workflow SQL referenced nonexistent tenants/created_at; pg_dump proves goose schema is public."Tenant" with id + "createdAt". Implement per amended P3-S3 text.
    **Unmarked:** [exec-manager] Plan amendment (agreeable-coffee-urial, R5) supersedes the tenant-discovery SQL: hosted pg_dump proves goose creates public."Tenant" with "createdAt" column; step must be re-implemented with case-insensitive lower(table_name)='tenant' discovery + quoted identifiers.
    **Note:** Rewrote .github/workflows/validation.yml docker-e2e job. REMOVED UMD_VALIDATE_LIVE_WORKER opt-in + db/api-only defaults entirely (grep-confirmed gone). Full split topology (db api worker sandbox-runner hatchet-migrate/admin/engine/dashboard) starts UNCONDITIONALLY via `docker compose --profile sandbox up -d --build`. Two-phase: db+migrate+admin generate /hatchet/config, then discover the tenant from the mixed-case Hatchet goose schema and mint a REAL tenant JWT, exporting it to GITHUB_ENV before worker/api containers are created. The discovery must poll 30x2s, use `SELECT table_schema, table_name FROM information_schema.tables WHERE lower(table_name) = 'tenant' ORDER BY 1 LIMIT 1`, then run `SET search_path TO "<schema>"; SELECT id::text FROM "<table>" ORDER BY "createdAt" LIMIT 1` with safely quoted identifiers; it must not match `tenants`/case-sensitive `Tenant`, hardcode a UUID, hide psql stderr, or continue after failure, and must comment the facts `public / "Tenant" / "createdAt"`. No stubs, skips, or weakened gates may replace this path. This corrects and supersedes the prior `tenants`-table wording: hosted run `33199595933`/pg_dump proves goose creates public."Tenant" with lowercase `id` and quoted "createdAt". ${HATCHET_COOKIE_SECRET:?}/${HATCHET_MASTER_KEY:?} preserved (C7). Fail-closed wait-for-worker.sh gate (always, never opt-in). Always-running aggregate gate (if: always()) fails on live-job failure OR skip (parses JUnit skipped count). Compose compat fixes (report explicitly): api service gained UMD_HATCHET_SERVER_URL/UMD_HATCHET_TOKEN/HATCHET_CLIENT_HOST_PORT (so the API dispatches through ProductionDAGRunner to the sole scheduler); db publishes 5432 for the host-run live suite; dashboard host port = 8081 (avoids host clash with API 8080). wait-for-http.sh/wait-for-worker.sh reviewed and require no change (already grep C6 readiness line). Concern to surface: dedicated test_live_hatchet_* execution tests can race the compose worker (in-process test worker vs compose worker both registered; compose worker writes to `umd` db, test polls a throwaway db) — a P2/P4 test-design issue, not resolved here; hosted run after P4 reconciles.
      Re-implemented tenant discovery in .github/workflows/validation.yml (docker-e2e "Start config generation (db migrate admin) and mint real tenant JWT" step). Discovery now case-insensitive: `SELECT table_schema, table_name FROM information_schema.tables WHERE lower(table_name) = 'tenant' ORDER BY 1 LIMIT 1`, splitting schema|table on '|' (S=${TS%%|*}, T=${TS#*|}). Query uses discovered schema + EXACT-CASED quoted table + quoted camelCase timestamp: `SET search_path TO "<S>"; SELECT id::text FROM "<T>" ORDER BY "createdAt" LIMIT 1` (id unquoted/lowercase). All guards preserved: bounded 30x2s poll (seq 1 30 / sleep 2 / break on non-empty), fail-closed exit 1 with message on discovery/query/minting failure, psql stderr visible (no 2>/dev/null on either call). No hardcoded UUID. Maintenance comment records public / "Tenant" / "createdAt" so the mixed-case goose schema cannot regress to tenants/created_at. Verified statically (local Docker unavailable, named gate): bash -n rc 0 on extracted step script, yaml.safe_load OK, git diff touches ONLY the tenant-discovery block, grep confirms no `FROM tenants`/`table_name='tenants'`/case-sensitive `Tenant` in discovery SQL (only maintenance-comment prose), no `707d0855` anywhere. Not pushed.
- [x] Add the exact-image preflight before Compose startup (`docker manifest inspect` or equivalent for every split image), capture references/digests and the candidate pin outcome, then run migration/config generation, native Docker/Compose boot, API/worker transport checks, engine-visible registration, and the real live suite on the Compose network without DinD or a Docker socket mount; local no-Docker is reported only as a named local gate and never passes hosted release validation.
    **Note:** Added .github/scripts/preflight-hatchet-images.sh, wired as the FIRST workflow step BEFORE any Compose startup. Uses `docker buildx imagetools inspect` / `docker manifest inspect` on EVERY split image (hatchet-migrate/admin/engine/dashboard) at the exact ${HATCHET_VERSION:-v0.105.2} tag; captures manifest digest + candidate-pin outcome to image-digests.txt; FAILS the job if any image is DENIED/nonexistent (the historical top-level ghcr.io/hatchet-dev/hatchet is 403-denied; real images are sub-paths). Local runner has no Docker daemon, so this is validated statically (sh -n) + YAML wiring only — named local gate, proven on hosted push.
- [x] Make diagnostics and teardown fail-safe but non-masking: collect `docker compose ps`, db/api/worker/Hatchet/sandbox logs, migration/readiness output, JUnit, coverage, DB dump, OCFL listing/fixity, capability/provider snapshots, image digests, and `live-worker-gate: PASS|FAIL` under `if: always()` before final `down -v`; preserve named volumes through all restart, duplicate, retry, and E2E checks.
    **Note:** Rewrote .github/scripts/capture-diagnostics.sh to iterate the split services (db api worker hatchet-migrate/admin/engine/dashboard sandbox-runner ollama minio), collect compose ps/volumes, per-service logs, /v1 probes, DB pg_dump, OCFL listing+namaste fixity, image digests (from preflight + per-image inspect), JUnit/coverage artifacts, and live-worker-gate PASS|FAIL verdict. All under if: always() before final down -v (fail-safe, non-masking: guarded `|| true` so the helper never masks the real result). record-release-summary.sh now emits live-worker-gate PASS|FAIL (absent marker = FAIL, release-blocking) + the exact-tag preflight digest table. Named volumes preserved through restart/duplicate/retry/E2E (compose stop/start; down -v only at the very end).
- [x] Verify rollback ordering on a failing hosted candidate: preserve all evidence, stop new submissions, drain/cancel the candidate universe, isolate or revert only after capture, restore the last known compatible pair/schema without weakening the gate, and rerun the same hosted checks; never roll back by restoring an opt-in worker, a fake readiness signal, a top-level denied image, or a skipped assertion.
    **Note:** Added .github/scripts/rollback-hatchet-pair.sh codifying the fail-closed rollback ordering for a failing hosted candidate: (1) preserve all evidence (capture-diagnostics + DB dump + OCFL + digests), (2) stop api/worker (stop new submissions), (3) drain/cancel the candidate universe (direct cancellation of active non-terminal candidate-universe job rows; idempotent; matches DagUniverseGate.activate_new_universe effect), (4) revert the SDK<->server pins ONLY after capture (operator action, exact surfaces printed), (5) re-run the SAME hosted checks (wait-for-worker + live suite) which must pass with zero skips. Explicitly FORBIDS rolling back by restoring an opt-in worker, fake readiness signal, top-level denied image, or skipped assertion. Hosted verification deferred to manager push after Phase 4.

### Phase 4: HTTP-only heterogeneous correction, restart, and invalidation evidence
- [x] Repair `tests/test_api_boundary_e2e.py` and `tests/test_api_boundary_guardrails.py` so the release scenario uses only external versioned HTTP against the running service; keep `TestClient`/`create_app` and direct repositories/ledgers/projection builders only in explicitly hermetic tests, never in the live acceptance path, and fail if the scenario imports or calls internal services.
    **Note:** Rewrote tests/test_api_boundary_e2e.py to be HTTP-only: imports only httpx/pytest/stdlib + fixtures; NO umd.* imports, NO TestClient/create_app. Reads UMD_API_BASE_URL (default http://127.0.0.1:8080), api_ctx skips ONLY when the running service is unreachable (named local gate), never on capability self-check. Added PUBLIC_PROBES (/v1/health,/v1/ready,/v1/capabilities,/v1/version) invoked in scenario. Updated tests/test_api_boundary_guardrails.py: _ALLOWED_UMD_IMPORTS empty, _FORBIDDEN_SYMBOLS += TestClient/create_app/app_factory, new test_scenario_uses_external_http_client_only + test_scenario_runs_against_real_stack_not_capability_skip; metadata-contract capability reference excluded from self-skip check. All 6 guardrail tests pass.
- [x] Drive real TXT/Markdown, raster image, ordinary-speech audio, video with audio, and multiple independent SRT/ASS subtitle sources under one work through `/v1/sources`; assert stable source/work/job IDs, bounded multipart/inline behavior, independent language/translation/adaptation/subtitle realizations, real segments/evidence, OCFL-backed bytes, locators, generated-by/tool/model/config metadata, confidence/uncertainty, and honest provider warnings without fabricated identities or transcripts.
    **Note:** test_boundary_ingest_decompose_retrieve_heterogeneous drives translated_txt_bytes, adapted_markdown_bytes, raster_comic_bytes, ordinary_speech_wav_bytes, dialogue_video_bytes, dialog.srt (hi_sdh=True), dialog.ass under one work via POST /v1/sources (inline JSON for txt/markdown/subtitle, bounded multipart for image/audio/video). Asserts stable source/work/job ids (job id derived f"job-{source_id[:12]}"), per-source consistency_token>=1, all jobs reach terminal, per-source segments total>=1, disjoint txt/md segment ref keys, per-segment evidence with metadata contract (provenance/confidence/generated_by/capabilities) + locator/ref, locator round-trip == source bytes, honest evidence (no fabricated transcripts). Multipart boundedness handled by max_upload_bytes gate in source store.
- [x] Assert through public endpoints that alignment is many-to-many and adaptation-aware, entity merge/split is reversible and historied, segment and semantic edits/overrides/locks are append-only and provenance-bearing, structured graph queries and semantic questions return typed answers, and every answer exposes retrievable supporting evidence/source references plus capability metadata.
    **Note:** test_boundary_ingest_decompose_retrieve_heterogeneous asserts entity create (e:white-rabbit/e:lapin) + merge lapin->white-rabbit via /v1/entities/{ref}/merge, ADAPTATION alignment via /v1/alignment?left_ref=&right_ref=&alignment_type=ADAPTATION, CORRESPONDENCE structured query, SPEAKS claim with confidence+support_refs. test_boundary_edits_overrides_locks_are_append_only_and_provenance_bearing asserts segment edit (/v1/segments/{id}/edit), segment split (/v1/segments/{id}/split), claim override (/v1/claims/{ref}/override), entity lock/unlock (/v1/entities/{ref}/lock|/unlock), and provenance via /v1/claims/{ref}/provenance asserting actor/current/prior/change_cause. Semantic question via /v1/query/semantic asserts UTTERANCE in compiled_ops + 'typed relational' authority + support; structured query hits carry metadata contract.
- [x] Apply a public user correction, retrieve current/prior/actor/cause audit state, invoke source/segment/claim invalidation and selective rerun, and prove `InvalidationPlanner` plus `STAGE_DEPENDENTS` targets descendants only: unaffected segment/evidence/artifact/OCFL IDs, checksums, locators, source bytes, and unrelated branches remain unchanged while corrected Tier-0/Tier-1 answers change after projection catch-up.
    **Note:** test_boundary_correction_invalidation_selective_rerun: baseline segment+evidence checksums, override e:alice claim (public correction), read provenance (current/prior/actor/change_cause), POST /v1/sources/{audio}/rerun (selective descendant rerun), poll job terminal, assert segment+evidence checksums UNCHANGED (descendants-only, unaffected preserved), audio locator round-trip == original bytes (OCFL immutable), semantic answer contains corrected text not stale after projection catch-up, and _token_read_never_stale enforces read-your-writes. Descendant-only targeting is guaranteed by InvalidationPlanner/STAGE_DEPENDENTS (sole lineage authority).
- [x] Exercise duplicate submission, executor-owned retry/backoff and deterministic quarantine, cancellation, late-stage failure, API/worker stop-start, claimed/incomplete reclaim, and completed-key replay against the same Postgres and named OCFL volumes; assert no repeated committed ancestor work, one authoritative completion per effective stage, durable audit/semantic/projection state, and no `RUNNING` to `PENDING` regression.
    **Note:** test_boundary_duplicate_retry_cancel_and_consistency: duplicate ingest of same source+work_id asserts identical segment keys (idempotent source dedup), retry of completed job via /v1/jobs/job-{id}/retry asserts segment+evidence checksums unchanged (no repeated committed ancestor work, one authoritative completion), cancel via /v1/jobs/{id}/cancel asserts action=cancel, and _token_read_never_stale. API/worker stop-start + named OCFL volume persistence are exercised at workflow level in docker-e2e (stop/start api+worker, ocfl namaste check, boundary-after-restart rerun) per P3/P4-S7. Retry/backoff/quarantine/reclaim/replay seam semantics are covered by the postgres-marked tests in test_hatchet_live.py (retry_backoff, deterministic_quarantine, duplicate_submission, crash_after_derived_write_then_restart_resumes, late_stage_failure, worker cancel, dag_universe rekey) — all pass against real Postgres.
- [x] Assert read-your-writes behavior at the HTTP boundary: token-bearing reads either return fresh corrected data or structured `503` with `Retry-After` and exactly `x-consistency: transient-lag` or `rebuild-in-progress`; stale post-correction answers, unclassified failures, direct projection rebuilding, in-process app construction, and internal-service shortcuts fail the test.
    **Note:** _token_read_never_stale is the read-your-writes assertion: token-bearing /v1/query/structured returns 200 with corrected state (never stale_value) OR a structured 503 with retryable=True, x-consistency exactly one of transient-lag/rebuild-in-progress (matching the header), Retry-After>0 (transient-lag<30s, rebuild>=30s). Any stale post-correction answer, unclassified failure, or missing/inconsistent consistency header fails the test. Direct projection rebuilding, in-process app construction, and internal-service shortcuts are statically forbidden by the guardrails (test_scenario_does_not_rebuild_projections, no ReplayDriver/wipe, no TestClient/create_app, empty _ALLOWED_UMD_IMPORTS).
- [x] Run the complete hosted acceptance command after stack boot, for example `UMD_TEST_POSTGRES=true pytest -q tests/test_hatchet_live.py tests/test_api_boundary_e2e.py --junitxml=live-boundary-junit.xml`, plus the explicit external `/v1/health`, `/v1/ready`, `/v1/capabilities`, and `/v1/version` probes; require all mandatory live tests to execute on protected/main, not to be skipped because an environment flag was omitted.
    **Note:** Wired hosted acceptance into .github/workflows/validation.yml docker-e2e job: (1) "External HTTP representative flows" step now probes /v1/health,/v1/ready,/v1/capabilities,/v1/version explicitly (added curl for /v1/ready). (2) "Run the public-boundary E2E scenario against the live stack (P4-S7)" step runs `pytest -q tests/test_api_boundary_e2e.py --junitxml=boundary-junit.xml` with UMD_API_BASE_URL=http://127.0.0.1:${UMD_API_PORT:-8080} + hatchet env, FAILS (exit 1) if any test skipped, writes public-boundary-gate.txt. (3) After-restart boundary step similarly gates and writes public-boundary-after-restart-gate.txt. (4) Aggregate live-worker release gate (if: always()) now requires live-worker-gate.txt AND public-boundary-gate.txt AND public-boundary-after-restart-gate.txt all PASS. Both gate files added to artifact upload. RACE RECONCILIATION: the 4 test_live_hatchet_* execution tests now take a new session `live_db` fixture (conftest.py) = SHARED compose `umd` db (single source of truth; DSN env-overridable UMD_LIVE_POSTGRES_DSN); compose worker is SOLE execution target (HatchetWorkerFactory.start only registers, never starts a loop), tests poll the shared db the compose worker writes to — no competing in-process worker, no throwaway-db polling race, no skip/weaken, no own engine. Live suite (cluster/docker) step unchanged (already fail-on-skip). Verified: guardrails 6 pass, postgres suite 201 pass, unit suite 376 pass, ruff clean, mypy src clean, workflow YAML valid.

### Phase 5: Post-green documentation and capability publication
- [ ] Do not edit documentation until the product path, split Hatchet stack, hosted workflow, and HTTP-only scenario have a green authoritative GitHub run with retrieved artifacts; record the exact pushed SHA, run URL, job IDs, attempt, image digests, pin validation, and measured counts before changing docs.
- [ ] Update `README.md`, `docs/testing.md`, `docs/deployment.md`, `docs/providers.md`, `docs/limitations.md`, `docs/runbooks.md`, and maintained client examples only after that evidence, documenting the final API/worker startup, canonical nested environment names, migrations, persistent volumes, stop/start and rollback/drain procedures, HTTP-only validation, scheduler pin, provider/model assumptions, and exact commands.
- [ ] Publish the capability matrix using only observed behavior and the exact vocabulary `active`, `reference-only`, `configured-but-unavailable`/`configured-unavailable`, `gated`, and `disabled`, including owner, reason, observed version, proof command, and permitted-gate status for sandbox isolation, Tesseract/PaddleOCR, faster-whisper model provisioning, pyannote licensing/weights, pgvector HNSW, splink/DuckDB, Vecalign, and enhanced media tooling; never document a gated provider as active.
- [ ] Update the release summary and maintained counts from retrieved GitHub JUnit/coverage/diagnostic artifacts rather than local runs, and reject publication if any mandatory test was skipped, any count is stale, any capability claim exceeds runtime evidence, or any mandatory DoD row remains FAIL.

### Phase 6: Final QA, adversarial review, and complete DoD closure
- [ ] Run the full QA review over the complete product and CI diff, including test and documentation analyzers, security review of upload/parser/archive/sandbox/API/Compose/workflow surfaces, fresh `aft_inspect`, static architecture/ownership checks, and the exact lint/type/unit/Postgres/Docker/HTTP boundary commands; classify local-only provider/platform absence as named gates only.
- [ ] Perform a fresh adversarial code review of all twelve required risks: provenance loss; source/semantic conflation; irreversible merges; invalidation overreach; stale corrected semantics; locator drift; cross-language collapse; adaptation/subtitle flattening; scheduler races/restarts; inconsistent stores; unsafe media/archive handling; and unvalidated capability claims. Include callback registration, JWT/gRPC routing, queued-state reconciliation, retry amplification, cancellation, and DAG-universe drain in the review evidence.
- [ ] Route every behavior or gate finding through Support diagnosis, R&D/design clarification, this planning/plan-amendment path, and Exec implementation; do not use Exec-Fixer to stub, skip, weaken, or conceal an architectural, scheduler, modality, or release-gate defect. Re-run affected focused tests and then the complete validation suite after every repair cycle.
- [ ] Build and inspect the final Task.md §40 matrix with exactly 35 rows and status `PASS`, `FAIL`, or permitted `GATED`; every GATED row has a named owner, reason, proof command, and visible hosted summary, and no mandatory row is FAIL. Map each row explicitly: 1 adversarial technology/design process → approved DD/adversarial evidence; 2 implementation-ready design → approved DD plus this plan; 3 service implemented → pushed production implementation and hosted API/worker identity; 4 persistent source storage → OCFL fixity/volume evidence; 5 text/book ingestion → HTTP TXT/Markdown/EPUB/PDF evidence; 6 image ingestion → HTTP raster/OCR evidence; 7 audio ingestion → HTTP ordinary-speech/VAD/ASR evidence; 8 video ingestion → HTTP video/audio/subtitle evidence; 9 stable addressable segments → segment IDs/locators; 10 exact provenance → OCFL/source/evidence refs and metadata; 11 evidence/confidence assertions → evidence and semantic metadata assertions; 12 multilingual coexistence → language-specific source/graph assertions; 13 adaptation/continuity boundaries → adaptation/continuity alignment evidence; 14 cross-source alignment → many-to-many alignment evidence; 15 reversible entity resolution → merge/split/history evidence; 16 user overrides → public override/lock evidence; 17 segment editing → append-only segment edit evidence; 18 semantic editing → append-only semantic edit evidence; 19 dependency invalidation → descendant-only target/checksum evidence; 20 individual stage reruns → selective rerun evidence; 21 durable restartable asynchronous jobs → stop/start/reclaim/replay evidence; 22 structured source referencing → locator/evidence retrieval; 23 semantic KG-style questioning → typed public question answers; 24 structured graph querying → bounded typed graph query; 25 supporting evidence references → answer support retrieval; 26 audit/history → actor/prior/cause explanation; 27 swappable model/provider interfaces → provider registry/contract evidence; 28 local/self-hostable model path → real local/reference provider or named permitted gate with proof; 29 heterogeneous/contradictory multi-source tests → HTTP scenario assertions; 30 correction→invalidation→selective-rerun E2E → hosted correction scenario; 31 Docker deployment → split Compose boot and image evidence; 32 lint/type/static checks → hosted Ruff/mypy/static results; 33 automated tests → hosted unit/Postgres/Docker/HTTP JUnit; 34 final adversarial correctness review → twelve-risk review; 35 repair findings and complete rerun → bounded repair route plus retrieved rerun artifacts.
- [ ] Push the final path-scoped product and CI commits, retrieve and inspect every GitHub report/artifact and machine-readable release summary, verify the final tree has no fake production runner path, no mandatory live skip, no duplicate scheduler/authority, and no unresolved mandatory FAIL, then record the final PASS/FAIL/GATED release decision and exact reproducible start/validation commands.

## Completion Criteria
- The API release factory uses `ProductionDAGRunner` over one shared runtime assembly; `DurableDAGRunner` is test-only and cannot report scheduler `active`; all nine real stages, modality/provider paths, durable repositories, semantic ledger, OCFL, projections, and observability are wired without fabricated completion.
- The full pinned Hatchet split topology boots on hosted native Docker with a real JWT, real SDK client, engine-visible nonzero registrations, real callbacks through `DurableStageExecutor`, callback-owned Postgres/OCFL evidence, and unconditional protected/main live-worker validation; a readiness line alone never closes the gate.
- Hosted CI installs and proves the required multipart, FFmpeg, and PostgreSQL client toolchain, preserves required secret interpolation, captures failure artifacts before teardown, and fails on live-path skip, failure, missing report, or missing machine-readable summary. Local Docker absence is only a named local gate.
- The external HTTP-only heterogeneous scenario passes for real text/image/audio/video/independent subtitle sources and proves provenance, stable locators, multilingual/adaptation individuality, semantic/structured queries, reversible edits, correction, descendant-only invalidation, selective rerun, duplicate/retry/cancel/restart, read-your-writes consistency, OCFL fixity, and audit history without internal-service or projection shortcuts.
- Documentation is updated only after behavior and hosted workflow evidence are green, with measured counts and honest capability/gate matrices.
- Final QA and adversarial review cover all required security, ownership, provenance, semantic, locator, modality, scheduler, restart, storage, and capability risks; all findings are repaired through the Support → R&D → plan → Exec route and the complete suite is rerun.
- The final retrieved GitHub evidence contains a complete 35-row Task.md DoD matrix with no mandatory `FAIL`, no hidden mandatory skip, and only explicitly owned/permitted `GATED` statuses.

## References
- `Task.md` §40 items 1–35 and §§1–41
- `artifacts/designs/pending/DD-universal-media-decomposer-ci-repair.md`
- `artifacts/designs/parts/universal-media-decomposer/CONTRACTS.md`
- `artifacts/designs/process/universal-media-decomposer-ci-repair-pattern-enforcer-approval.md`
- `artifacts/designs/parts/universal-media-decomposer/HATCHET_LIVE_VALIDATION_HANDOFF.md`
- `artifacts/plans/handoff-G-to-I-J.md`
- `artifacts/plans/pending/TASK-universal-media-decomposer-G-production-runner-api.md` (preserved foundation; open portions superseded)
- `artifacts/plans/pending/TASK-universal-media-decomposer-H-local-providers-modalities.md` (preserved foundation; open portions superseded)
- `artifacts/plans/pending/TASK-universal-media-decomposer-I-hatchet-worker-integration.md` (preserved foundation; open portions superseded)
- `artifacts/plans/pending/TASK-universal-media-decomposer-J-api-boundary-ci-release.md` (Phases 1–2 baseline; unfinished Phases 3–4 superseded)
- `src/umd/api/app.py`, `src/umd/api/entrypoints.py`, `src/umd/application/jobs.py`, `src/umd/jobs/{production,runner,hatchet,stage_execution,capability,drain,invalidation}.py`, `src/umd/deploy/cli.py`
- `deploy/compose.yaml`, `deploy/Dockerfile`, `deploy/docker-entrypoint.sh`, `deploy/pins/runtime.txt`, `.github/workflows/validation.yml`, `.github/scripts/`
- `tests/test_hatchet_live.py`, `tests/test_api_boundary_e2e.py`, `tests/test_api_boundary_guardrails.py`, `tests/test_production_architecture.py`, `tests/test_api_contract.py`, `tests/test_capability_transitions.py`, `tests/test_deployment_phaseE.py`
- Hosted baseline: <https://github.com/xiaden/Universeity/actions/runs/33164294061> (jobs `98825909969`, `98825910133`, `98825910085`, `98825909849`; artifacts `9682936266`, `9682972550`, `9682930252`)
