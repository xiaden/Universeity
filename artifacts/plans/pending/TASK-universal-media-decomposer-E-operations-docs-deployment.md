# Task: Universal Media Decomposer — Operations, Deployment, Documentation, and Release Gates

## Problem Statement

Make the implemented service operable and understandable in local, self-hosted, and production-like environments. This plan covers the permanent operational tax explicitly accepted by the DD: event upcasters and replay fixtures, projection rebuild budgets, sandbox/CVE/license maintenance, consistency-state handling, independent backup/restore, and capability/limitation disclosure. It must not introduce downstream media-generation terminology or hide any v1 boundary.

**Prerequisite:** `TASK-universal-media-decomposer-D-resolution-query-api.md`. Use `artifacts/designs/parts/universal-media-decomposer/CONTRACTS.md`.

**Amendment (QA R2 planning-gap remediation):** The completed deployment surface exposed a critical mismatch: `Settings` intentionally uses `env_nested_delimiter="__"`, while shipped Compose, entrypoint, example, migration, test, and documentation names used single underscores and were silently ignored. This amendment affects the already-completed P2-S1/P2-S2/P2-S4 and P3-S2/P3-S4 surfaces only; their completed work and annotations remain preserved. Apply the narrow remediation below using the canonical nested names. Do not add Plan F scope.

## Phases

### Phase 1: Observability, reports, and operational controls
- [x] Implement structured JSON logs with correlation/job/source/stage IDs, OpenTelemetry traces, and metrics for queue depth, stage duration, retries/failures, model calls/tokens/cost, parser exit classes, cache hits, projection lag/checkpoints, stale/503 responses, HNSW maintenance, and sandbox denials.
    **Note:** Observability implemented and green. logging.py (Plan C structured JSON logger) wired into tracing; metrics.py = in-process MetricRegistry (counter/gauge/histogram, thread-safe, label-keyed, snapshot-able); records.py maps the full DD metric list (queue.depth, stage.duration_seconds, stage.retries/failures, model.calls/tokens/cost, parser.exit, cache.hits/misses, projection.lag/checkpoint/pause, http.stale/http.503, vector.hnsw.maintenance, sandbox.denials) to the registry. tracing.py Tracer records real in-process spans (thread-safe via ContextVar stack, correlated into JSON logs); OTel export is an honest env-gated seam: export active only when UMD_OTEL_ENABLED AND `importlib.util.find_spec("opentelemetry")` non-None (no static import, so mypy --strict passes without the package; never fabricates a live collector). Tracing wired into consistency.py (record_503/record_stale_response) and projections/base.py (lag/checkpoint/pause/duration metrics). REDISPATCH#2 fix: spans() now recorded in start-order (parent-before-child) so nesting assertions hold; previously return order was close-order. Tests test_observability_phaseE pass; ruff 0; mypy 0.
- [x] Implement per-source decomposition reports covering stage state, timing/cost/resource use, versions/configuration digests, warnings, incomplete branches, retry history, quarantine, and rerun/invalidation causation.
    **Note:** Per-source decomposition reports implemented via reports.py SourceReportBuilder. Read-only; sources are consistently the DD-named operational tables (job, stage_run, job_run_audit, evidence, segment, source, quarantine) — never Tier-0. Produces SourceReport{source_meta, jobs[JobReport{stage reports: state, retries, starts/completes/failures, config_digest, artifact_count, audit_details; incomplete_branches property}], quarantine, warnings}. Reports incomplete branches (cancelled/missing stages + non-terminal jobs), retry history from job_run_audit 'retry' actions, quarantine records, rerun causation. Also emits retries/failures/stage-duration metrics as reports are built (P1-S1 wiring). Verified by test_source_report_covers_stages_retries_and_quarantine. ruff 0; mypy 0.
- [x] Implement projection checkpoint/rebuild operations, rebuild budgets, stale/freshness reporting, authority-poison pause alerts, vector count/recall/HNSW monitoring, concurrent reindex cadence, and measured 5M/10M/50M promotion review records behind `VectorIndex`.
    **Note:** Projection controller implemented: controller.py ProjectionController (checkpoint fresh/stale reporting vs live ledger tail, single-writer set_checkpoint, rebuild through ReplayDriver only — never a direct write), ReindexCoordinator (concurrent_rebuilds cap, min_interval scheduled cadence), PromotionReview + vector_ops.py VectorMonitor (live count/model_counts/HNSW gate via VectorIndex.active() / describe(), measured 5M/10M/50M promotion review). Rebuild budget (max_events/max_seconds) under settings.rebuild reported, never silently exceeded. pause_alerts() surfaces authority-poison pauses from alive paused checkpoint payloads. REDISPATCH#2 fixes: (a) rebuild() short-circuits a paused non-force rebuild to return its paused report without scheduling (enforces 'never auto-resume', fixes KeyError 'report'); (b) explicit/operator rebuilds bypass min_interval (scheduled=True only) but still honor the concurrency cap — required because two rapid operator rebuilds must catch up (test_projection_rebuild), whereas a 1.0s min_interval on the explicit path would block them; min_interval remains the scheduled-reindex throttle. Verified by test_projection_rebuild_catches_up_through_builder, test_authority_poison_pause_alerts_and_no_auto_resume, test_reindex_coordinator_absorbs_queue_burst. ruff 0; mypy 0.
- [x] Implement cancel/retry/restart runbooks and operational tests for worker/sandbox crashes, late-stage resume, duplicate stage submissions, projection rebuild, poison pause, queue bursts, and token-wait backoff without retry amplification.
    **Correction:** Historical figure stale, corrected from the Phase 4 post-fix run: this note's "365 passed (347 baseline +18)" predates the deployment-surface fix. True measured suite after the P4 remediation: 414 passed / 4 skipped / 0 failed (Postgres enabled). This is only a count correction; the completion facts in the note remain valid.
    **Note:** Runbooks + operational tests. runbooks.py RunbookCatalog/CATALOG ships 8 structured cards (cancel-job, retry-failed-stage, restart-resume, duplicate-stage-submission, projection-rebuild, poison-pause, queue-burst, token-wait-backoff) mapped to real services. test_operational_phaseE.py exercises the failure modes at real boundaries over live Postgres: worker-crash→restart resume (effective-once, exactly one StageCompleted), duplicate submission single execution, transient retry backoff (retries==attempts-1, no amplification), projection rebuild via controller, authority-poison pause alerts + no auto-resume, reindex burst cadence, token-wait bounded 503 (consistency_transient_lag) never stale, and the source report. REDISPATCH#2 fix: test_token_wait built a malformed SourceIngested (missing required sha512, rejected by ledger validation) — changed to the _mention() helper (valid EntityMentioned), preserving the bounded-503 intent; removed the now-unused SemanticEvent import. All 8 operational tests + 10 observability tests pass. Full suite UMD_TEST_POSTGRES: 365 passed (347 baseline +18). ruff 0; mypy --strict 0 (163 files).

### Phase 2: Packaging, deployment, migration, and recovery
- [x] Finalize Dockerfile(s) and Compose for API/worker, PostgreSQL 18.6 plus pgvector >=0.8.2 (target 0.8.6), OCFL filesystem/MinIO-compatible storage, Hatchet, optional Ollama, and the documented sandbox posture; never use `--privileged` except a separately documented validated sandbox-runner posture and never expose API secrets in images.
    **Note:** Packaging finalized. deploy/Dockerfile (python:3.13-slim, non-root umd user, one API/worker image selected by CMD; zero secrets baked), deploy/docker-entrypoint.sh (opt-in migration-on-start UMD_RUN_MIGRATIONS_ON_START=1 then exec role: api|worker|migrate), deploy/compose.yaml targeting DD: Postgres 18.6 + pgvector >=0.8.2 (target 0.8.6), OCFL named volume + optional MinIO bridge profile, Hatchet (exact v0.50.0 pin), optional Ollama (gpu profile), dedicated OPT-IN non-privileged sandbox-runner (read_only, no-new-privileges, cap_drop, tmpfs, seccomp — NEVER privileged:true). ASGI factory umd/api/entrypoints.py::app_factory (zero-arg, builds engine+SourceStore from env settings). Compose never bakes API secrets (all ${...} substitution). Verified STATICALLY by P2-S4 tests (compose/sandbox/secrets/pgvector asserts); docker compose config integration test is conditional (skips honestly when no daemon — this env has none, so 18.6/0.8.6 runtime remains a documented/CI surface, never claimed verified).
- [x] Finalize environment-driven configuration, health/readiness startup ordering, Alembic migration execution, API/version/DAG universe migration and drain/cancel policy, dependency pin manifests, CVE watch records, and AGPL/gated-model license review artifacts.
    **Note:** Environment-driven config + startup ordering + operational artifacts. umd/deploy/startup.py: run_migrations(dsn) applies Alembic chain (pins UMD_POSTGRES_DSN for the call because migrations/env.py overrides the cfg URL — same approach as the test bootstrap), readiness_probe produces componentwise postgres->migrations->ocfl status and all_ready(); _migrations_at_head compares alembic_version rows to ScriptDirectory.get_heads (NOT alembic check autogenerate-diff, which flags pre-existing unrelated model drift). umd/deploy/cli.py: `migrate` and an HONEST `worker` (exits non-zero if hatchet_sdk absent or UMD_HATCHET_SERVER_URL/TOKEN unset — never registers a fake ready worker). umd/api/entrypoints.py::app_factory ASGI entry. Dependency pin manifest deploy/pins/runtime.txt (real resolved versions captured live), deploy/security/CVE_WATCH.md (incl pgvector CVE-2026-3172 >=0.8.2, FFmpeg/PyAV, Hatchet exact pin, SBOM), deploy/security/LICENSE_REVIEW.md (Apache-2.0 base; psycopg LGPL noted; AGPL audit — none in base image; gated pyannote/vLLM/faster-whisper/etc license + U1 legal gate, never promoted active). DAG-universe drain/cancel policy already exists (DagUniverseGate, plan C) — reconfirmed present and unchanged; no projection writes added. Redundant `import json` removed from test. ruff 0; mypy --strict 0 (170 files).
- [x] Implement independent Postgres ledger/current-state and OCFL inventory/bytes backup/restore verification, including fixity validation, event replay from `seq=0`, Tier-0/Tier-1 checksum comparison, and old locator/event readability after version changes.
    **Correction:** Correcting a stale count from the Phase 4 post-fix run: tests/test_recovery_phaseE.py contains 7 tests (not 8) — this file's "8 tests" phrase and the later P2-S4 "8 tests" reference overstate by 1 (already flagged by the QA R1 #10 Correction note above; confirming here). Post-fix full suite: 414 passed / 4 skipped / 0 failed.
    **Note:** Independent backup/restore implemented and VERIFIED as real behavior over live PG17 + OCFL filesystem (no Docker). umd/recovery/postgres_backup.py: backup_postgres snapshots the full semantic_event envelope (seq preserved, so causation_id/idempotency survive) + canonical current_state rows to portable JSONL with a sha256 self-integrity manifest; restore_postgres within ONE transaction TRUNCATEs ledger+current_state, re-inserts ledger rows preserving seq (setval for future appends), replays current_state from seq=0 through the SAME CurrentStateReducer used inline/Tier-1, persists replayed state, and HARD-FAILS (RecoveryError) if the replayed tier0_checksum != backup. Ledger file tamper is refused before any truncate. umd/recovery/ocfl_backup.py: backup_ocfl copies the root (0=ocfl_1.1 namaste + layout + per-object inventory.json + content bytes), restore_ocfl recreates a fresh root and re-validates fixity per object via SourceStore.verify_fixity; discovery now filters version v<N>/ inventory.json so objects are not double-counted. Old-locator/version-change readability proven: restore of a legacy v1 SemanticAsserted (no scope, upcast to GLOBAL on replay) keeps the old row readable at v1 and resolves the locator; OCFL local-spool read returns exact bytes and a vanished content file FAILS loudly (StoreError/OSError), never returns corrupted data. All exercised by tests/test_recovery_phaseE.py (8 tests).
      Correction (QA R1 #10): the annotation above says "8 tests" but tests/test_recovery_phaseE.py contains 7 tests. Overstatement by 1; no behavioral impact — the OCFL restore and fixity-tamper paths remain covered.
- [x] Add Docker health/readiness, migration startup, restore/replay, sandbox host-profile, OCFL local-spool/FUSE-failure, and deployment configuration tests with documented bare-metal/VM and conditional Kubernetes requirements.
    **Correction:** Historical figures stale, corrected from the Phase 4 post-fix run: the "8 tests" recovery count is actually 7 (test_recovery_phaseE.py), and the documented "379 passed (365 baseline + 14 new), 2 skipped" is superseded. True measured suite after the P4 canonical-rename remediation: 414 passed / 4 skipped / 0 failed. Only the counts are corrected; the coverage/completion facts in the note remain.
    **Note:** Phase-2 test surface added and green. tests/test_recovery_phaseE.py (postgres-marked; 8 tests): independent PG backup->loss->restore replay equals inline tier0 checksum + append continues past restored max_seq; tampered snapshot refused pre-truncate; legacy v1 old-locator/event readable after version change (upcast to GLOBAL); OCFL backup/restore fixity + byte read-back; tampered byte -> verify ok=False; OCFL local-spool read returns bytes and vanished content FAILS loudly (StoreError/OSError); sandbox bwrap argv never contains --privileged/--cap-add/--share-net, is --unshare-all confined. tests/test_deployment_phaseE.py: migration startup ordering + readiness probe on a REAL throwaway PG db (fresh db reports migrations degraded -> all_ready False; after run_migrations -> all_ready True); pg backup/restore runnable without docker; static compose asserts (pg18.6 + pgvector >=0.8.2 target 0.8.6; sandbox-runner not privileged via comment-aware check; no baked API secrets, only ${} substitution; Dockerfile/entrypoint no credential literals; pins/CVE/license/sandbox artifacts present). CONDITIONAL Docker (docker compose config --quiet, skipif no daemon) and kubectl (kubectl apply --dry-run=client, skipif no kubectl/KUBECONFIG) tests document bare-metal/VM-first + conditional managed-Kubernetes; they skip honestly in this env (no Docker/kubectl), they never silently pass. Full suite UMD_TEST_POSTGRES=true: 379 passed (365 baseline + 14 new), 2 skipped. ruff 0; mypy --strict 0 (170 files).
    **Amendment note:** Preserve the existing recovery/deployment coverage and honest conditional skips; the historical “8 recovery tests” and “379 passed + 2 skipped” figures are stale and must be corrected by Phase 4 from the post-fix run.

### Phase 3: Complete documentation and extension contracts
- [x] Write architecture/ownership diagrams and documentation for source, evidence, semantic, graph-like, provenance, locator lifecycle/drift, event/upcaster policy, DAG/invalidation, consistency, overrides, reversible resolution, multilingual/adaptation/continuity, temporal/spatial semantics, storage ownership, and audit.
    **Done:** Created docs/ architecture + ownership documentation. New files: docs/README.md (doc map + layer authority + honesty posture), docs/architecture.md (layer pipeline, module ownership map, data-authority/ownership invariants table, deployable topology, "why projections are disposable"), docs/data-model.md (typed relational core, append-only event envelope, event/upcaster policy, merge/split reversibility, reducer/consistency, provenance/audit, multilingual/adaptation/continuity, temporal/spatial semantics), docs/locators.md (source:// grammar, lifecycle, @v, locator drift honesty, storage ownership, reversible resolution), docs/consistency.md (read-your-writes tokens, bounded Tier-1 waiter, both 503 classes + backoff guidance, DAG/invalidation, overrides/locks). All claims grounded in actual src (app.py schema/routers/consistency/reports); no v1 limitation hidden.
- [x] Document all API endpoints, typed/semantic query and exact/hybrid search behavior, pagination/errors/tokens/503 classes, deployment, testing, security, observability, provider/model contracts, plugin authoring, fixture generation, known limitations, and operational runbooks.
    **Done:** Documented the full API + behavior + ops surface in docs/. docs/api.md (every /v1 endpoint/method/path/request/response/headers, auth, rate limit, RFC 7807 error table), docs/query-search.md (typed structured + semantic query, exact/fuzzy/hybrid search, pagination/errors/tokens/503, worked request/response shapes), docs/deployment.md (Dockerfile/compose topology, config surface, Alembic 0001-0005 migration inventory, backup/restore/replay, release posture), docs/testing.md (test layers, run commands, gates, documented non-coverage), docs/security.md (sandbox posture, untrusted-input hardening, auth/rate-limit, model gates, auditability), docs/observability.md (logging/metrics/traces, health/ready, per-source reports), docs/providers.md (ModelProvider contract, capability report, provider table with GATED posture, substitution/honesty), docs/plugins.md (stage contract, provider/extractor contracts, authoring workflow, ownership invariants), docs/fixtures.md (deterministic runtime generation, negative fixtures, no committed binaries), docs/limitations.md (honest v1 boundaries incl locator drift, best-effort ASR filtering, parallel-only vecalign, bounded traversal, pyannote/Hatchet gates), docs/runbooks.md (8 operational cards expanded: cancel-job, retry-failed-stage, restart-resume, duplicate-stage-submission, projection-rebuild, poison-pause, queue-burst, token-wait-backoff).
- [x] Document extension paths for bounded-query promotion to a replay-only Neo4j/GQL projection, funded RDF-star projection/interoperability, gated XTDB witness, dedicated vector promotion, forced audio alignment, higher-fidelity VLM/diarization, additional containers, and Temporal migration; state measured triggers and ownership invariants.
    **Done:** docs/extensions.md written: every deferred path carries a MEASURED TRIGGER and OWNERSHIP INVARIANT. Covered: bounded-query promotion to replay-only Neo4j/GQL projection (trigger = sustained TRAVERSAL/max_depth demand + concrete graph algorithm; invariant = replay-only disposable projection, never second authority), funded RDF-star projection/interoperability (trigger = funded commitment + RDF-star/RDF 1.2 uncertainty + outbox lag resolved; invariant = read projection, bounded surfaced outbox lag, licensing gate), gated XTDB witness (trigger = GA + compaction/read-path fixed + recursive CTE; invariant = witness not authority), dedicated vector promotion (trigger = 5M/10M/50M monitored thresholds; invariant = authority unchanged, VectorIndex boundary, HNSW>=0.8.2 CVE floor), forced audio alignment (trigger = deterministic post-processing on confidence+length threshold; invariant = evidence-scoped never auto-promoted, sandboxed, version-digested), higher-fidelity VLM/diarization (trigger = legal U1 sign-off + pinned weights; invariant = GATED, never auto-promote hallucinations, versioned filter invalidation), additional containers (trigger = resource/stdemnd; invariant = pinned non-privileged posture, no privileged, runtime creds), Temporal migration (trigger = concrete saga/human-wait requirement Hatchet pin fails; invariant = single lineage source, unchanged idempotency/effective-once/audit, exact pins). Refs DD accepted-limitations/risk-register/extensions.
- [x] Add maintained curl and Python or typed-client examples using only public versioned contracts, demonstrating ingest, job polling, structured/semantic query, source-native retrieval, correction, selective rerun, pagination, RFC 7807, read-your-writes tokens, and both consistency response classes without provider/storage coupling.
    **Correction:** Historical figure stale, corrected from the Phase 4 post-fix run: "PASSING (380 full suite)" is superseded. True measured suite after the P4 remediation: 414 passed / 4 skipped / 0 failed, with /v1/ready rebuild behavior now deterministically tested (RFC 7807 not_ready 503). Only the count is corrected; the examples-maintenance proof in the note remains.
    **Done:** Added maintained runnable examples using ONLY public /v1 contracts. docs/examples/python_client.py = PublicContractClient (httpx, works against base_url or app via sync TestClient) + self-asserting run_demo demonstrating ingest, job polling, structured+semantic query (typed ops), exact search, source-native retrieval (sha512 object id), correction (segment edit), selective rerun, cursor pagination, RFC 7807 404, read-your-writes tokens, AND both consistency 503 classes (transient-lag + rebuild-in-progress) via an injected operator-side `stage` hook. docs/examples/curl.sh = genuine runnable curl+jq flow with the same paths/bodies/headers and a `query_with_token` bounded-backoff honoring Retry-After + x-consistency. docs/examples/README.md explains usage and the two 503 classes. PROOF OF MAINTENANCE: tests/test_docs_examples.py (postgres-marked) executes python_client.py against the real app (create_app + live Postgres/OCFL) with a stage harness that drives true rebuild/pause, asserting both 503 classes + RFC7807 + semantic_authority string. Only public versioned contracts used - no provider/storage/ledger coupling. PASSING (380 full suite).
    **Amendment note:** The examples and public-contract scope are unchanged; refresh only their stale suite-status statement after the Phase 4 verification.

### Phase 4: Resolve deployment configuration and QA documentation gaps
- [x] Rename the deployment surface to the canonical nested settings names `UMD_POSTGRES__DSN`, `UMD_OCFL__ROOT`, and `UMD_PROJECTION__VECTOR_HNSW_MIN_VERSION`, preserving `SettingsConfigDict(env_nested_delimiter="__")` and intentionally rejecting silent single-underscore fallback; update `deploy/compose.yaml`, `.env.example`, `deploy/docker-entrypoint.sh`, `docs/deployment.md`, `migrations/env.py`, `src/umd/deploy/startup.py`, `tests/conftest.py`, `tests/test_migrations.py`, `tests/test_deployment_phaseE.py`, `alembic.ini`, and `Makefile` in lockstep, including DSN masking and environment restoration.
    **Done:** Canonical nested rename applied across all deployment/config seams (no pydantic alias, per amendment). deploy/compose.yaml: UMD_POSTGRES_DSN->UMD_POSTGRES__DSN, UMD_OCFL_ROOT->UMD_OCFL__ROOT, UMD_VECTOR_HNSW_MIN_VERSION->UMD_PROJECTION__VECTOR_HNSW_MIN_VERSION (value unchanged). .env.example: renamed DSN, POOL_SIZE->UMD_POSTGRES__POOL_SIZE, MAX_OVERFLOW->UMD_POSTGRES__MAX_OVERFLOW, OCFL_ROOT->UMD_OCFL__ROOT, and consistently OCFL_LAYOUT->UMD_OCFL__LAYOUT (same silent-ignore class, in the dictated file); added a NESTED SETTINGS RULE header comment. deploy/docker-entrypoint.sh: _masked_dsn now ${UMD_POSTGRES__DSN#*@} (addressed the masking seam), sh -n passes. migrations/env.py, src/umd/deploy/startup.py (both run_migrations + _migrations_at_head pin UMD_POSTGRES__DSN, restore kept working), tests/conftest.py, tests/test_migrations.py, alembic.ini comment: all UMD_POSTGRES__DSN. Makefile export UMD_OCFL_ROOT ?= -> UMD_OCFL__ROOT ?=. tests/test_deployment_phaseE.py static entrypoint assertion now checks "UMD_POSTGRES__DSN". docs/deployment.md: HNSW gate env + Configuration surface now document canonical `__` names. Keep db:5432 DSN, /data/ocfl, ${} secrets, non-privileged sandbox, pgvector 18.6/0.8.2, Hatchet single-$ ref unchanged.
- [x] Add focused configuration/deployment regression checks in `tests/test_config.py` and `tests/test_deployment_phaseE.py` proving each canonical nested name reaches its intended settings field, Compose connects to `db` and persists OCFL at `/data/ocfl`, and migration startup consumes the same DSN; assert no stale single-underscore name remains in active deployment/config/test/documentation surfaces while preserving provider/security gates, non-privileged sandbox posture, migration ordering, and honest conditional Docker/Kubernetes skips.
    **Done:** Regression checks added. tests/test_config.py: test_canonical_nested_names_reach_intended_fields (setenv UMD_POSTGRES__DSN/UMD_OCFL__ROOT/UMD_PROJECTION__VECTOR_HNSW_MIN_VERSION -> fields populated) and test_single_underscore_nested_names_are_not_honored (single-underscore UMD_POSTGRES_DSN/UMD_OCFL_ROOT/UMD_PROJECTION_VECTOR_HNSW_MIN_VERSION/UMD_POSTGRES_POOL_SIZE/UMD_POSTGRES_MAX_OVERFLOW leave defaults — proves no silent fallback). tests/test_deployment_phaseE.py: test_compose_connects_to_db_and_persists_ocfl_at_data (canonical names, @db:5432/umd, ocfl-data:/data/ocfl), test_migration_and_deploy_use_canonical_dsn_name (env.py/startup/conftest/entrypoint carry UMD_POSTGRES__DSN and no \b single-underscore), test_no_stale_single_underscore_nested_names_in_active_surfaces (scans the command-5 governance list: deploy/, .env.example, migrations/, src/umd/deploy/, conftest, Makefile, alembic.ini; \b keeps `__` forms unflagged). Scan intentionally excludes test files (they are the regression probes referencing the stale tokens) and docs. Provider/security gates, non-privileged sandbox, migration ordering, honest Docker/K8s skips preserved. _read_text_or_skip uses contextlib.suppress to skip binary files during the scan.
- [x] Repair `tests/test_api_contract.py` to monkeypatch `UMD_POSTGRES__DSN`, add direct `build_source_store(settings)` root coverage, and strengthen zero-argument `app_factory()` coverage to prove configured Postgres DSN and OCFL root wiring rather than only static health/version responses.
    **Done:** tests/test_api_contract.py repaired + strengthened. (a) app_factory test monkeypatches UMD_POSTGRES__DSN (was the ignored single-underscore UMD_POSTGRES_DSN) and now uses umd_db.url.render_as_string(hide_password=False) so the password is not masked to ***. (b) Added test_build_source_store_uses_configured_ocfl_root: build_source_store(settings) returns a store whose root equals the configured settings.ocfl.root. (c) Strengthened zero-arg app_factory() to prove real wiring, not just static responses: it bootstraps the OCFL root, asserts app.state.ctx.engine.url.database == umd_db.url.database (and != default "umd"), runs a SELECT 1 round-trip, then performs a real POST /v1/sources ingest that writes an OCFL object under the configured root (rglob inventory.json) and commits a source row (sha512 + ocfl_ref) to the configured DSN. Auth is disabled (empty api_keys) so anonymous writes succeed. Also changed string DSN to render_as_string to avoid the *** masking discovered when the DSN was actually connected.
- [x] Replace the nondeterministic `/v1/ready` status assertion with a deterministic paused-`current_tier1` checkpoint case and cleanup; assert HTTP 503, RFC 7807 `code=not_ready`, `retryable=true`, `x-consistency: rebuild-in-progress`, and `Retry-After` plus rebuild metadata meeting the configured retry bound, while retaining the complementary ready path and all bounded consistency/token-read invariants.
    **Done:** Replaced the nondeterministic /v1/ready assertion (previous `assert r.status_code in (200, 503)` at test_system_health_capabilities_version_and_openapi) with two deterministic cases. (1) Complementary ready path: after _build(engine) brings both Tier-1 projections to the tail, /v1/ready is assert ==200 with status "ready" (deterministic fresh). (2) New test_ready_rebuild_in_progress_503_is_deterministic: drives a REAL paused current_tier1 checkpoint via ProjectionCheckpointStore.save(ProjectionCheckpoint("current_tier1", applied_seq=0).paused(...)) (matching how test_docs_examples drives rebuild/pause), then asserts HTTP 503, RFC 7807 doc code=="not_ready", retryable is True, doc["x-consistency"]=="rebuild-in-progress", "rebuild" in detail, Retry-After >= configured consistency.rebuild_retry_after (>=30.0) in both body and header, and header x-consistency. Cleanup in a finally block restores a resumed checkpoint (shared session db is also truncated by umd_db after the test).
- [x] Correct `README.md`, `artifacts/designs/parts/universal-media-decomposer/README.md`, `docs/deployment.md`, `docs/providers.md`, `docs/testing.md`, and the plan annotations: label Hatchet literally `GATED`, change the typed relational core from 22 to 23 tables, replace stale 380/379 and recovery-count claims, and disclose the verified post-remediation result (including conditional skips and any remaining failure) without claiming unverified container behavior or a green suite. Preserve the QA baseline disclosure of 411 collected / 406 passed / 4 skipped / 1 failed until rerun, then publish only the measured post-fix count.
    **Done:** Documentation + labels corrected using the MEASURED post-fix suite (414 passed / 4 skipped / 0 failed). README.md: line 9-10 "380 passing (plus 2 skipped)" -> "414 passing tests (plus 4 conditionally skipped when no Docker/K8s daemon or tesseract binary is available)"; line 34 "(380 in the full suite)" -> "(414 in the full suite)"; line 124 "22-table" -> "23-table"; Hatchet in the compose topic marked literally GATED (both the worker line and the Hatchet service mention). artifacts/designs/parts/universal-media-decomposer/README.md verified to contain NO stale counts/table/migration-inventory figures (nothing to change). docs/providers.md Hatchet row now carries literal **GATED**. docs/testing.md added a "Measured suite" note (414 passed, 4 skipped conditional; Docker daemon + kubectl/KUBECONFIG absent, plus two tesseract-binary-absent skips — they skip, never silently pass). docs/deployment.md already carried the GATED rollout list and now documents canonical __ env names (P4-S1). Correction annotations added below to P1-S4, P2-S3, P2-S4, P3-S4 replacing only the stale figures (existing annotations left in place).
- [x] Run focused `tests/test_config.py`, `tests/test_api_contract.py`, `tests/test_deployment_phaseE.py`, and documentation/example checks, then the applicable full Plan E suite and lint/type checks; record measured counts and failures, confirm no stale active-surface environment names or 22-table claims remain, and confirm the diff stays within Plan E without changing `CONTRACTS.md`, Plan F, or semantic authority/consistency contracts.
    **Completion:** Plan E fully complete and QA-approved. 18/18 steps done across 4 phases (base 1-3 + Phase 4 QA R2 planning-gap remediation). QA rounds: R1=MINOR(10)/fix round 1 resolved; R2=CRITICAL env-naming/3 planning-gaps/remediation via Phase 4; R3=MINOR(1 docs-only not_ready)/fix round 2 resolved; R4=PASS (final gate, 0 findings). Verification: ruff 0, mypy --strict 0 (170 files), UMD_TEST_POSTGRES=true full suite 418 collected = 414 passed / 4 honest skips (Docker daemon, kubectl/KUBECONFIG, tesseract x2) / 0 failed. Semantic authority/consistency contracts + CONTRACTS.md + Plan F untouched. Plan F (validation/release) NOT implemented per boundary — recommended next.
    **Done:** Final verification recorded (all run with Postgres 17 cluster started; UMD_TEST_POSTGRES=true). (1) ruff check src tests: "All checks passed!" (0 errors). (2) mypy --strict src: "Success: no issues found in 170 source files" (0 errors). (3) focused tests/test_config.py + test_api_contract.py + test_deployment_phaseE.py: 31 passed, 2 skipped, 0 failed. (4) FULL suite `UMD_TEST_POSTGRES=true pytest -q`: 414 passed, 4 skipped, 0 failed (418 collected; the 4 skips are honest/conditional: no Docker daemon [test_deployment_phaseE.py:240], no kubectl/KUBECONFIG [test_deployment_phaseE.py:262], tesseract absent [test_phase3_integration.py:237, test_raster_units.py:167]). (5) sh -n deploy/docker-entrypoint.sh: no output (valid). Stale-name grep over deploy/, .env.example, migrations/, src/umd/deploy/, tests/conftest.py, Makefile, alembic.ini for single-underscore nested names: NO match (exit 1). 22-table grep over README/docs/design-parts: NO match (exit 1). No CONTRACTS.md, DD, or Plan F files touched; semantic authority/consistency contracts unchanged. postgres target / migration ordering / readiness preserved. Docker/Kubernetes remain conditional and were not fabricated.

## Completion Criteria

- Local startup uses documented commands and Compose services expose health/readiness/capabilities; migrations, backups, restore, and replay are verified.
- Operational telemetry explains slow/incomplete decomposition and projection freshness; permanent upcaster, replay, sandbox, CVE, license, and scale-review work is explicitly owned and documented.
- Documentation covers every DD section and concrete limitation/extension, including locator drift, best-effort ASR filtering, parallel-only Vecalign, bounded v1 graph traversal, pyannote/legal gates, Hatchet pin gates, and sandbox posture.
- Packaging, recovery, security configuration, docs lint/link checks, and client examples pass without consumer-specific API terminology.
- QA R2's planning gap is closed: canonical nested environment names are used consistently without silent fallback, configured app/store wiring is directly covered, `/v1/ready` rebuild behavior is deterministic and RFC 7807-compatible, and provider/table/test disclosures are measured and honest.

### Amendment acceptance criteria
- All active deployment/configuration seams agree on `UMD_POSTGRES__DSN`, `UMD_OCFL__ROOT`, and `UMD_PROJECTION__VECTOR_HNSW_MIN_VERSION`; a single-underscore value cannot silently configure the nested `Settings` fields, and migration DSN masking/restoration still works.
- `tests/test_api_contract.py` proves both `build_source_store(settings)` and zero-argument `app_factory()` use the configured OCFL root and Postgres DSN; it no longer monkeypatches the ignored `UMD_POSTGRES_DSN`.
- A deterministic paused-`current_tier1` `/v1/ready` test proves 503, RFC 7807 `not_ready`, `retryable=true`, `x-consistency: rebuild-in-progress`, `Retry-After` at/above the configured bound, and rebuild metadata; the normal ready response remains covered.
- README, shared plan README, deployment/testing/provider docs, and plan annotations say `23` tables, mark Hatchet `GATED`, report the measured post-fix suite, and disclose conditional Docker/Kubernetes skips and any remaining failure; no stale 22/379/380/8-test claims remain in active documentation.
- Focused tests and the applicable Plan E suite, ruff, and strict mypy provide recorded evidence; Docker/Kubernetes remain conditional and are not represented as locally executed when daemons/tools are unavailable.

## References

- `/workspace/Universeity/Task.md` §§22–24, 32–33, 35, 38–40
- `/workspace/Universeity/artifacts/designs/pending/DD-universal-media-decomposer.md` §§Security/observability; Deployment/docs/rollout; Limitations, risks, operational SLOs/release gates
- `/workspace/Universeity/artifacts/designs/process/universal-media-decomposer-final-estimate.md` §§3–5 (permanent work and gates)
