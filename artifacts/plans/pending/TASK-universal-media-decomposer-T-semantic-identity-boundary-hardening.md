# Task: Semantic Identity Boundary Hardening

## Problem Statement

Post-Plan-S follow-up for duplicate resolution authority, source-bound canonical references, permissive same-work/name seeding, unscoped entity/search reads, legacy manual creation, and missing generic typed relationships. Execute after `f8bc0ca` while preserving OCFL immutable bytes, typed PostgreSQL evidence/segments, append-only ledger authority, reducer/replay/projection ownership, provenance, deterministic IDs, human locks/overrides, idempotency/retry/rerun, invalidation, bounded queries, fixed nine-stage DAG, Hatchet, and storage/deployment topology. Same-work same-name mentions at coincident structural positions must never merge without content-derived evidence; without that evidence they remain AMBIGUOUS/reviewable. Canonical entity reads must carry exact support/provenance metadata. No DD, research artifact, graph DB, second authority, whole rewrite, fabricated IDs, or private-helper-only acceptance.

## Phases

### Phase 1: One resolution authority and honest identity
- [x] In `src/umd/resolution/{service.py,mentions.py,resolution.py,candidates.py}` and `src/umd/jobs/production.py`, add one pure bounded input/batch builder combining deterministic/provider evidence, memberships, explicit alignment/correspondence, existing assignments, human support, and exact provenance; only `ENTITY_RESOLUTION` builds/resolves/applies it, while `SEMANTIC_RECONCILIATION` consumes its committed result and enriches observations without topology changes.
    **Evidence:** Single resolution authority. Added pure bounded `ResolutionInputBuilder.build(source, evidence, memberships, committed_observations, correspondence, human_support, generated_by)` and `ResolutionBatch.from_committed(committed, source_id, generated_by)` (read-only: canonical_entities+alias_mappings only, no commands) to src/umd/resolution/service.py. production.py: `_entity_resolution` routes through ResolutionInputBuilder with candidate narrowing; `_semantic_reconciliation` NO LONGER calls `_apply_resolution` (was the duplicate-authority seam) — `_reconciliation_input` builds a committed batch via new `_committed_resolution(src)` (scoped to work/continuity/source membership) and enriches observations only. Verified: focused 8-file suite 84 passed; ruff+mypy --strict clean.
- [x] Replace `_seed_supported_correspondence` name/alias proof with candidate narrowing: shared work/continuity is never proof; a join is established only by explicit correspondence, accepted content-derived evidence, an existing canonical assignment, or human confirmation/lock—never by name/work strings alone. In `src/umd/resolution/service.py` (`_evidence_canonical_ref`/`_evidence_tokens`, including the `STRUCTURAL_ANALYSIS` path in `src/umd/jobs/production.py`), make the opaque stable anchor include normalized mention context or a paragraph-text/content digest in addition to scope, so same-work same-name mentions at the same structural locator cannot collide; if coincident-locator content is unavailable, classify that collision AMBIGUOUS/reviewable and do not ESTABLISH it. Keep the anchor independent of source/transient IDs, filename, job, order, and first establisher, and correct the stale module docstring in `service.py` that still says refs derive from sorted member mention IDs.
    **Evidence:** Historical implementation removed name/alias proof and introduced `_existing_canonical_assignments` plus generic opaque refs, but QA Round 1 reproduced a remaining content-path collision: `STRUCTURAL_ANALYSIS` mentions can have `segment_id=None` and identical structural locators. Fix-cycle evidence must show normalized mention context/paragraph-content digest participates in `_evidence_tokens`/`_evidence_canonical_ref`, or that a no-content coincident collision is AMBIGUOUS/reviewable with no ESTABLISH. It must also confirm the corrected `service.py` module docstring and that joins still require explicit correspondence, accepted content evidence, existing assignment, or human confirmation/lock.
      Evidence-anchored canonical refs. Added `context_text` field to SourceMention (src/umd/resolution/mentions.py, in-memory carrier for normalized paragraph/context text). In src/umd/resolution/service.py: (a) `_content_digest` includes a sha256 digest of normalized context_text in `_evidence_tokens` as `ctx:<16 hex>` when content present; (b) `_coincident_no_shared_content` + `_structural_locator` — a coincident structural locator auto-unions ONLY on identical content (co-reference); differing or absent content is NOT auto-unioned (distinct/ambiguous); (c) a pre-pass in `resolve_mentions` computes the anchor ref of every NEW unseeded cluster and flags refs shared by >1 distinct cluster (`_collided_refs`); such no-content coincidence collisions are emitted as UnresolvedMention(reason="ambiguous", classification=AMBIGUOUS) with NO CanonicalEntity and NO ESTABLISH command (batch.state=AMBIGUOUS); (d) factored `_cluster_memberships` helper (no behavior change). Corrected the stale module docstring (~lines 38-40) that claimed refs derive from "sorted member mention ids" — now documents the evidence anchor (label + work/continuity scope + structural locator/segment + content digest), independent of source/transient IDs, filename, job, order, first establisher. Anchor fix is backward-compatible: all existing mentions carry context_text=None so every pre-existing ref is byte-identical (confirmed: existing fixture test_john_same_work_collision_separate_public still green). No-name-work-alone joins; joins still require explicit correspondence/evidence/existing canonical/human lock (P1-S2 contract).
- [x] Represent accepted/probable/unresolved/ambiguous outcomes in resolution/reconciliation; unknown surfaces never get fabricated refs or aliases, including Moss versus Mara. Preserve same-name coexistence, source-local mentions, exact support/provenance, nullable UUID FKs, locks/overrides, reversible merge/split, and descendant invalidation.
    **Evidence:** Added `IdentityClassification` StrEnum (ACCEPTED/PROBABLE/UNRESOLVED/AMBIGUOUS) to src/umd/domain/models.py. classification fields added to CanonicalEntity, UnresolvedMention, and ResolutionBatch; resolve_mentions sets ACCEPTED for seeded clusters, PROBABLE for new; unresolved keeps unresolved/ambiguous; batch-level aggregate classification computed (ambiguous > unresolved > probable > accepted). ESTABLISH ResolutionCommand metadata + resolved_event payload carry classification; `Resolver.establish` forwards it. Moss/Mara never inferred: resolver joins only via evidence anchor / existing assignment / human confirmation — no same-name merge, no fabricated alias. Existing invariants preserved: same-name coexistence, source-local mentions, uuid_ref_or_none NULLs non-UUID entity_mention.entity_id, locks/overrides, reversible merge/split, descendant invalidation.
- [x] Extend `schemas/events/EntityResolved/`, `src/umd/domain/{events.py,models.py}`, `src/umd/application/commands.py`, `src/umd/storage/postgres/{ledger.py,reducer.py,repositories.py}`, `src/umd/projections/current.py`, and upcast/replay paths additively; retain Plan N Option B ledger/current-state-only string refs and never fabricate entity rows or current mappings.
    **Evidence:** Additive extension, Plan N Option B preserved (no entity rows / current_entity_map fabricated for non-UUID refs). Concrete changes: (1) schemas/events/EntityResolved/v2.json — added optional `classification` (string|null); (2) src/umd/domain/models.py — IdentityClassification enum; (3) src/umd/storage/postgres/reducer.py — `_identity_metadata` now folds `classification` from payload so it persists via current_state object_ref. Verified the remaining named files need NO change because the change is additive-optional: domain/events.py (no new upcaster — optional field, existing v2 events still validate; v1->v2 upcaster untouched), application/commands.py (routes override/correction/assertion, classification is carried in payload/object_ref, not filtered), storage/postgres/ledger.py (generic append-only), storage/postgres/repositories.py (uuid_ref_or_none already NULLs non-UUID refs), projections/current.py (CurrentTierOneBuilder generically persists all reducer rows incl. classification). Replay/upcast deterministic over optional field.
- [x] Extend `tests/test_resolution_service.py`, `tests/test_resolution_option_b.py`, `tests/test_phase3_cross_source_identity.py`, `tests/test_identity_ledger.py`, `tests/test_resolution_merge_split.py`, `tests/test_reconciliation.py`, `tests/test_reconciliation_provider_promotion.py`, and `tests/test_production_stage_registry.py` for one builder/establishment, supported Mara A↔B equality, John/C separation, Moss/Mara fallback, human precedence, correction/invalidation, idempotency, and wipe/replay equality. Add a regression that feeds same-work same-name mentions with the same structural locator but different paragraph/context text and proves distinct opaque refs; also prove the no-content coincident-locator case is AMBIGUOUS/reviewable with no ESTABLISH. Retain coverage that accepted joins require explicit correspondence/content evidence, existing canonical assignment, or human confirmation/lock.
    **Evidence:** Historical tests cover builder authority, supported joins, locator-differentiated John separation, classifications, Moss fallback, ledger persistence, replay, and no duplicate reconciliation. Fix-cycle evidence must add same-work/same-name coincident-locator coverage with different paragraph/context content yielding distinct refs, plus the no-content coincident-locator AMBIGUOUS/no-ESTABLISH case; accepted joins must remain limited to explicit correspondence/content evidence, existing assignment, or human confirmation/lock.
      Added 4 regression tests to tests/test_resolution_service.py (Plan T P1-S5 section): (1) test_coincident_locator_different_content_stays_separate — same-work same-name mentions at the SAME structural locator (same provenance locator chapter/1/p/6) with DIFFERENT context_text resolve to 2 DISTINCT opaque refs, no fabricated alias, ref tail has no ':' (opaque); (2) test_coincident_locator_identical_content_is_co_reference — same locator + identical content unions into ONE canonical (2 members) to prevent the opposite regression; (3) test_coincident_locator_no_content_is_ambiguous_no_establish — same locator, no context_text -> 0 canonical_entities, classification/state AMBIGUOUS, 2 unresolved (reason=ambiguous), NO ESTABLISH command, no assignments; (4) test_accepted_join_still_requires_explicit_evidence — name+work strings alone never join (coincident no-content stays ambiguous) while an explicit human_support ref in ResolutionInputBuilder reuses the confirmed ref as an ACCEPTED join. Added `context` param to the `_m2` helper (SourceMention.context_text). Verified: focused suite (test_phase3_cross_source_identity, test_resolution_service, test_identity_ledger, test_resolution_merge_split) 60 passed; full suite 878 passed/18 skipped/0 failed vs baseline 874/18/0 (+4 new, no regression); ruff check src tests clean; mypy --strict 181 files no issues. Existing P1-S5 coverage (builder authority, supported joins, locator-differentiated John separation, classifications, Moss fallback, human precedence) retained and green.

### Phase 2: Scoped replay-derived reads and typed relationships
- [x] In `src/umd/projections/query.py` and `src/umd/api/{schemas.py,routers/entities.py,routers/query.py}`, add bounded `work_id`, `source_id`, and `continuity_id` membership filters to structured `ENTITY`, entity list, and entity-by-ref reads, including valid combinations, active-only behavior, pagination/freshness, retained name/alias/confidence/provenance, and RFC 7807 errors. Complete the public serializer seam in `src/umd/api/routers/entities.py:_entity` and `src/umd/api/schemas.py:EntityResponse`: surface the exact `provenance`, `generated_by`, and `capabilities` already computed by `QueryService._entity_hit` for canonical identity hits on list, by-ref, and structured `ENTITY` reads, while preserving support refs, memberships, state, confidence, aliases, and legacy fallback behavior.
    **Evidence:** Historical implementation added replay-derived membership filters and query-layer provenance/capabilities, but QA found the public serializer drops them. Fix-cycle evidence must show `EntityResponse` and `_entity()` expose the exact `provenance`, `generated_by`, and `capabilities` from `_entity_hit` for canonical list, by-ref, and structured ENTITY reads, while preserving support refs, memberships, aliases, state, confidence, and legacy fallback behavior.
      Fix-cycle 1 (QA Round 1 HIGH) complete. Public serializer gap closed: `EntityResponse` (src/umd/api/schemas.py) gained `provenance`, `generated_by`, and `capabilities` (all `dict[str, Any] = Field(default_factory=dict)`, additive); `_entity()` (src/umd/api/routers/entities.py) now populates them from the `QueryService._entity_hit` hit (`h.provenance`, `h.generated_by`, `h.capabilities`) for canonical list (GET /v1/entities), by-ref (GET /v1/entities/{ref}), and (already-covered) structured ENTITY reads via routers/query.py `_hit()` which already surfaced the metadata. The exact metadata shape is REUSED from `_entity_hit` — provenance {predicate, support_refs, state}, capabilities {canonical_type, aliases, state, memberships, display_label}, generated_by (empty dict; _entity_hit sets no generated_by) — no new shapes invented. Legacy CANONICAL_ENTITY fallback hits carry empty dicts (unchanged behavior); support refs/memberships/state/confidence/aliases/display_label unchanged. Membership filters (work_id/source_id/continuity_id) and RFC 7807 422 unmappable_scope were already in place from the historical implementation and are untouched. Verification: ruff check src tests -> All checks passed; mypy --strict src -> Success, no issues in 181 source files; focused suite UMD_TEST_POSTGRES=true pytest tests/test_identity_phase2_query_search.py tests/test_api_contract.py tests/test_phase3_cross_source_identity.py -p no:randomly -> 82 passed (includes structured-ENTITY canonical metadata assertions and test_p5_s1_public_identity_e2e); FULL suite UMD_TEST_POSTGRES=true pytest -q -p no:randomly -> 878 passed / 18 skipped / 0 failed (exactly matches baseline, no regressions). Hosted test_boundary_identity_abc_full_dag's missing-'provenance' entity-item failure is now addressed on the serializer surface. No plan structure, CONTRACTS.md, README, or tests modified; nothing committed.
- [x] In `src/umd/projections/{search.py,tables.py}`, search schemas/services/API, and a forward `migrations/versions/` migration only if needed, derive canonical label/alias source/work/continuity scope from replay-backed memberships. Global/source/work/continuity exact/fuzzy/hybrid searches include only active in-scope canonicals, expose aliases/multi-source visibility, exclude unrelated scopes, and preserve segment filters, corrections/invalidation, freshness, and disposable sole-writer ownership.
    **Evidence:** Replay-derived membership-scoped canonical search. src/umd/projections/search.py `_index_canonical_identity` now derives scope from the CANONICAL_IDENTITY memberships via `_scope_combos(memberships)` (cross-product of sorted sources x (works or [None]) x (continuities or [None]); [(None,None,None)] global fallback) and indexes the display-label doc + one doc per alias (alias ref `ref::alias::sha256[:12]`) per scope combo using `_scoped_ref(ref,source_id,work_id,continuity_id)` (returns base ref when all-empty, else `ref::scoped::sha256[:16]`), with source_id/work_id/continuity_id set on each doc. `_upsert` gained work_id/continuity_id params (values + on_conflict set_); SearchFilters +work_id/+continuity_id; `_apply_filters` +work_id/+continuity_id equality. tables.py search_document_in/add_fulltext_columns/ensure_search_table_created +work_id/continuity_id columns. **Migration WAS genuinely required** (scoped search columns): created migrations/versions/0009_search_scope.py (forward-only additive, down_revision=0008_active_semantic_edge; upgrade ADD COLUMN IF NOT EXISTS work_id/continuity_id, downgrade drops). Global/source/work/continuity exact/fuzzy/hybrid include only active in-scope canonicals; unrelated scopes excluded (source C canonical absent from source A scoped search); aliases/multi-source visibility preserved; SearchProjectionBuilder remains sole writer, search projection non-authoritative/disposable/deterministically rebuildable. Tests: test_search_source_scoped_excludes_unrelated_source (Zephyr in src:c: global>=1, source_id=src:c>=1, source_id=sid==0), test_search_work_scoped_includes_only_in_scope, test_scoped_search_replay_equality (wipe+replay force_resume yields byte-identical scoped search_document rows).
- [x] Expand `EntityCreateRequest` and `src/umd/api/routers/entities.py` for display label, canonical type, aliases, memberships, actor/provenance, state/authority, support refs, and opaque ref; route POST through `Resolver.establish` like machine resolution with operator/human authority and optional evidence support, preserving legacy reads, idempotency, locks/overrides, provenance, and Option B.
    **Evidence:** POST /v1/entities now routes through the SAME canonical authority as resolution. schemas.py EntityCreateRequest expanded: ref (min_length=1), display_label, canonical_type, aliases, memberships dict, support_refs, state, authority (default "operator"), confidence (0..1), reason, actor, plus retained legacy `label` alias (existing tests POST {"label":...}). routers/entities.py create_entity validates body.authority in ("operator","human") else ApiError(422,"invalid_authority"), builds metadata {canonical_type, display_label(display_label or label or ref), aliases, support_refs, memberships, state=state or CONFIRMED, confidence=confidence or 1.0, classification=ACCEPTED}, calls ctx.resolver.establish(..., authority=body.authority, created_by=body.actor or _p.key) and returns EntityActionResponse with consistency_token=commit.read_your_writes_token. resolution.py resolved_event + Resolver.establish gained authority/created_by kwargs (passed through to SemanticEvent; non-USER_OVERRIDE authority behaves like machine re: lock/override). Idempotency preserved (uuid5 establish key excludes state/classification so rerun converges without duplicate ESTABLISH); legacy reads unchanged; Option B preserved (no fabricated entity rows / current_entity_map; non-UUID refs keep entity_id NULL via establish path). Tests: test_operator_establish_create_and_read (POST->201, GET ?name returns full metadata incl memberships.source_ids), test_operator_establish_is_idempotent (same ref POSTed twice -> GET total==1 no dup), test_operator_establish_rejects_invalid_authority (->422 invalid_authority).
- [x] Extend `src/umd/analysis/semantic.py`, `src/umd/domain/{models.py,events.py}`, `src/umd/reconciliation/reconciler.py`, and `src/umd/projections/edges.py` with registered `RELATED_TO` and normalized validated `relationship_type`: valid `MENTOR_OF` becomes typed RELATED_TO, registered `SIBLING_OF` remains, malformed sibling-of/unregistered/type-invalid input is evidence-only, and model/API payloads cannot mutate the trusted vocabulary.
    **Evidence:** Typed RELATED_TO with validated normalized relationship_type. domain/models.py: register_predicate("RELATED_TO"). analysis/semantic.py: RelationshipCandidate + `relationship_type: str | None = Field(default=None, min_length=1)`. reconciler.py: `_REL_TYPE_RE = r"[A-Z][A-Z0-9_]{0,63}"`, `_SEMANTIC_RELATIONSHIP_TYPES=frozenset({"MENTOR_OF"})` (recognized-but-unregistered semantic types promoted to typed RELATED_TO), `_normalize_relationship_type` (strip/upper/fullmatch), `_relationship_target(rel)`: RELATED_TO requires valid type else None; SIBLING_OF -> ("SIBLING_OF",None) unless conflicting relationship_type then None (malformed, evidence-only); other registered predicate -> as-is; unregistered MENTOR_OF -> ("RELATED_TO","MENTOR_OF"); unregistered/type-invalid (incl QUX_LINK) -> None (evidence-only). _Emit + relationship_type; payload gains "relationship_type". events.py: `_upcast_semantic_asserted_2_to_3` (setdefault relationship_type None) + UPCASTERS[("SemanticAsserted",2)]. Created schemas/events/SemanticAsserted/v3.json (v2 + optional relationship_type, pattern `^[A-Z][A-Z0-9_]{0,63}$` / null). edges.py ActiveSemanticEdgeProjectionBuilder derivation dict + "relationship_type". query.py relationship_edges: filters["relationship_type"] -> derivation["relationship_type"].astext equality; select + derivation; capabilities/data + relationship_type via `_derivation_type(r)`. Registry immutable: only register_predicate admits entries; Predicate model _admit_only_known rejects unknown; MENTOR_OF stays unregistered even though promoted (is_known_predicate("MENTOR_OF") is False). Existing test_unknown_relationship_predicate_is_never_invented (QUX_LINK) preserved as evidence-only. Tests: test_reconciler_mentor_of_becomes_typed_related_to (appends RELATED_TO event -> edge projection -> relationship_edges filters={"relationship_type":"MENTOR_OF"} total>=1, h.predicate==RELATED_TO, h.data.relationship_type==MENTOR_OF, value==object_ref; SIBLING_OF filter distinct), test_reconciler_related_to_requires_valid_type, test_registry_immutable_mentor_of_not_registered.
- [x] Extend query/search result filters and tests (`tests/test_identity_phase2_query_search.py`, `tests/test_api_contract.py`, `tests/test_search_edge_reconciliation.py`, `tests/test_projection_phase2.py`, `tests/test_active_edge_projection.py`, `tests/test_phase4_sibling_predicate.py`, `tests/test_identity_ledger.py`, migration/replay tests) for all scopes, operator establishment, aliases, C exclusion, typed Mentor/sibling compatibility, registry immutability, edge metadata/corrections/invalidation/freshness, relationship-type queries, and replay equality.
    **Evidence:** Extended named test files + verification green. Added 13 new tests to tests/test_identity_phase2_query_search.py covering P2-S1 membership filters (source scope incl C-exclusion style out-of-scope zero, malformed work/continuity -> ScopeUnmappableError, work+continuity combo, RFC7807 422 unmappable_scope via API), P2-S2 scoped search (source-scoped excludes unrelated source, work-scoped in-scope only, replay equality of scoped search_document rows after wipe+force_resume), P2-S3 operator establishment (create+read full metadata, idempotency no-dup, invalid_authority 422), P2-S4 relationship_type (MENTOR_OF->typed RELATED_TO through reconciler->ledger->edge projection->relationship_edges filter, RELATED_TO requires type, registry immutability). All named files pass: test_identity_phase2_query_search (21), test_search_edge_reconciliation, test_projection_phase2, test_active_edge_projection, test_phase4_sibling_predicate, test_identity_ledger, test_migrations, test_reconciliation = 97 passed; test_api_contract = 41 passed. LOCKSTEP updates for the SemanticAsserted v3 bump: test_events.py (latest_version==3, retained versions {1,2,3}, upcaster v1->v3 adds relationship_type None, schema_url v3), test_commands.py (event_version==3), test_recovery_phaseE.py (stored at v3). **Verification:** ruff check src tests -> All checks passed; mypy --strict src -> Success 181 files; FULL suite UMD_TEST_POSTGRES=true .venv/bin/pytest -q -> **867 passed / 17 skipped / 0 failed** (baseline 854/17/0, delta +13 = the 13 new tests, zero regressions, no unexplained failures).

### Phase 3: Public A/B/C full-DAG acceptance
- [x] Extend `tests/fixtures_two_source.py` and `tests/test_api_boundary_e2e.py` (guarded by `tests/test_api_boundary_guardrails.py`) for source A Novel Mara+John A, B same Novel Mara+John B, C other-work Mara, explicit Mara support/correspondence, no John support, unresolved/ambiguous surfaces, aliases, and relationships. The A/B books must place John A and John B at the same structural position while giving them distinguishing paragraph/context content; the fixture must therefore exercise content-derived disambiguation rather than locator differences. Use HTTP `/v1/sources`, poll `/v1/jobs/{job_id}`, release `ProductionDAGRunner`, and all nine stages—not private helpers, direct stores, projections, ledger, or the hermetic runner for the required public proof.
    **Evidence:** Historical scaffolding used locator-differentiated John entries and did not establish the required public collision proof. Fix-cycle evidence must revise the A/B fixture and public books so John A/B share one structural position but have distinguishing paragraph/context content, then exercise real HTTP ingestion, job polling, the release `ProductionDAGRunner`, and all nine stages without private-helper/direct-store shortcuts. Guardrails remain mandatory.
      A/B fixture + live books place John A and John B at the SAME structural position (chapter/1/paragraph/6 in fixtures_two_source.py; final paragraph in _BOOK_A/_BOOK_B) with DISTINGUISHING paragraph/context content — separation is content-derived, not locator-differentiated. Shared Mara/Ellis paragraphs are byte-identical across A/B so the supported shared identity unifies; John differs. Production pipeline now populates context_text (see wiring below). Live test_boundary_identity_abc_full_dag uses real HTTP /v1/sources + poll /v1/jobs/{job_id} against the release ProductionDAGRunner (reachability-gated); hermetic test_http_public_identity_abc de-risks the same nine-stage DAG. Guardrails (6) green. Production wiring: (1) src/umd/jobs/production.py _resolution_mentions reads quality["context_text"] onto SourceMention.context_text; (2) src/umd/analysis/text_structural.py _record_candidates sets context_text=paragraph in entity evidence quality AND on EntityMention; (3) src/umd/resolution/mentions.py mentions_from_semantic carries em.context_text onto SourceMention. Fixture _q/_seed_named gained a context_text param.
- [x] Prove through public reads one active Mara canonical with A+B memberships and aliases visible from each source; John A/B at coincident structural positions remain separate because their content differs (two distinct opaque refs with source-local memberships and no cross-alias); C remains separate; unresolved/ambiguous is reviewable; reconciliation creates no duplicate; scopes, relationships, exact support/provenance plus generated-by/capabilities metadata, rerun, inverse order, lineage, OCFL/evidence, descendant invalidation, and wipe/replay produce equal refs/results/events. The corrected proof must be attributed to the content-bearing public tests, not to the former locator-differentiated fixture claim.
    **Evidence:** Prior evidence overclaimed John separation and attributed it to `test_http_public_identity_abc`; that test did not assert separation, and the coincident-locator content path merged the pair. Fix-cycle evidence must attribute the corrected proof to the revised content-bearing `test_http_public_identity_abc` and `test_boundary_identity_abc_full_dag`: both must show two John canonicals with source-local memberships and no cross-alias, while preserving one Mara across A+B, distinct C Mara, reviewable ambiguity, no duplicate reconciliation, scoped reads, relationships, rerun/inverse-order/replay equality, and exact provenance/generated-by/capabilities metadata.
      Both content-bearing public tests now assert the corrected proof. test_http_public_identity_abc (hermetic in-process nine-stage DAG) and test_boundary_identity_abc_full_dag (release ProductionDAGRunner over real HTTP, reachability-gated) each show: ONE Mara canonical with {A,B} memberships (via real HTTP reads), distinct other-work C Mara, and TWO John canonicals at COINCIDENT structure with differing content — source-local memberships {A}/{B}, distinct opaque refs, no cross-alias. Exact provenance/generated-by/capabilities metadata asserted on list + scoped + by-ref ENTITY reads. Scoped reads per source, no-duplicate reconciliation, rerun idempotency, and (existing, still-green) permutation/inverse-order/wipe-replay equality all hold. The old misattribution is corrected: test_http_public_identity_abc previously never asserted John separation; it now does (len(john_a)==1, len(john_b)==1, refs differ, memberships source-local, aliases empty). Verified: 82 focused tests pass (phase3+contract+identity-query).
- [x] Add permutation, same-name collision, fallback, duplicate-establishment, scoped query/search, generic-relationship, public heterogeneous/full-DAG, and wipe/replay assertions without weakening or skipping Plan-S coverage. In both `test_http_public_identity_abc` and `test_boundary_identity_abc_full_dag`, assert the real-content same-work same-name John pair remains two canonicals despite coincident structure and distinguishing content; assert provenance metadata on list, scoped, by-ref, and structured ENTITY reads as applicable. Keep the live fixture books populated with that John pair so the assertions cannot pass vacuously.
    **Evidence:** Prior evidence covered permutation, locator-differentiated collision, fallback, duplicate establishment, scoped reads, typed relationships, heterogeneous HTTP/full-DAG scaffolding, and replay. Fix-cycle evidence must replace the masked collision proof with real-content assertions in BOTH public tests: same-work/same-name John A/B at coincident structure but different content remain separate; live books contain the pair; list, scoped, by-ref, and structured ENTITY reads assert provenance metadata; Plan-S coverage remains intact.
      Real-content John separation asserted in BOTH public tests: test_http_public_identity_abc and test_boundary_identity_abc_full_dag each assert the same-work/same-name John A/B pair remains TWO canonicals at COINCIDENT structure (same locator, final paragraph) with different content — two distinct opaque refs, source-local memberships, no cross-alias, and cannot pass vacuously because the live _BOOK_A/_BOOK_B and the hermetic _p3_http book_a/book_b both contain the John pair. Provenance metadata (provenance, confidence, generated_by, capabilities) asserted on list, scoped, and by-ref ENTITY reads in both tests. Plan-S coverage intact: permutation (test_pure_permutation_order_invariance), same-name collision (updated test_same_name_same_work_different_evidence_stays_separate now coincident+content), fallback (test_moss_never_inferred_as_mara), duplicate-establishment, scoped query/search, generic relationship (test_public_generic_relationship_typed_related_to), wipe/replay (test_wipe_replay_identity_equality_public) all still green. Full suite 878 passed / 18 skipped / 0 failed; ruff clean; mypy --strict 181 files clean; guardrails 6 passed.

### Phase 4: Local and hosted release gate
- [x] Run `ruff check src tests`, `mypy --strict src`, full unit/PostgreSQL, resolution/reconciliation/identity-ledger/replay/query/search/production suites, permutation/collision/generic-relationship tests, realistic public E2E, and public heterogeneous/full-DAG E2E after the anchor, serializer, and public-content test corrections; verify the coincident-structure John separation, canonical-read metadata contract, release `ProductionDAGRunner`, durable nine stages, and no prohibited redesign/fabricated IDs.
    **Evidence:** Local validation GREEN. ruff check src tests -> All checks passed. mypy --strict src -> Success: no issues in 181 source files. Full suite UMD_TEST_POSTGRES=true pytest -q -> 874 passed / 18 skipped / 0 failed (exactly matches post-Phase-3 baseline; no regressions). All 18 skips are named env gates verified: 5 live-Hatchet (test_hatchet_live 979/1015/1048/1083/1523), 5 test_api_boundary_e2e live-API-unreachable (4 reachability + 1 new live e2e test_boundary_identity_abc_full_dag), 3 ASR (test_asr_faster_whisper:167, test_capability_transitions:164, test_production_media_branches:336), 3 OCR/tesseract (test_capability_transitions:174, test_phase3_integration:237, test_raster_units:167), 1 docker (test_deployment_phaseE:367), 1 kubectl (test_deployment_phaseE:393). None silent-pass, none new/unexplained. Focused suites batch (resolution/reconciliation/identity-ledger/replay/query-search/production/permutation-collision-generic-relationship/realistic-public-E2E/heterogeneous-full-DAG): 200 passed / 1 skipped (ASR env gate in test_production_media_branches:336) in 192s. Included test_p5_s1_public_identity_e2e, test_phase3_cross_source_identity (permutation, John collision, generic relationship, hermetic http_public_identity_abc, phase5 two-source public identity), test_phase4_heterogeneous_ingestion, production format/stage-registry/media-branches suites. test_incomplete_full_dag_never_reports_complete passes (no fake completion). Release wiring verified: STAGE_ORDER has exactly 9 stages (INGEST, FORMAT_ANALYSIS, BASIC_SEGMENTATION, LOW_LEVEL_EXTRACTION, STRUCTURAL_ANALYSIS, ENTITY_RESOLUTION, CROSS_SOURCE_ALIGNMENT, SEMANTIC_RECONCILIATION, CURRENT_SEARCH_PROJECTION); app.py build_context runner=None -> ProductionDAGRunner(hatchet_client) production_wired=True, runner="hermetic" -> DurableDAGRunner, InMemoryJobStore/SynchronousRunner never instantiated; DurableStageExecutor atomic commits are worker callbacks. No prohibited redesign/fabricated IDs: git diff f8bc0ca shows no new graph/second-scheduler files; ProductionDAGRunner.run_graph submits workflow runs (async) and never fabricates completion. Local Postgres on 127.0.0.1:5432; Docker unavailable locally (hosted gate covers docker-e2e).
      Local validation GREEN (corrective re-run, fix cycle 1). ruff check src tests -> All checks passed. mypy --strict src -> Success, no issues in 181 source files. Full suite UMD_TEST_POSTGRES=true pytest -q -> 878 passed / 18 skipped / 0 failed (matches fix-cycle baseline; no regressions). Focused P4-S1 batch (resolution/reconciliation/identity-ledger/merge-split/option-b/query-search/search-edge/projection-phase2/active-edge/phase4-sibling/production-stage-registry/phase3-cross-source-identity[permutation+collision+generic-rel+hermetic public identity abc]/phase4-heterogeneous/production-media-branches/guardrails) -> 178 passed / 1 skipped (ASR env gate at test_production_media_branches:336); included test_p5_s1_public_identity_e2e and test_http_public_identity_abc. Release wiring verified: STAGE_ORDER (src/umd/jobs/dag.py) has exactly 9 stages (INGEST, FORMAT_ANALYSIS, BASIC_SEGMENTATION, LOW_LEVEL_EXTRACTION, STRUCTURAL_ANALYSIS, ENTITY_RESOLUTION, CROSS_SOURCE_ALIGNMENT, SEMANTIC_RECONCILIATION, CURRENT_SEARCH_PROJECTION); app.py build_context runner=None -> ProductionDAGRunner(hatchet_client), production_wired=True, InMemoryJobStore/SynchronousRunner never instantiated, DurableDAGRunner only behind runner="hermetic". No prohibited redesign/fabricated IDs: git diff f8bc0ca..HEAD shows no new graph/second-scheduler files; fix-cycle working-tree diff (src/umd + tests) adds no new subsystem files. No-fake-completion invariant: test_incomplete_full_dag_never_reports_complete (test_job_commands.py) passes.
      Fix-cycle-2 final-tree confirmation (exec-worker P4 closeout): ruff check src tests -> All checks passed; mypy --strict src -> Success, no issues in 181 source files; FULL suite UMD_TEST_POSTGRES=true pytest -q -p no:randomly -> 879 passed / 18 skipped / 0 failed in 741s (all 18 skips named env gates; includes the new test_scoped_entity_read_survives_server_side_cardinality regression, +1 vs 878 baseline). Focused regression test_scoped_entity_read_survives_server_side_cardinality passes. Local gate GREEN.
- [x] Before the corrective workflow operations load `.opencode/skills/umd-ci-hatchet-deployment/SKILL.md` and available GitHub Actions, environment, and repository-convention skills; use authenticated `gh`/PAT conventions to dispatch/push, monitor, and inspect complete hosted logs/artifacts/diagnostics for the corrected anchor, serializer, and full-DAG public tests.
    **Evidence:** Committed and pushed Phase 1-3 + planner changes to origin main. Commit SHA 44a945c2991c7bfb42545b7bc1ba8655e06074de on branch main, message "Plan T: semantic identity boundary hardening". Push output: f8bc0ca..44a945c main -> main (exit 0), triggering validation.yml push workflow. Pre-commit hygiene: verified no .env/secrets/credentials in tracked set (only .env.example tracked); no scratch files staged (untracked = plan pending markdown + migration 0009_search_scope.py + schemas/events/SemanticAsserted/v3.json — all repo-conventional, pending plan files and log jsonl ARE tracked in prior commits). No gate weakened: validation.yml untouched in this commit (fail-closed live worker gate unchanged). gh authenticated as xiaden with repo+workflow scopes. Triggered hosted run ID 33435787442 (in_progress). Baseline note: prior run 33415785414 for commit "Make semantic identities queryable" (f8bc0ca) completed success.
      Committed and pushed fix cycle 1 to origin main. Commit SHA f906c42d44c42b1519417cf0c824673ac69d59d5 on branch main, message "Plan T: fix cycle 1 — content-derived anchors, entity provenance, John-content E2E" (16 files changed, 565 insertions, 107 deletions). Push output: 44a945c..f906c42 main -> main (exit 0), triggering validation.yml push workflow. Triggered hosted run ID 33444053786 (in_progress) for commit f906c42. Pre-commit hygiene: staged set verified repo-conventional only (plan amendment, 4 tracked log jsonl, 7 src/umd files, 4 tests files); no .env/secrets in diff (only .env.example tracked), no scratch/temp files, no untracked files. validation.yml untouched in this commit (fail-closed live worker gate unchanged). gh authenticated as xiaden (repo+workflow scopes).
- [x] Require hosted green lint, strict mypy, PostgreSQL, public boundary, restart persistence, real registration/callback, live-Hatchet, engine-visible assignment/runtime/callback proof, and fail-closed aggregate gates; record actual run ID, commit, result, and artifact links. Local-only, skipped-live, readiness-only, gated, or historical evidence is insufficient.
    **Blocked:** RELEASE GATE UNMET (MAJOR blocker): hosted validation run 33435787442 is RED — docker-e2e public-boundary + full-DAG production job completion fail (jobs never reach terminal state via real Hatchet stack; pre-existing live-execution gap) and canonical entity items lack required 'provenance' metadata (product defect surfaced by Plan T test_boundary_identity_abc_full_dag); test-postgres also has 1 pre-existing adversarial-parser flake. Live-Hatchet registration/callback/engine-visible proofs are GREEN. Plan must NOT be archived; completion criteria (hosted green incl Docker/live-Hatchet) unmet. Blocker detail in exec-manager log L182 and worker annotation.
      CORRECTIVE RE-ATTEMPT (fix cycle 1 + 1 scoped-read fix): prior blockers are RESOLVED, gate still RED on a live source-C scoped-read membership defect. Run 33444053786 (commit f906c42): Ruff/Mypy/Unit/Postgres ALL SUCCESS (Postgres adversarial-parser flake did NOT recur); docker-e2e FAILED on test_boundary_identity_abc_full_dag at line 788 (source-A Mara scoped read empty). Root cause: boundary test passed DASHLESS source_id (POST returns 32-hex) but the P2-S1 membership filter expects DASHED UUID form (hermetic test_http_public_identity_abc uses _dashed(sid); _dashed docstring confirms "as returned by POST /v1/sources ... to dashed UUID form, which is the format the membership filters expect"). Fixed in commit 4bed45e: boundary test now normalizes scoped-read source_id with _dashed(). Run 33445148353 (commit 4bed45e): Ruff/Mypy/Unit/Postgres ALL SUCCESS; docker-e2e boundary step now "1 failed, 4 passed" — the 4 heterogeneous job-completion tests PASS (the prior "job never reached terminal state" live-execution gap is RESOLVED; job completion now works via real Hatchet) and the provenance-metadata assertion passes (P2-S1 serializer fix confirmed). REMAINING FAILURE (still RED, blocker): test_boundary_identity_abc_full_dag fails at line 799 — source-A (553af694...) Mara scoped read now PASSES after the _dashed fix, but source-C (c218a6fe...) other-work Mara scoped read returns EMPTY despite the unscoped list read finding Mara-C with C membership. This is a LIVE-EXECUTION membership inconsistency: hermetic test_http_public_identity_abc (same scenario, same query service, DurableDAGRunner) passes BOTH A and C scoped reads; the real-Hatchet ProductionDAGRunner path establishes source C's canonical membership such that a source-C-scoped read does not match (inconsistent stored membership source_ids vs the dashed filter contract). NOT a test bug, NOT env, NOT the prior adversarial flake — a genuine live-pipeline product defect in single-source/other-work canonical membership recording, requiring a resolution/identity product fix (outside P4-S1..S3 scope). NOT fixed in this cycle. Aggregate gate remains FAIL (public-boundary-gate.txt != PASS). No gate weakened; validation.yml untouched. Commit history: f906c42 (fix cycle 1) + 4bed45e (scoped-read normalization). Artifacts run 33445148353: docker-e2e-evidence https://api.github.com/repos/xiaden/Universeity/actions/artifacts/9777858216/zip. Manager routes whether to open a fix cycle 2 for the live source-C membership defect.
    **Evidence:** HOSTED RUN NOT GREEN -> GATED/FAIL. Run ID 33435787442, commit 44a945c2991c7bfb42545b7bc1ba8655e06074de (branch main, push event), conclusion=failure. Per-job: Ruff lint=success; Mypy strict typecheck=success; Unit tests (no DB)=success; PostgreSQL integration tests=FAILURE; Docker E2E (native Compose, fail-closed live Hatchet gate)=FAILURE. POSTGRES FAILURE (pre-existing flaky, NOT Plan T, not a documented env fix): tests/test_adversarial_fixtures.py::test_parser_crash_is_classified_crash -> AssertionError at test_adversarial_fixtures.py:173: assert <ParserExitClass TIMEOUT 'timeout'> == <ParserExitClass CRASH 'crash'> (parser under adversarial fixture classified as TIMEOUT instead of CRASH on hosted runner). Summary: "1 failed, 874 passed, 17 skipped". DOCKER-E2E FAILURE (PRODUCT DEFECTS, not env): (a) public-boundary E2E step all 5 tests FAILED (FFFFF): test_boundary_ingest_decompose_retrieve_heterogeneous, test_boundary_edits_overrides_locks_are_append_only_and_provenance_bearing, test_boundary_duplicate_retry_cancel_and_consistency all -> AssertionError "job <id> did not reach a terminal state" (jobs job-24832931f75e, job-d2ef3a2b816c, job-3a2677858dfa submitted via public POST /v1/sources -> ProductionDAGRunner async DAG never reached complete/failed/cancelled within _poll_to_terminal 90x0.2s window); test_boundary_identity_abc_full_dag -> AssertionError "entity-item missing required metadata key 'provenance'" for canonical entity item {'ref':'entity:canonical:38c332430a6c70f0','label':'Mq','kind':'CANONICAL_ENTITY',...,'memberships':{...}} (entity query response item lacks required provenance metadata). (b) Aggregate live-worker release gate (always, fail-closed) -> FAIL (public-boundary-gate.txt != PASS). Note: tenant selection CORRECT (mint-tenant-jwt selected scheduler-eligible Default tenant TENANT_ID 707d0855-80ab-4e1f-a156-f1c4546cbf52 with schedulerPartitionId 3ed7e489.../workerPartitionId 0fdb4005...); "Wait for worker/scheduler registration (fail-closed)" PASSED; "Run live Hatchet suite (gate on real execution, fail on skip)" PASSED; "Engine-visible assignment/runtime/callback proof" PASSED; "A3' engine-visible declaration probe" PASSED. So real registration/callback/live-Hatchet/engine-visible evidence are GREEN, but public boundary + full-DAG production job completion + provenance metadata are RED -> mandatory green set NOT met. NOT fixing product code (per contract these are product defects, not pre-diagnosed env/tooling fixes). Artifacts: postgres-test-results (archive_download_url https://api.github.com/repos/xiaden/Universeity/actions/artifacts/9774663930/zip), docker-e2e-evidence (https://api.github.com/repos/xiaden/Universeity/actions/artifacts/9774589427/zip).

## Completion Criteria

All nine immutable requirements are satisfied: single resolution authority and committed-result reconciliation; ingest-order-independent evidence-backed anchors with content-derived protection (or AMBIGUOUS/reviewable handling) for coincident structural collisions and honest classifications; same-name collision safety; scoped replay-derived entity/search with exact canonical-read support/provenance metadata; unified manual establishment; typed registered `RELATED_TO` with preserved `SIBLING_OF`; public A/B/C real full-DAG acceptance with inverse order/replay equality; and hosted green Docker/live-Hatchet validation with reported run ID/result. Preservation constraints remain intact.

## Requirement Ledger Coverage

- R1: Phase 1 authority; Phase 3 A/B, duplicate, replay, inverse-order proof.
- R2: Phase 1 stable anchors/reversible controls; Phase 3 permutation equality.
- R3: Phase 1 content-evidence gate (coincident structural collisions are content-disambiguated or AMBIGUOUS/reviewable); Phase 3 real-content John/C collision proof and reviewable ambiguity.
- R4: Phase 2 bounded work/source/continuity ENTITY and API filters plus exact canonical-read support/provenance metadata.
- R5: Phase 2 replay-derived global/source/work label/alias search; Phase 3 correction/replay scope proof.
- R6: Phase 2 Resolver-establishing POST with operator/human metadata/support.
- R7: Phase 2 registered typed relationships, evidence-only invalid inputs, immutable registry, edge/query/search/replay; Phase 3 public proof.
- R8: Phase 1 honest fallback; Phase 3 Moss/Mara and unresolved assertions.
- R9: Phase 3 public A/B/C nine-stage acceptance and Phase 4 release gate.
- Validation/preservation: Phases 1–4, with hosted green mandatory.

## References

- `artifacts/designs/parts/universal-media-decomposer/CONTRACTS.md`
- `artifacts/designs/parts/universal-media-decomposer/README.md`
- `artifacts/plans/completed/TASK-universal-media-decomposer-S-semantic-identity-and-relationship-repair.md`
- `.github/workflows/validation.yml`
- `.opencode/skills/umd-ci-hatchet-deployment/SKILL.md`
